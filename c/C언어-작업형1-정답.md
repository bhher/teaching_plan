# C언어 작업형1 평가 문제 정답

---

## 문제 1: 배열의 최댓값과 최솟값 찾기 정답

### 📁 problem1.c

```c
#include <stdio.h>

int main() {
    int arr[6];
    int i;
    int max, min;
    
    // 6개의 정수 입력받기
    for (i = 0; i < 6; i++) {
        scanf("%d", &arr[i]);
    }
    
    // 첫 번째 값을 최댓값과 최솟값의 초기값으로 설정
    max = arr[0];
    min = arr[0];
    
    // 나머지 값들과 비교하여 최댓값과 최솟값 찾기
    for (i = 1; i < 6; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
        if (arr[i] < min) {
            min = arr[i];
        }
    }
    
    // 결과 출력
    printf("최댓값: %d\n", max);
    printf("최솟값: %d\n", min);
    
    return 0;
}
```

---

### 🔍 코드 설명

#### 1. 변수 선언
```c
int arr[6];    // 6개의 정수를 저장할 배열
int i;         // 반복문에 사용할 변수
int max, min;  // 최댓값과 최솟값을 저장할 변수
```

#### 2. 입력 처리
```c
for (i = 0; i < 6; i++) {
    scanf("%d", &arr[i]);
}
```
- 반복문으로 6개의 정수를 배열에 저장
- `&arr[i]`: 배열의 i번째 요소의 주소

#### 3. 최댓값과 최솟값 찾기
```c
max = arr[0];  // 첫 번째 값을 최댓값으로 초기화
min = arr[0];  // 첫 번째 값을 최솟값으로 초기화

for (i = 1; i < 6; i++) {
    if (arr[i] > max) {  // 현재 값이 최댓값보다 크면
        max = arr[i];    // 최댓값 갱신
    }
    if (arr[i] < min) {  // 현재 값이 최솟값보다 작으면
        min = arr[i];    // 최솟값 갱신
    }
}
```

**동작 순서:**
1. 첫 번째 값을 최댓값과 최솟값의 초기값으로 설정
2. 두 번째 값부터 마지막까지 순회하며 비교
3. 더 큰 값을 찾으면 최댓값 갱신
4. 더 작은 값을 찾으면 최솟값 갱신

---

### 📊 실행 흐름 예시

```
입력: 12 7 25 9 18 3

초기값: max = 12, min = 12

i=1: arr[1]=7
  - 7 > 12? 아니오 → max 유지
  - 7 < 12? 예 → min = 7

i=2: arr[2]=25
  - 25 > 12? 예 → max = 25
  - 25 < 7? 아니오 → min 유지

i=3: arr[3]=9
  - 9 > 25? 아니오 → max 유지
  - 9 < 7? 아니오 → min 유지

i=4: arr[4]=18
  - 18 > 25? 아니오 → max 유지
  - 18 < 7? 아니오 → min 유지

i=5: arr[5]=3
  - 3 > 25? 아니오 → max 유지
  - 3 < 7? 예 → min = 3

결과: max = 25, min = 3
```

---

## 문제 2: 평균 이상인 값의 개수 정답

### 📁 problem2.c

```c
#include <stdio.h>

int main() {
    int arr[8];
    int i;
    int sum = 0;
    float average;
    int count = 0;
    
    // 8개의 정수 입력받기
    for (i = 0; i < 8; i++) {
        scanf("%d", &arr[i]);
    }
    
    // 합계 계산
    for (i = 0; i < 8; i++) {
        sum += arr[i];
    }
    
    // 평균 계산 (형변환 필요)
    average = (float)sum / 8;
    
    // 평균 이상인 값의 개수 세기
    for (i = 0; i < 8; i++) {
        if (arr[i] >= average) {
            count++;
        }
    }
    
    // 결과 출력
    printf("평균: %.2f\n", average);
    printf("평균 이상인 값의 개수: %d\n", count);
    
    return 0;
}
```

---

### 🔍 코드 설명

#### 1. 변수 선언
```c
int arr[8];      // 8개의 정수를 저장할 배열
int i;            // 반복문에 사용할 변수
int sum = 0;      // 합계를 저장할 변수 (초기값 0)
float average;    // 평균을 저장할 변수 (실수형)
int count = 0;    // 평균 이상인 값의 개수 (초기값 0)
```

#### 2. 합계 계산
```c
for (i = 0; i < 8; i++) {
    sum += arr[i];  // 각 요소를 합계에 누적
}
```

#### 3. 평균 계산
```c
average = (float)sum / 8;
```
- `(float)sum`: 정수를 실수로 형변환
- 실수 나눗셈을 위해 형변환 필수
- 정수 나눗셈(`sum / 8`)을 하면 소수점이 버려짐

#### 4. 평균 이상인 값의 개수 세기
```c
for (i = 0; i < 8; i++) {
    if (arr[i] >= average) {  // 평균 이상이면
        count++;              // 카운터 증가
    }
}
```

---

### 📊 실행 흐름 예시

```
입력: 72 88 65 90 78 85 70 95

합계 계산:
sum = 72 + 88 + 65 + 90 + 78 + 85 + 70 + 95 = 643

평균 계산:
average = (float)643 / 8 = 80.375

평균 이상인 값 확인:
72 >= 80.375 ✗
88 >= 80.375 ✓ → count = 1
65 >= 80.375 ✗
90 >= 80.375 ✓ → count = 2
78 >= 80.375 ✗
85 >= 80.375 ✓ → count = 3
70 >= 80.375 ✗
95 >= 80.375 ✓ → count = 4

결과: 평균 80.38, 평균 이상인 값의 개수 3
```

---

## 문제 3: 문자열에서 특정 문자 개수 세기 정답

### 📁 problem3.c

```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[101];
    char ch;
    int i;
    int count = 0;
    int len;
    
    // 문자열 입력받기
    fgets(str, 101, stdin);
    
    // fgets는 \n도 포함하므로 제거
    len = strlen(str);
    if (len > 0 && str[len - 1] == '\n') {
        str[len - 1] = '\0';
        len--;
    }
    
    // 찾을 문자 입력받기
    scanf(" %c", &ch);  // 공백 주의!
    
    // 문자열에서 특정 문자 개수 세기
    for (i = 0; i < len; i++) {
        if (str[i] == ch) {
            count++;
        }
    }
    
    // 결과 출력
    printf("%c가 나타나는 횟수: %d\n", ch, count);
    
    return 0;
}
```

---

### 🔍 코드 설명

#### 1. 변수 선언
```c
char str[101];   // 문자열을 저장할 배열 (최대 100자 + \0)
char ch;         // 찾을 문자
int i;           // 반복문에 사용할 변수
int count = 0;    // 문자 개수 카운터
int len;         // 문자열 길이
```

#### 2. 문자열 입력
```c
fgets(str, 101, stdin);
```
- `fgets()`: 공백을 포함한 문자열 입력 가능
- `scanf("%s")`는 공백 전까지만 입력됨
- `fgets()`는 `\n`(엔터)도 포함하므로 제거 필요

#### 3. 개행 문자 제거
```c
len = strlen(str);
if (len > 0 && str[len - 1] == '\n') {
    str[len - 1] = '\0';
    len--;
}
```
- `fgets()`로 입력받으면 마지막에 `\n`이 포함됨
- `\n`을 `\0`으로 교체하여 제거

#### 4. 문자 입력
```c
scanf(" %c", &ch);  // 공백 주의!
```
- 공백을 넣어야 이전 입력의 `\n`을 무시함
- `scanf(" %c")`에서 앞의 공백이 중요

#### 5. 문자 개수 세기
```c
for (i = 0; i < len; i++) {
    if (str[i] == ch) {
        count++;
    }
}
```
- 문자열의 모든 문자를 순회
- 찾는 문자와 같으면 카운터 증가

---

### 📊 실행 흐름 예시

```
입력:
C Programming
g

문자열 처리:
str = "C Programming\0"
len = 13

문자 개수 세기:
i=0: str[0]='C' == 'g'? 아니오
i=1: str[1]=' ' == 'g'? 아니오
i=2: str[2]='P' == 'g'? 아니오
i=3: str[3]='r' == 'g'? 아니오
i=4: str[4]='o' == 'g'? 아니오
i=5: str[5]='g' == 'g'? 예 → count = 1
i=6: str[6]='r' == 'g'? 아니오
i=7: str[7]='a' == 'g'? 아니오
i=8: str[8]='m' == 'g'? 아니오
i=9: str[9]='m' == 'g'? 아니오
i=10: str[10]='i' == 'g'? 아니오
i=11: str[11]='n' == 'g'? 아니오
i=12: str[12]='g' == 'g'? 예 → count = 2

결과: g가 나타나는 횟수: 2
```

---

## 문제 4: 배열에서 짝수와 홀수 분리하기 정답

### 📁 problem4.c

```c
#include <stdio.h>

int main() {
    int arr[6];
    int i;
    int even[6], odd[6];  // 짝수와 홀수를 저장할 배열
    int evenCount = 0, oddCount = 0;  // 짝수와 홀수의 개수
    
    // 6개의 정수 입력받기
    for (i = 0; i < 6; i++) {
        scanf("%d", &arr[i]);
    }
    
    // 짝수와 홀수 분리
    for (i = 0; i < 6; i++) {
        if (arr[i] % 2 == 0) {           // 짝수인 경우
            even[evenCount] = arr[i];
            evenCount++;
        } else {                         // 홀수인 경우
            odd[oddCount] = arr[i];
            oddCount++;
        }
    }
    
    // 짝수 출력
    printf("짝수: ");
    if (evenCount == 0) {
        printf("없음\n");
    } else {
        for (i = 0; i < evenCount; i++) {
            printf("%d", even[i]);
            if (i < evenCount - 1) {     // 마지막이 아니면 공백 출력
                printf(" ");
            }
        }
        printf("\n");
    }
    
    // 홀수 출력
    printf("홀수: ");
    if (oddCount == 0) {
        printf("없음\n");
    } else {
        for (i = 0; i < oddCount; i++) {
            printf("%d", odd[i]);
            if (i < oddCount - 1) {      // 마지막이 아니면 공백 출력
                printf(" ");
            }
        }
        printf("\n");
    }
    
    return 0;
}
```

---

### 🔍 코드 설명

#### 1. 변수 선언
```c
int arr[6];              // 입력받을 6개의 정수 배열
int even[6], odd[6];     // 짝수와 홀수를 저장할 배열
int evenCount = 0;       // 짝수의 개수
int oddCount = 0;        // 홀수의 개수
```

#### 2. 짝수와 홀수 분리
```c
for (i = 0; i < 6; i++) {
    if (arr[i] % 2 == 0) {        // 나머지가 0이면 짝수
        even[evenCount] = arr[i];
        evenCount++;
    } else {                     // 나머지가 1이면 홀수
        odd[oddCount] = arr[i];
        oddCount++;
    }
}
```

**나머지 연산자(`%`) 사용:**
- `arr[i] % 2 == 0`: 짝수 (2로 나눈 나머지가 0)
- `arr[i] % 2 == 1` 또는 `arr[i] % 2 != 0`: 홀수 (2로 나눈 나머지가 1)

#### 3. 출력 형식 처리
```c
if (evenCount == 0) {
    printf("없음\n");
} else {
    for (i = 0; i < evenCount; i++) {
        printf("%d", even[i]);
        if (i < evenCount - 1) {  // 마지막이 아니면 공백
            printf(" ");
        }
    }
    printf("\n");
}
```

**출력 형식 주의사항:**
- 마지막 숫자 뒤에는 공백이 없어야 함
- `i < evenCount - 1` 조건으로 마지막 요소가 아닐 때만 공백 출력

---

### 📊 실행 흐름 예시

#### 예시 1
```
입력: 2 5 8 11 14 17

분리 과정:
arr[0]=2: 2 % 2 = 0 → 짝수 → even[0]=2, evenCount=1
arr[1]=5: 5 % 2 = 1 → 홀수 → odd[0]=5, oddCount=1
arr[2]=8: 8 % 2 = 0 → 짝수 → even[1]=8, evenCount=2
arr[3]=11: 11 % 2 = 1 → 홀수 → odd[1]=11, oddCount=2
arr[4]=14: 14 % 2 = 0 → 짝수 → even[2]=14, evenCount=3
arr[5]=17: 17 % 2 = 1 → 홀수 → odd[2]=17, oddCount=3

결과:
짝수: 2 8 14
홀수: 5 11 17
```

#### 예시 2
```
입력: 3 7 9 13 15 19

분리 과정:
모든 값이 홀수 → evenCount=0, oddCount=6

결과:
짝수: 없음
홀수: 3 7 9 13 15 19
```

---

## 전체 실행 파일 (통합 테스트용)

### problem1.c
```c
#include <stdio.h>

int main() {
    int arr[6];
    int i;
    int max, min;
    
    for (i = 0; i < 6; i++) {
        scanf("%d", &arr[i]);
    }
    
    max = arr[0];
    min = arr[0];
    
    for (i = 1; i < 6; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
        if (arr[i] < min) {
            min = arr[i];
        }
    }
    
    printf("최댓값: %d\n", max);
    printf("최솟값: %d\n", min);
    
    return 0;
}
```

### problem2.c
```c
#include <stdio.h>

int main() {
    int arr[8];
    int i;
    int sum = 0;
    float average;
    int count = 0;
    
    for (i = 0; i < 8; i++) {
        scanf("%d", &arr[i]);
    }
    
    for (i = 0; i < 8; i++) {
        sum += arr[i];
    }
    
    average = (float)sum / 8;
    
    for (i = 0; i < 8; i++) {
        if (arr[i] >= average) {
            count++;
        }
    }
    
    printf("평균: %.2f\n", average);
    printf("평균 이상인 값의 개수: %d\n", count);
    
    return 0;
}
```

### problem3.c
```c
#include <stdio.h>
#include <string.h>

int main() {
    char str[101];
    char ch;
    int i;
    int count = 0;
    int len;
    
    fgets(str, 101, stdin);
    
    len = strlen(str);
    if (len > 0 && str[len - 1] == '\n') {
        str[len - 1] = '\0';
        len--;
    }
    
    scanf(" %c", &ch);
    
    for (i = 0; i < len; i++) {
        if (str[i] == ch) {
            count++;
        }
    }
    
    printf("%c가 나타나는 횟수: %d\n", ch, count);
    
    return 0;
}
```

### problem4.c
```c
#include <stdio.h>

int main() {
    int arr[6];
    int i;
    int even[6], odd[6];
    int evenCount = 0, oddCount = 0;
    
    for (i = 0; i < 6; i++) {
        scanf("%d", &arr[i]);
    }
    
    for (i = 0; i < 6; i++) {
        if (arr[i] % 2 == 0) {
            even[evenCount] = arr[i];
            evenCount++;
        } else {
            odd[oddCount] = arr[i];
            oddCount++;
        }
    }
    
    printf("짝수: ");
    if (evenCount == 0) {
        printf("없음\n");
    } else {
        for (i = 0; i < evenCount; i++) {
            printf("%d", even[i]);
            if (i < evenCount - 1) {
                printf(" ");
            }
        }
        printf("\n");
    }
    
    printf("홀수: ");
    if (oddCount == 0) {
        printf("없음\n");
    } else {
        for (i = 0; i < oddCount; i++) {
            printf("%d", odd[i]);
            if (i < oddCount - 1) {
                printf(" ");
            }
        }
        printf("\n");
    }
    
    return 0;
}
```

---

## 채점 기준 상세

### 문제 1 (5점)
- 컴파일 오류 없음: 1점
- 배열 선언 및 입력 처리: 1점
- 최댓값 찾기 로직: 1점
- 최솟값 찾기 로직: 1점
- 출력 형식 정확성: 1점

### 문제 2 (5점)
- 컴파일 오류 없음: 1점
- 배열 선언 및 입력 처리: 1점
- 평균 계산 로직 (형변환 포함): 1점
- 평균 이상인 값 개수 계산 로직: 1점
- 출력 형식 정확성 (소수점 둘째 자리): 1점

### 문제 3 (5점)
- 컴파일 오류 없음: 1점
- 문자열 및 문자 입력 처리 (fgets, scanf): 1점
- 문자열 길이 구하기 (strlen): 1점
- 문자 개수 세기 로직: 1점
- 출력 형식 정확성: 1점

### 문제 4 (5점)
- 컴파일 오류 없음: 1점
- 배열 선언 및 입력 처리: 1점
- 짝수 분리 로직 (% 연산자 사용): 1점
- 홀수 분리 로직: 1점
- 출력 형식 정확성 (공백 처리, "없음" 처리): 1점

---

## 추가 설명

### 문제 1 - 최댓값/최솟값 찾기
- 첫 번째 값을 초기값으로 설정하는 것이 중요
- 비교 연산자 사용: `>` (크다), `<` (작다)

### 문제 2 - 형변환
- 정수 나눗셈: `sum / 8` → 정수 결과 (소수점 버림)
- 실수 나눗셈: `(float)sum / 8` → 실수 결과
- `%.2f`: 소수점 둘째 자리까지 출력

### 문제 3 - 문자열 입력
- `scanf("%s")`: 공백 전까지만 입력 (공백 포함 문자열 불가)
- `fgets()`: 공백 포함 문자열 입력 가능
- `fgets()`는 `\n`도 포함하므로 제거 필요
- `scanf(" %c")`: 앞의 공백으로 이전 입력의 `\n` 무시

### 문제 4 - 출력 형식
- 마지막 숫자 뒤 공백 제거: `if (i < count - 1) printf(" ");`
- "없음" 처리: `if (count == 0) printf("없음\n");`
- 나머지 연산자: `%` (예: `5 % 2 = 1`, `6 % 2 = 0`)

---

## 핵심 포인트

### 1. 배열 사용법
- 배열 선언: `int arr[6];`
- 배열 접근: `arr[i]` (인덱스는 0부터 시작)
- 배열 입력: `scanf("%d", &arr[i]);`

### 2. 반복문 활용
- `for` 문: 반복 횟수가 정해진 경우
- 배열과 함께 사용하여 모든 요소 처리

### 3. 조건문 활용
- `if` 문: 조건에 따라 분기
- 비교 연산자: `>`, `<`, `>=`, `==`

### 4. 형변환
- `(float)변수`: 정수를 실수로 변환
- 실수 나눗셈을 위해 필요

### 5. 문자열 처리
- `fgets()`: 공백 포함 문자열 입력
- `strlen()`: 문자열 길이 구하기
- `charAt()` 대신 배열 인덱스로 접근: `str[i]`
