# C언어 교안 (11-12주차)

**목표:**
- C 언어 문법 이해
- 배열·함수·포인터 개념 습득
- 작업형 평가 문제 해결 가능
- 간단한 콘솔 프로그램 제작

---

## 📅 11주차 – 구조체

### 📌 학습 목표
- 구조체의 개념 이해
- 구조체 선언과 사용
- 구조체 배열 활용
- 구조체와 함수의 조합

### 📘 이론

#### 11.1 구조체란?
- **서로 다른 자료형을 묶어서 하나의 자료형으로 만듦**
- 관련된 데이터를 그룹화
- 객체 지향의 클래스와 유사

#### 11.2 구조체 선언

```c
struct Student {
    char name[20];
    int age;
    int score;
};
```

#### 11.3 구조체 변수 선언

```c
// 방법 1: 선언 후 변수 생성
struct Student s1;

// 방법 2: 선언과 동시에 변수 생성
struct Student {
    char name[20];
    int age;
} s1, s2;

// 방법 3: typedef 사용
typedef struct {
    char name[20];
    int age;
} Student;

Student s1;  // struct 없이 사용 가능
```

#### 11.4 구조체 멤버 접근

```c
struct Student s1;
strcpy(s1.name, "홍길동");
s1.age = 20;
s1.score = 85;
```

### 🧪 실습

#### 실습 1: 구조체 기본

```c
#include <stdio.h>
#include <string.h>

struct Student {
    char name[20];
    int age;
    int score;
};

int main() {
    struct Student s1;
    
    strcpy(s1.name, "홍길동");
    s1.age = 20;
    s1.score = 85;
    
    printf("이름: %s\n", s1.name);
    printf("나이: %d\n", s1.age);
    printf("점수: %d\n", s1.score);
    
    return 0;
}
```

#### 실습 2: 구조체 초기화

```c
#include <stdio.h>

struct Student {
    char name[20];
    int age;
    int score;
};

int main() {
    struct Student s1 = {"홍길동", 20, 85};
    
    printf("이름: %s\n", s1.name);
    printf("나이: %d\n", s1.age);
    printf("점수: %d\n", s1.score);
    
    return 0;
}
```

#### 실습 3: 구조체 배열

```c
#include <stdio.h>
#include <string.h>

struct Student {
    char name[20];
    int age;
    int score;
};

int main() {
    struct Student students[3];
    int i;
    
    // 입력
    for (i = 0; i < 3; i++) {
        printf("%d번 학생 정보 입력:\n", i + 1);
        printf("이름: ");
        scanf("%s", students[i].name);
        printf("나이: ");
        scanf("%d", &students[i].age);
        printf("점수: ");
        scanf("%d", &students[i].score);
    }
    
    // 출력
    for (i = 0; i < 3; i++) {
        printf("%s: 나이 %d, 점수 %d\n", 
               students[i].name, students[i].age, students[i].score);
    }
    
    return 0;
}
```

#### 실습 4: 구조체를 함수로 전달

```c
#include <stdio.h>
#include <string.h>

struct Student {
    char name[20];
    int age;
    int score;
};

void printStudent(struct Student s) {
    printf("이름: %s\n", s.name);
    printf("나이: %d\n", s.age);
    printf("점수: %d\n", s.score);
}

int main() {
    struct Student s1 = {"홍길동", 20, 85};
    printStudent(s1);
    return 0;
}
```

#### 실습 5: 구조체 포인터

```c
#include <stdio.h>
#include <string.h>

struct Student {
    char name[20];
    int age;
    int score;
};

int main() {
    struct Student s1 = {"홍길동", 20, 85};
    struct Student *p = &s1;
    
    printf("이름: %s\n", p->name);    // 화살표 연산자
    printf("나이: %d\n", p->age);
    printf("점수: %d\n", p->score);
    
    // 또는
    printf("이름: %s\n", (*p).name);   // 간접 참조
    printf("나이: %d\n", (*p).age);
    printf("점수: %d\n", (*p).score);
    
    return 0;
}
```

**포인터 접근 방법:**
- `p->name`: 화살표 연산자 (권장)
- `(*p).name`: 간접 참조

#### 실습 6: 구조체 배열에서 검색

```c
#include <stdio.h>
#include <string.h>

struct Student {
    char name[20];
    int age;
    int score;
};

int main() {
    struct Student students[3] = {
        {"홍길동", 20, 85},
        {"김철수", 22, 95},
        {"이영희", 21, 75}
    };
    
    char search[20];
    int i, found = 0;
    
    printf("검색할 이름: ");
    scanf("%s", search);
    
    for (i = 0; i < 3; i++) {
        if (strcmp(students[i].name, search) == 0) {
            printf("찾았습니다!\n");
            printf("나이: %d, 점수: %d\n", 
                   students[i].age, students[i].score);
            found = 1;
            break;
        }
    }
    
    if (!found) {
        printf("찾을 수 없습니다.\n");
    }
    
    return 0;
}
```

### 🎯 핵심 정리
- 구조체는 여러 자료형을 묶은 사용자 정의 타입
- `.` 연산자로 멤버 접근
- `->` 연산자로 포인터 멤버 접근
- 구조체 배열로 여러 데이터 관리

### 📝 연습 문제
1. 학생 정보를 입력받아 출력하는 프로그램
2. 구조체 배열에서 최고 점수 학생 찾기
3. 구조체를 이용한 간단한 학생 관리 시스템

---

## 📅 12주차 – 파일 입출력 & 종합 실습

### 📌 학습 목표
- 파일 열기/닫기 방법
- 파일 읽기/쓰기 함수 사용
- 종합 실습 문제 해결

### 📘 이론

#### 12.1 파일 포인터

```c
FILE *fp;  // 파일 포인터 선언
```

#### 12.2 파일 열기/닫기

```c
FILE *fp = fopen("파일명", "모드");
// 작업 수행
fclose(fp);
```

**파일 모드:**
- `"r"`: 읽기 (Read)
- `"w"`: 쓰기 (Write, 기존 파일 덮어쓰기)
- `"a"`: 추가 (Append, 파일 끝에 추가)
- `"r+"`: 읽기/쓰기

#### 12.3 파일 쓰기 함수

```c
fprintf(fp, "형식", 변수들);  // printf와 유사
fputs("문자열", fp);          // 문자열 쓰기
fputc('문자', fp);            // 문자 쓰기
```

#### 12.4 파일 읽기 함수

```c
fscanf(fp, "형식", 변수들);   // scanf와 유사
fgets(문자열, 크기, fp);      // 문자열 읽기
fgetc(fp);                    // 문자 읽기
```

### 🧪 실습

#### 실습 1: 파일에 쓰기

```c
#include <stdio.h>

int main() {
    FILE *fp = fopen("data.txt", "w");
    
    if (fp == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
    
    fprintf(fp, "Hello File\n");
    fprintf(fp, "안녕하세요 파일입니다.\n");
    
    fclose(fp);
    printf("파일 쓰기 완료!\n");
    
    return 0;
}
```

#### 실습 2: 파일에서 읽기

```c
#include <stdio.h>

int main() {
    FILE *fp = fopen("data.txt", "r");
    char buffer[100];
    
    if (fp == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
    
    while (fgets(buffer, 100, fp) != NULL) {
        printf("%s", buffer);
    }
    
    fclose(fp);
    
    return 0;
}
```

#### 실습 3: 숫자 파일에 저장

```c
#include <stdio.h>

int main() {
    FILE *fp = fopen("numbers.txt", "w");
    int i;
    
    if (fp == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
    
    for (i = 1; i <= 10; i++) {
        fprintf(fp, "%d\n", i);
    }
    
    fclose(fp);
    printf("숫자 파일 저장 완료!\n");
    
    return 0;
}
```

#### 실습 4: 파일에서 숫자 읽기

```c
#include <stdio.h>

int main() {
    FILE *fp = fopen("numbers.txt", "r");
    int num, sum = 0;
    
    if (fp == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
    
    while (fscanf(fp, "%d", &num) != EOF) {
        printf("%d ", num);
        sum += num;
    }
    
    printf("\n합계: %d\n", sum);
    fclose(fp);
    
    return 0;
}
```

#### 실습 5: 파일에 구조체 저장

```c
#include <stdio.h>
#include <string.h>

struct Student {
    char name[20];
    int age;
    int score;
};

int main() {
    FILE *fp = fopen("students.txt", "w");
    struct Student students[3] = {
        {"홍길동", 20, 85},
        {"김철수", 22, 95},
        {"이영희", 21, 75}
    };
    int i;
    
    if (fp == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
    
    for (i = 0; i < 3; i++) {
        fprintf(fp, "%s %d %d\n", 
                students[i].name, students[i].age, students[i].score);
    }
    
    fclose(fp);
    printf("학생 정보 저장 완료!\n");
    
    return 0;
}
```

### 🎯 종합 실습 문제

#### 문제 1: 학생 관리 시스템
- 학생 정보 입력 (이름, 나이, 점수)
- 전체 학생 목록 출력
- 평균 점수 계산
- 파일에 저장/불러오기

#### 문제 2: 간단한 계산기
- 사칙연산 수행
- 계산 이력 파일에 저장
- 이력 불러오기

#### 문제 3: 성적 관리 프로그램
- 여러 학생의 성적 입력
- 평균, 최고점, 최저점 계산
- 등급 부여 (A, B, C, D, F)
- 결과를 파일로 저장

### 🎯 핵심 정리
- 파일 작업 후 반드시 `fclose()` 호출
- 파일 열기 실패 시 NULL 체크 필수
- `fprintf`, `fscanf`는 `printf`, `scanf`와 유사
- 파일 모드에 따라 읽기/쓰기 가능 여부 결정

### 📝 연습 문제
1. 10개의 정수를 파일에 저장하고 읽어서 평균 계산
2. 학생 정보를 파일에 저장하고 검색 기능 구현
3. 간단한 일기장 프로그램 (파일 저장/불러오기)

---

## 📚 작업형 평가 대비

### 자주 나오는 문제 유형

1. **배열 문제**
   - 최댓값/최솟값 찾기
   - 평균 계산
   - 배열 정렬
   - 배열 검색

2. **문자열 문제**
   - 문자열 길이 계산
   - 문자열 비교
   - 문자열 검색
   - 문자열 변환

3. **함수 문제**
   - 배열을 매개변수로 받는 함수
   - 재귀 함수
   - 포인터를 이용한 함수

4. **구조체 문제**
   - 구조체 배열 관리
   - 구조체 검색
   - 구조체 정렬

### 문제 해결 전략

1. **문제 이해**: 요구사항 정확히 파악
2. **알고리즘 설계**: 단계별로 접근
3. **코드 작성**: 주석을 활용하여 명확하게
4. **테스트**: 다양한 입력값으로 검증

---

## 🎓 학습 체크리스트

- [ ] C 언어 기본 문법 이해
- [ ] 변수와 자료형 사용 가능
- [ ] 입력/출력 함수 사용 가능
- [ ] 조건문과 반복문 작성 가능
- [ ] 배열 사용 가능
- [ ] 문자열 처리 가능
- [ ] 함수 작성 및 사용 가능
- [ ] 포인터 개념 이해
- [ ] 구조체 사용 가능
- [ ] 파일 입출력 가능

---

**12주간의 C 언어 학습을 완료하셨습니다! 🎉**
