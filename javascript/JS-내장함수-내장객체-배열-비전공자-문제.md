# 자바스크립트 내장 함수 · 내장 객체 · 배열 종합 문제 (비전공자)

[내장 함수 · 내장 객체 정리](./자바스크립트-내장함수-내장객체-비전공자-정리.md)와  
[배열 정리](./자바스크립트-배열-비전공자-정리.md)를 바탕으로 한 문제입니다.

정답: [JS-내장함수-내장객체-배열-비전공자-정답.md](./JS-내장함수-내장객체-배열-비전공자-정답.md)

---

# Part 1. 내장 함수 · 내장 객체

## 1번

사용자에게 **입력**을 받는 함수는?

① `alert()`  
② `prompt()`  
③ `confirm()`  
④ `console.log()`

---

## 2번

`confirm("삭제하시겠습니까?")` 에서 사용자가 **취소**를 누르면 반환값은?

① `"취소"`  
② `null`  
③ `false`  
④ `0`

---

## 3번

다음 코드의 출력은?

```javascript
console.log(typeof Number("100"));
console.log(Number("100px"));
```

① `number` / `100`  
② `number` / `NaN`  
③ `string` / `100`  
④ `number` / `100px`

---

## 4번

`parseInt("100원")` 의 결과는?

① `"100원"`  
② `100`  
③ `NaN`  
④ `0`

---

## 5번

다음 중 **실수**로 변환하는 함수는?

① `parseInt()`  
② `parseFloat()`  
③ `Number.isInteger()`  
④ `Math.floor()`

---

## 6번

다음 코드의 출력은?

```javascript
console.log(Boolean(0));
console.log(Boolean(""));
console.log(Boolean("Java"));
```

① `true` / `true` / `false`  
② `false` / `false` / `true`  
③ `false` / `true` / `false`  
④ `true` / `false` / `true`

---

## 7번

`isNaN("Java")` 의 결과와 의미로 올바른 것은?

① `false` — 숫자이다  
② `true` — 숫자가 아니다  
③ `true` — 숫자이다  
④ `false` — 숫자가 아니다

---

## 8번

한글 이름을 URL 쿼리 값으로 안전하게 넣을 때 권장하는 함수는?

① `escape()`  
② `encodeURI()`  
③ `encodeURIComponent()`  
④ `eval()`

---

## 9번

`setTimeout(fn, 3000)` 에서 `3000`의 단위는?

① 초  
② 밀리초 (1/1000초)  
③ 분  
④ 프레임

---

## 10번

`setInterval`로 시작한 반복을 **멈출** 때 사용하는 함수는?

① `clearTimeout()`  
② `clearInterval()`  
③ `stopInterval()`  
④ `cancel()`

---

## 11번 (빈칸)

문자열 `"10+20"` 을 JavaScript 코드로 실행하는 함수는 `________` 이다.  
실무에서는 보안 문제로 **거의 사용하지 않는다**.

---

## 12번

다음 코드의 출력은?

```javascript
console.log(Math.floor(3.8));
console.log(Math.ceil(3.2));
console.log(Math.round(3.5));
```

① `3` / `3` / `3`  
② `3` / `4` / `4`  
③ `4` / `3` / `4`  
④ `3` / `4` / `3`

---

## 13번

`Math.max(10, 20, 30)` 의 결과는?

① `10`  
② `20`  
③ `30`  
④ `[10, 20, 30]`

---

## 14번

`Math.random()` 이 반환하는 값의 범위로 올바른 것은?

① 0 초과 1 미만  
② 0 이상 1 미만  
③ 1 이상 10 이하  
④ 0 이상 1 이하

---

## 15번

`new Date()` 로 만든 객체에서 **8월**을 얻으려면?

① `getMonth()` → `8`  
② `getMonth() + 1` → `8`  
③ `getDate()` → `8`  
④ `getDay()` → `8`

---

## 16번

다음 코드의 출력은?

```javascript
let str = "JavaScript";
console.log(str.indexOf("S"));
console.log(str.substring(0, 4));
```

① `4` / `"Java"`  
② `5` / `"JavaS"`  
③ `4` / `"Script"`  
④ `5` / `"Java"`

---

## 17번

문자열 메서드(`toUpperCase`, `replace` 등)에 대한 설명으로 **옳은** 것은?

① 원본 문자열을 직접 수정한다  
② 새 문자열을 반환한다  
③ 배열만 반환한다  
④ 숫자만 반환한다

---

## 18번

`String(100)` 과 `typeof String(100)` 의 결과는?

① `100` / `"number"`  
② `"100"` / `"string"`  
③ `100` / `"string"`  
④ `"100"` / `"number"`

---

## 19번 (빈칸)

사용자에게 **확인/취소** 창을 띄우는 함수는 `________` 이다.  
확인을 누르면 `________` 가 반환된다.

---

## 20번

다음 중 **브라우저 전용** 내장 함수로, Node.js에는 없는 것은?

① `parseInt()`  
② `Number()`  
③ `alert()`  
④ `isNaN()`

---

# Part 2. 배열 (Array)

## 21번

배열 `["사과", "바나나", "오렌지"]` 에서 `fruits[1]` 의 값은?

① `"사과"`  
② `"바나나"`  
③ `"오렌지"`  
④ `undefined`

---

## 22번

배열의 **첫 번째** 요소 인덱스는?

① `-1`  
② `0`  
③ `1`  
④ `length`

---

## 23번

다음 코드 실행 후 `fruits`의 결과는?

```javascript
let fruits = ["사과", "바나나", "오렌지"];
fruits[1] = "포도";
```

① `["사과", "바나나", "오렌지"]`  
② `["사과", "포도", "오렌지"]`  
③ `["포도", "바나나", "오렌지"]`  
④ `["사과", "포도"]`

---

## 24번

다음 코드의 출력은?

```javascript
let fruits = ["사과", "바나나"];
fruits.push("딸기");
console.log(fruits.length);
```

① `1`  
② `2`  
③ `3`  
④ `4`

---

## 25번

`pop()` 메서드에 대한 설명으로 올바른 것은?

① 배열 **앞**에서 요소를 제거한다  
② 배열 **끝**에서 요소를 제거하고, 제거한 값을 반환한다  
③ 배열 **끝**에 요소를 추가한다  
④ 배열을 정렬한다

---

## 26번

다음 코드의 출력은?

```javascript
let fruits = ["포도", "사과", "바나나"];
let item = fruits.shift();
console.log(item);
console.log(fruits);
```

① `포도` / `["사과", "바나나"]`  
② `바나나` / `["포도", "사과"]`  
③ `포도` / `["포도", "사과", "바나나"]`  
④ `사과` / `["포도", "바나나"]`

---

## 27번

`fruits.indexOf("포도")` 가 `-1` 이면 의미는?

① `"포도"`가 1번 인덱스에 있다  
② `"포도"`가 배열에 **없다**  
③ 배열이 비어 있다  
④ `"포도"`가 마지막에 있다

---

## 28번

다음 코드의 출력은?

```javascript
let fruits = ["사과", "바나나", "오렌지"];
console.log(fruits.includes("사과"));
console.log(fruits.includes("포도"));
```

① `true` / `true`  
② `false` / `false`  
③ `true` / `false`  
④ `false` / `true`

---

## 29번

다음 코드 실행 후 `fruits`의 결과는?

```javascript
let fruits = ["사과", "바나나", "오렌지"];
fruits.splice(1, 1);
```

① `["사과", "오렌지"]`  
② `["바나나", "오렌지"]`  
③ `["사과", "바나나"]`  
④ `["사과", "바나나", "오렌지"]`

---

## 30번

`slice(1, 3)` 의 의미로 올바른 것은?

① 1번 인덱스부터 3번 인덱스 **포함**까지 복사  
② 1번 인덱스부터 3번 인덱스 **직전**까지 복사  
③ 1개부터 3개까지 삭제  
④ 원본 배열을 1~3번만 남기고 수정

---

## 31번

다음 코드의 출력은?

```javascript
let nums = [10, 5, 30, 2];
nums.sort();
console.log(nums);
```

① `[2, 5, 10, 30]`  
② `[10, 2, 30, 5]`  
③ `[30, 10, 5, 2]`  
④ `[10, 5, 30, 2]`

---

## 32번

숫자 배열을 **오름차순**으로 정렬하는 올바른 코드는?

① `nums.sort()`  
② `nums.sort((a, b) => a - b)`  
③ `nums.sort((a, b) => b - a)`  
④ `nums.reverse()`

---

## 33번

다음 코드의 출력은?

```javascript
let fruits = ["사과", "바나나", "오렌지"];
console.log(fruits.join("-"));
```

① `["사과-바나나-오렌지"]`  
② `"사과,바나나,오렌지"`  
③ `"사과-바나나-오렌지"`  
④ `"사과 바나나 오렌지"`

---

## 34번

다음 코드의 출력은?

```javascript
let nums = [10, 20, 30, 40, 50];
let result = nums.filter(n => n >= 30);
console.log(result);
```

① `[10, 20]`  
② `[30, 40, 50]`  
③ `[30]`  
④ `30`

---

## 35번

다음 코드의 출력은?

```javascript
let nums = [1, 2, 3];
let result = nums.map(n => n * 10);
console.log(result);
```

① `[1, 2, 3]`  
② `[10, 20, 30]`  
③ `6`  
④ `[11, 12, 13]`

---

## 36번

`find()` 메서드에 대한 설명으로 올바른 것은?

① 조건에 맞는 **모든** 요소를 배열로 반환한다  
② 조건에 맞는 **첫 번째** 요소 하나를 반환한다  
③ 배열의 길이를 반환한다  
④ 원본 배열을 정렬한다

---

## 37번

다음 코드의 출력은?

```javascript
let scores = [80, 90, 100];
let sum = scores.reduce((total, n) => total + n, 0);
console.log(sum);
```

① `90`  
② `270`  
③ `3`  
④ `[80, 90, 100]`

---

## 38번

다음 코드 실행 후 `students` 배열의 순서는? (점수 기준)

```javascript
let students = [
    { name: "홍길동", score: 90 },
    { name: "김철수", score: 80 },
    { name: "이영희", score: 95 }
];
students.sort((a, b) => a.score - b.score);
```

① 이영희(95) → 홍길동(90) → 김철수(80)  
② 김철수(80) → 홍길동(90) → 이영희(95)  
③ 홍길동 → 김철수 → 이영희 (이름순)  
④ 정렬되지 않는다

---

## 39번

원본 배열을 **바꾸지 않는** 메서드는?

① `push()`  
② `splice()`  
③ `slice()`  
④ `sort()`

---

## 40번 (빈칸)

배열 끝에 요소를 추가하는 메서드는 `________` 이고,  
끝에서 요소를 제거하는 메서드는 `________` 이다.

---

# Part 3. 코드 작성 문제 (41~45)

## 41번

`prompt()`로 이름을 입력받아 `"안녕하세요, ○○님!"` 을 `alert()`로 출력하는 코드를 작성하세요.

---

## 42번

문자열 `"3.14"` 를 실수로 변환한 뒤, `Math.round()`로 반올림하여 출력하세요.

---

## 43번

배열 `["사과", "바나나", "오렌지"]`에서 `"바나나"`를 삭제하고, `"딸기"`를 마지막에 추가한 뒤  
`join(", ")`으로 `"사과, 오렌지, 딸기"` 형태 문자열을 만드세요.  
(`splice`, `push` 사용)

---

## 44번

숫자 배열 `[15, 8, 42, 3, 27]`에서 **10보다 큰 수만** 골라 **2배**한 새 배열을 만드세요.  
(`filter`, `map` 사용)

---

## 45번

학생 객체 배열에서 **90점 이상**인 학생의 **이름만** 배열로 만드세요.

```javascript
let students = [
    { name: "홍길동", score: 90 },
    { name: "김철수", score: 80 },
    { name: "이영희", score: 95 },
    { name: "박민수", score: 70 }
];
// 결과 예: ["홍길동", "이영희"]
```

---

## 참고 교안

- [자바스크립트-내장함수-내장객체-비전공자-정리.md](./자바스크립트-내장함수-내장객체-비전공자-정리.md)
- [자바스크립트-배열-비전공자-정리.md](./자바스크립트-배열-비전공자-정리.md)
