# CSS로 보노보노 캐릭터 만들기 🐰

## 📌 개요

HTML과 CSS만으로 보노보노 캐릭터를 그리는 예제입니다. `position` 속성과 `transform` 속성을 활용하여 각 부위를 배치하고 회전시켜 만듭니다.

---

## 📁 파일 구조

```
프로젝트/
├── index.html
└── css/
    └── bono.css
```

---

## 📄 전체 코드

### HTML (index.html)

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>보노보노 캐릭터</title>
    <link rel="stylesheet" href="css/bono.css">
</head>
<body>
    <div class="bigbox">
        <div class="face">
            <div class="eyeball left"></div>
            <div class="eyeball right"></div>
            <div class="nose"></div>
            <div class="mouth left"></div>
            <div class="box b_left">
                <div class="aline aline1"></div>
                <div class="aline aline2"></div>
                <div class="aline aline3"></div>
            </div>
            <div class="mouth right"></div>
            <div class="box b_right">
                <div class="bline bline1"></div>
                <div class="bline bline2"></div>
                <div class="bline bline3"></div>
            </div>
        </div>
    </div>
</body>
</html>
```

### CSS (css/bono.css)

```css
* {
    margin: 0;
    padding: 0;
}

/* 부모 컨테이너 */
.bigbox {
    width: 500px;
    height: 500px;
    border: 1px solid #333;
    margin: 100px auto 0;
    position: relative;
}

/* 얼굴 */
.face {
    width: 500px;
    height: 500px;
    border: 10px solid #333;
    border-radius: 50%;
    background-color: cornflowerblue;
    box-sizing: border-box;
}

/* 눈 */
.eyeball {
    width: 30px;
    height: 30px;
    border: 1px solid #333;
    border-radius: 50%;
    position: absolute;
    top: 190px;
    background-color: black;
}

.eyeball.left {
    left: 100px;
}

.eyeball.right {
    right: 100px;
}

/* 코 */
.nose {
    width: 40px;
    height: 40px;
    border: 1px solid #333;
    background-color: black;
    border-radius: 50%;
    position: absolute;
    top: 300px;
    left: 50%;
    margin-left: -20px;
    z-index: 1;
}

/* 입 */
.mouth {
    width: 110px;
    height: 90px;
    border: 5px solid #333;
    background-color: #fff;
    box-sizing: border-box;
    border-radius: 50%;
    position: absolute;
    top: 320px;
}

.mouth.left {
    transform: rotate(-45deg);
    left: 160px;
}

.mouth.right {
    transform: rotate(45deg);
    right: 160px;
}

/* 입 근처 박스 컨테이너 */
.box {
    position: absolute;
    width: 90px;
    height: 90px;
    top: 320px;
}

.box.b_left {
    left: 110px;
}

.box.b_right {
    right: 110px;
}

/* 왼쪽 선들 (aline) */
.aline {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
    top: 15px;
    right: 5px;
}

.aline1 {
    transform-origin: right center;
    transform: rotate(20deg);
}

.aline2 {
    top: 50px;
}

.aline3 {
    transform-origin: right center;
    top: 80px;
    transform: rotate(-20deg);
}

/* 오른쪽 선들 (bline) */
.bline {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
    top: 15px;
    right: 5px;
}

.bline1 {
    transform-origin: left center;
    transform: rotate(-20deg);
}

.bline2 {
    top: 50px;
}

.bline3 {
    transform-origin: left center;
    top: 80px;
    transform: rotate(20deg);
}
```

---

## 🏗️ 구조 설명

### 요소 계층 구조

```
.bigbox (부모 컨테이너)
└── .face (얼굴)
    ├── .eyeball.left (왼쪽 눈)
    ├── .eyeball.right (오른쪽 눈)
    ├── .nose (코)
    ├── .mouth.left (왼쪽 입)
    ├── .box.b_left (왼쪽 박스)
    │   ├── .aline.aline1
    │   ├── .aline.aline2
    │   └── .aline.aline3
    ├── .mouth.right (오른쪽 입)
    └── .box.b_right (오른쪽 박스)
        ├── .bline.bline1
        ├── .bline.bline2
        └── .bline.bline3
```

### 요소 배치 순서

| 요소 | 위치 | 크기 | 특징 |
|------|------|------|------|
| **얼굴** (.face) | 전체 | 500px × 500px | 원형, 파란색 배경 |
| **눈** (left, right) | 상단 좌우 | 30px × 30px | 검은 원형 |
| **코** (.nose) | 중앙 | 40px × 40px | 검은 원형, z-index: 1 |
| **입** (left, right) | 하단 좌우 | 110px × 90px | 흰색 원형, 45도 회전 |
| **박스** (b_left, b_right) | 입 근처 | 90px × 90px | 선들의 컨테이너 |
| **선들** (aline, bline) | 박스 내부 | 80px × 3px | 3개씩, 각도 회전 |

---

## 🎨 주요 CSS 기법

### 1. 중앙 정렬 (부모 컨테이너)

```css
.bigbox {
    margin: 100px auto 0;
}
```

**설명:**
- `margin: 100px auto 0`: 위쪽 100px, 좌우 자동(중앙 정렬)
- `position: relative`: 자식 요소의 기준점이 됨

**시각화:**

```
┌─────────────────────────┐
│   100px                 │
│   ┌───────────────┐     │
│   │               │     │ ← margin: 100px auto 0
│   │   .bigbox     │     │   (상단 100px, 좌우 중앙)
│   │               │     │
│   └───────────────┘     │
└─────────────────────────┘
```

### 2. 원형 만들기 (border-radius)

```css
.face {
    border-radius: 50%;  /* 완전한 원 */
}
```

**설명:**
- `width: 500px`, `height: 500px`: 정사각형
- `border-radius: 50%`: 완전한 원형

### 3. position: absolute 활용

```css
.eyeball {
    position: absolute;
    top: 190px;
}

.eyeball.left {
    left: 100px;
}

.eyeball.right {
    right: 100px;
}
```

**설명:**
- `.bigbox`가 `position: relative`이므로 자식 요소들은 `.bigbox`를 기준으로 배치
- `top`, `left`, `right`로 정확한 위치 지정

### 4. 정중앙 정렬 (코)

```css
.nose {
    left: 50%;
    margin-left: -20px;  /* 요소 너비(40px)의 절반 */
}
```

**설명:**
- `left: 50%`: 부모의 중앙 위치로 이동
- `margin-left: -20px`: 요소 너비의 절반만큼 왼쪽으로 이동하여 완전한 중앙 정렬

**시각화:**

```
┌─────────────────────────┐
│                         │
│          ← 50%          │
│          ┌───┐          │
│       -20px │           │
│          └───┘          │
│                         │
└─────────────────────────┘
```

### 5. transform: rotate (입 회전)

```css
.mouth.left {
    transform: rotate(-45deg);  /* -45도 회전 (반시계 방향) */
    left: 160px;
}

.mouth.right {
    transform: rotate(45deg);  /* 45도 회전 (시계 방향) */
    right: 160px;
}
```

**설명:**
- `transform: rotate(-45deg)`: 반시계 방향 45도 회전
- `transform: rotate(45deg)`: 시계 방향 45도 회전
- 입을 대각선으로 배치하여 보노보노 특유의 입 모양 구현

**시각화:**

```
왼쪽 입 (-45도):        오른쪽 입 (45도):
       ↖                      ↗
        ┌─┐                  ┌─┐
        └─┘                  └─┘
```

### 6. transform-origin (회전 중심점)

```css
.aline1 {
    transform-origin: right center;  /* 오른쪽 중앙을 기준으로 회전 */
    transform: rotate(20deg);
}

.bline1 {
    transform-origin: left center;  /* 왼쪽 중앙을 기준으로 회전 */
    transform: rotate(-20deg);
}
```

**설명:**
- `transform-origin`: 회전의 중심점 지정
- `right center`: 오른쪽 중앙
- `left center`: 왼쪽 중앙

**시각화:**

```
왼쪽 선들 (transform-origin: right center):
        │
    20deg│
        │────── (aline1)
        │
        │────── (aline2, 수평)
        │
    -20deg│
        │────── (aline3)

오른쪽 선들 (transform-origin: left center):
    │
    │-20deg
    │────── (bline1)
    │
    │────── (bline2, 수평)
    │
    │20deg
    │────── (bline3)
```

### 7. z-index로 레이어 순서 제어

```css
.nose {
    z-index: 1;  /* 입보다 위에 표시 */
}
```

**설명:**
- `z-index: 1`: 코가 입보다 위에 표시되도록 설정
- 기본값은 0 (코드 순서대로 쌓임)

### 8. box-sizing: border-box

```css
.face {
    box-sizing: border-box;  /* padding과 border를 크기에 포함 */
}

.mouth {
    box-sizing: border-box;
}
```

**설명:**
- `box-sizing: border-box`: `width`와 `height`에 `padding`과 `border`가 포함됨
- 실제 크기 = 지정한 `width`/`height` 그대로

---

## 🎯 각 요소별 상세 설명

### 1. .bigbox (부모 컨테이너)

```css
.bigbox {
    width: 500px;
    height: 500px;
    border: 1px solid #333;
    margin: 100px auto 0;
    position: relative;
}
```

**역할:**
- 모든 요소를 담는 컨테이너
- 자식 요소들의 기준점 (`position: relative`)

**레이아웃:**
- 화면 중앙 정렬 (`margin: auto`)
- 상단에서 100px 떨어진 위치

**색상:**
- 테두리: `#333` (검은색)

---

### 2. .face (얼굴)

```css
.face {
    width: 500px;
    height: 500px;
    border: 10px solid #333;
    border-radius: 50%;
    background-color: cornflowerblue;
    box-sizing: border-box;
}
```

**역할:**
- 보노보노의 얼굴 배경
- 완전한 원형

**색상:**
- 배경색: `cornflowerblue` (하늘색/파란색)
- 테두리: `#333` (검은색, 10px)

**크기:**
- 500px × 500px (정사각형)
- `border-radius: 50%` → 완전한 원

---

### 3. .eyeball (눈)

```css
.eyeball {
    width: 30px;
    height: 30px;
    border: 1px solid #333;
    border-radius: 50%;
    position: absolute;
    top: 190px;
    background-color: black;
}

.eyeball.left {
    left: 100px;
}

.eyeball.right {
    right: 100px;
}
```

**역할:**
- 보노보노의 눈 (왼쪽, 오른쪽)

**색상:**
- 배경색: `black` (검은색)
- 테두리: `#333` (검은색)

**위치:**
- 얼굴 상단에서 190px 아래
- 왼쪽: 얼굴 왼쪽에서 100px
- 오른쪽: 얼굴 오른쪽에서 100px

**크기:**
- 30px × 30px (정사각형)
- `border-radius: 50%` → 완전한 원

---

### 4. .nose (코)

```css
.nose {
    width: 40px;
    height: 40px;
    border: 1px solid #333;
    background-color: black;
    border-radius: 50%;
    position: absolute;
    top: 300px;
    left: 50%;
    margin-left: -20px;
    z-index: 1;
}
```

**역할:**
- 보노보노의 코

**색상:**
- 배경색: `black` (검은색)
- 테두리: `#333` (검은색)

**위치:**
- 얼굴 상단에서 300px 아래
- 수평 중앙 정렬 (`left: 50%` + `margin-left: -20px`)

**크기:**
- 40px × 40px (정사각형)
- `border-radius: 50%` → 완전한 원

**z-index:**
- `z-index: 1`: 입보다 위에 표시

---

### 5. .mouth (입)

```css
.mouth {
    width: 110px;
    height: 90px;
    border: 5px solid #333;
    background-color: #fff;
    box-sizing: border-box;
    border-radius: 50%;
    position: absolute;
    top: 320px;
}

.mouth.left {
    transform: rotate(-45deg);
    left: 160px;
}

.mouth.right {
    transform: rotate(45deg);
    right: 160px;
}
```

**역할:**
- 보노보노의 입 (왼쪽, 오른쪽)

**색상:**
- 배경색: `#fff` (흰색)
- 테두리: `#333` (검은색, 5px)

**위치:**
- 얼굴 상단에서 320px 아래
- 왼쪽: 얼굴 왼쪽에서 160px
- 오른쪽: 얼굴 오른쪽에서 160px

**회전:**
- 왼쪽: `-45deg` (반시계 방향)
- 오른쪽: `45deg` (시계 방향)

**크기:**
- 110px × 90px (타원형)
- `border-radius: 50%` → 원형

---

### 6. .box (입 근처 박스 컨테이너)

```css
.box {
    position: absolute;
    width: 90px;
    height: 90px;
    top: 320px;
}

.box.b_left {
    left: 110px;
}

.box.b_right {
    right: 110px;
}
```

**역할:**
- 입 근처의 선들을 담는 컨테이너
- 선들의 위치 기준점

**위치:**
- 얼굴 상단에서 320px 아래 (입과 같은 높이)
- 왼쪽: 얼굴 왼쪽에서 110px
- 오른쪽: 얼굴 오른쪽에서 110px

**크기:**
- 90px × 90px (정사각형)

**참고:**
- 주석 처리된 `border: 1px solid red;`는 디버깅용

---

### 7. .aline (왼쪽 선들)

```css
.aline {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
    top: 15px;
    right: 5px;
}

.aline1 {
    transform-origin: right center;
    transform: rotate(20deg);
}

.aline2 {
    top: 50px;
}

.aline3 {
    transform-origin: right center;
    top: 80px;
    transform: rotate(-20deg);
}
```

**역할:**
- 보노보노 입 근처의 왼쪽 선들 (수염이나 주름 표현)

**색상:**
- 배경색: `#000` (검은색)

**위치:**
- `.box.b_left` 내부
- `right: 5px`: 박스 오른쪽에서 5px 떨어진 위치
- `top: 15px` (aline1), `top: 50px` (aline2), `top: 80px` (aline3)

**회전:**
- `transform-origin: right center`: 오른쪽 중앙을 기준으로 회전
- `aline1`: `20deg` (위로 기울임)
- `aline2`: 회전 없음 (수평)
- `aline3`: `-20deg` (아래로 기울임)

**크기:**
- 80px × 3px (가로 직선)

**시각화:**

```
.box.b_left
┌─────────────────┐
│                 │
│      ↗ aline1   │ (20deg, 오른쪽 기준)
│                 │
│      ─ aline2   │ (수평)
│                 │
│      ↘ aline3   │ (-20deg, 오른쪽 기준)
│                 │
└─────────────────┘
```

---

### 8. .bline (오른쪽 선들)

```css
.bline {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
    top: 15px;
    right: 5px;
}

.bline1 {
    transform-origin: left center;
    transform: rotate(-20deg);
}

.bline2 {
    top: 50px;
}

.bline3 {
    transform-origin: left center;
    top: 80px;
    transform: rotate(20deg);
}
```

**역할:**
- 보노보노 입 근처의 오른쪽 선들 (수염이나 주름 표현)

**색상:**
- 배경색: `#000` (검은색)

**위치:**
- `.box.b_right` 내부
- `right: 5px`: 박스 오른쪽에서 5px 떨어진 위치
- `top: 15px` (bline1), `top: 50px` (bline2), `top: 80px` (bline3)

**회전:**
- `transform-origin: left center`: 왼쪽 중앙을 기준으로 회전
- `bline1`: `-20deg` (위로 기울임)
- `bline2`: 회전 없음 (수평)
- `bline3`: `20deg` (아래로 기울임)

**크기:**
- 80px × 3px (가로 직선)

**시각화:**

```
.box.b_right
┌─────────────────┐
│                 │
│   bline1 ↖      │ (-20deg, 왼쪽 기준)
│                 │
│   bline2 ─      │ (수평)
│                 │
│   bline3 ↙      │ (20deg, 왼쪽 기준)
│                 │
└─────────────────┘
```

---

## 🎨 색상 팔레트

| 요소 | 색상 코드 | 색상 이름 | 용도 |
|------|----------|----------|------|
| 얼굴 배경 | `cornflowerblue` | 하늘색/파란색 | 얼굴 배경색 |
| 테두리 | `#333` | 진한 회색 | 모든 테두리 |
| 눈/코/선들 | `#000` / `black` | 검은색 | 눈, 코, 선들 |
| 입 배경 | `#fff` | 흰색 | 입 배경색 |

---

## 🔑 핵심 개념 정리

### 1. Position 속성 활용

| 속성 | 설명 | 예시 |
|------|------|------|
| `position: relative` | 기준점 설정 | `.bigbox { position: relative; }` |
| `position: absolute` | 절대 위치 배치 | `.eyeball { position: absolute; }` |
| `top`, `bottom` | 수직 위치 | `.eyeball { top: 190px; }` |
| `left`, `right` | 수평 위치 | `.eyeball.left { left: 100px; }` |
| `z-index` | 레이어 순서 | `.nose { z-index: 1; }` |

### 2. Transform 속성

#### rotate (회전)

```css
.element {
    transform: rotate(45deg);  /* 시계 방향 45도 */
    transform: rotate(-45deg);  /* 반시계 방향 45도 */
}
```

#### transform-origin (회전 중심점)

```css
.element {
    transform-origin: right center;  /* 오른쪽 중앙 기준 */
    transform-origin: left center;   /* 왼쪽 중앙 기준 */
    transform: rotate(20deg);
}
```

**transform-origin 값:**

| 값 | 설명 |
|----|------|
| `left center` | 왼쪽 중앙 |
| `right center` | 오른쪽 중앙 |
| `center center` | 정중앙 (기본값) |
| `top center` | 상단 중앙 |
| `bottom center` | 하단 중앙 |

### 3. 정중앙 정렬 방법

#### 방법 1: margin 사용 (수평 정렬)

```css
.element {
    margin: 0 auto;  /* 좌우 중앙 정렬 */
}
```

#### 방법 2: left + margin-left (절대 위치 중앙 정렬)

```css
.element {
    position: absolute;
    left: 50%;
    margin-left: -20px;  /* 요소 너비의 절반 */
}
```

### 4. 원형 만들기

```css
.circle {
    width: 100px;
    height: 100px;
    border-radius: 50%;  /* 정사각형 → 원 */
}
```

**조건:**
- 너비와 높이가 같아야 함 (정사각형)
- `border-radius: 50%` 적용

### 5. 대칭 배치

```css
/* 왼쪽 */
.element.left {
    left: 100px;
    transform: rotate(-45deg);
}

/* 오른쪽 */
.element.right {
    right: 100px;
    transform: rotate(45deg);
}
```

**팁:**
- `left`와 `right` 값을 조정하여 대칭 배치
- `transform: rotate()`의 각도를 반대 방향으로 설정 (대칭 회전)

---

## 🚀 실습 과제

### 기본 과제

1. **색상 변경**: 보노보노의 얼굴 색상을 다른 색으로 변경해보세요.
2. **크기 변경**: 전체 크기를 1.5배로 키워보세요.
3. **위치 조정**: 눈의 위치를 조정하여 표정을 바꿔보세요.

### 응용 과제

1. **애니메이션 추가**: 눈을 깜빡이게 만들어보세요.
2. **다른 표정 만들기**: 웃는 표정이나 놀란 표정을 만들어보세요.
3. **다른 캐릭터**: 야옹이(포로리)나 너부리를 만들어보세요.

---

## 📚 학습 포인트

1. ✅ **Position 속성**: `relative`와 `absolute`의 관계
2. ✅ **정중앙 정렬**: `left: 50%` + `margin-left: -width/2` 기법
3. ✅ **Transform**: `rotate()`로 회전 효과
4. ✅ **Transform-origin**: 회전 중심점 설정
5. ✅ **z-index**: 레이어 순서 제어
6. ✅ **Box-sizing**: `border-box`로 크기 계산
7. ✅ **대칭 배치**: `left`/`right`와 `rotate()`의 조합

---

## 🔍 디버깅 팁

### 1. 테두리로 위치 확인

```css
.element {
    border: 1px solid red;  /* 임시로 테두리 추가 */
}
```

**예시:**
- `.box`의 주석 처리된 `border: 1px solid red;`는 디버깅용

### 2. 배경색으로 영역 확인

```css
.element {
    background-color: rgba(255, 0, 0, 0.3);  /* 반투명 빨강 */
}
```

### 3. 개발자 도구 활용

- F12로 개발자 도구 열기
- Elements 탭에서 요소 선택
- Styles 패널에서 CSS 수정 및 확인
- Computed 탭에서 실제 크기 확인

### 4. transform-origin 확인

```css
.element {
    transform-origin: right center;
    transform: rotate(20deg);
    /* 회전 중심점이 오른쪽 중앙인지 확인 */
}
```

---

## 💡 참고사항

- 이 예제는 순수 CSS로 캐릭터를 그리는 방법을 학습하기 위한 것입니다.
- 실제 프로젝트에서는 이미지를 사용하는 것이 더 효율적일 수 있습니다.
- CSS로 그림을 그리는 연습은 CSS 속성에 대한 이해도를 높이는 데 도움이 됩니다.
- `transform-origin`은 회전의 중심점을 지정하는 중요한 속성입니다.
- 대칭된 요소는 `left`/`right`와 반대 각도의 `rotate()`를 사용합니다.

---

## 🎯 라이언 캐릭터와의 비교

| 특징 | 라이언 | 보노보노 |
|------|--------|----------|
| **얼굴 색상** | `#d59729` (노란색) | `cornflowerblue` (파란색) |
| **얼굴 모양** | 타원형 (500px × 450px) | 원형 (500px × 500px) |
| **입 구조** | 입 커버로 가림 | 대각선 회전된 입 2개 |
| **특수 요소** | 눈썹, 입 커버 | 입 근처 선들 (aline, bline) |
| **회전 활용** | 입 커버 (45deg) | 입 (45deg), 선들 (20deg) |
| **transform-origin** | 사용 안 함 | 선들에 활용 |

---

## 📝 코드 개선 팁

### 1. 반복 코드 줄이기

```css
/* 현재 (반복) */
.aline {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
}

.bline {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
}

/* 개선 (공통 클래스 사용) */
.line {
    position: absolute;
    width: 80px;
    height: 3px;
    background-color: #000;
}
```

### 2. 변수 사용 (CSS 변수)

```css
:root {
    --face-color: cornflowerblue;
    --border-color: #333;
    --eye-size: 30px;
}

.face {
    background-color: var(--face-color);
    border: 10px solid var(--border-color);
}
```

---

## 🔚 마무리

이 예제를 통해 다음을 학습할 수 있습니다:

1. **Position 속성**을 활용한 정확한 배치
2. **Transform** 속성으로 회전 효과
3. **Transform-origin**으로 회전 중심점 제어
4. **대칭 배치** 기법
5. **z-index**로 레이어 순서 제어

보노보노 캐릭터 만들기를 통해 CSS의 다양한 속성을 실전에서 활용하는 방법을 배울 수 있습니다! 🐰


