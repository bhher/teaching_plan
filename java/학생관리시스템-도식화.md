# 학생 관리 시스템 - 도식화 설명

## 📐 시스템 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                    StudentApp (메인 클래스)                    │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  ArrayList<Student> list                              │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐           │  │
│  │  │ Student  │→ │ Student  │→ │ Student  │→ ...      │  │
│  │  │객체 참조 │  │객체 참조 │  │객체 참조 │           │  │
│  │  └──────────┘  └──────────┘  └──────────┘           │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  메서드들:                                                     │
│  • printAll()     - 전체 출력                                 │
│  • findStudent()  - 검색                                      │
│  • updateStudent() - 수정                                     │
│  • deleteStudent() - 삭제                                     │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ 사용
                              │
                  ┌───────────┴───────────┐
                  │                       │
        ┌─────────▼──────────┐  ┌────────▼──────────┐
        │   Student 클래스    │  │  ArrayList 클래스  │
        │                     │  │  (Java 라이브러리) │
        │  - name: String     │  │                   │
        │  - age: int         │  │  - add()          │
        │                     │  │  - remove()       │
        │  + toString()       │  │  - get()          │
        └─────────────────────┘  │  - size()         │
                                 └───────────────────┘
```

---

## 📁 Student.java

```java
public class Student {
    String name;  // 이름
    int age;      // 나이
    
    // 생성자
    public Student(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // toString() 메서드: 객체를 문자열로 변환
    @Override
    public String toString() {
        return "Student{name='" + name + "', age=" + age + "}";
    }
}
```

---

## 📁 StudentApp.java

```java
import java.util.ArrayList;

public class StudentApp {
    public static void main(String[] args) {

        ArrayList<Student> list = new ArrayList<>();
        list.add(new Student("홍길동", 20));
        list.add(new Student("김철수", 22));

        printAll(list);

        System.out.println("=== 수정 ===");
        updateStudent(list, "홍길동", 25);
        printAll(list);

        System.out.println("=== 삭제 ===");
        deleteStudent(list, "김철수");
        printAll(list);

        System.out.println("=== 검색 ===");
        Student s = findStudent(list, "홍길동");
        System.out.println(s);
    }

    // 메서드들
    static Student findStudent(ArrayList<Student> list, String name) {
        for (Student s : list) {
            if (s.name.equals(name)) return s;
        }
        return null;
    }

    static boolean updateStudent(ArrayList<Student> list, String name, int newAge) {
        for (Student s : list) {
            if (s.name.equals(name)) {
                s.age = newAge;
                return true;
            }
        }
        return false;
    }

    static boolean deleteStudent(ArrayList<Student> list, String name) {
        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).name.equals(name)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }

    static void printAll(ArrayList<Student> list) {
        for (Student s : list) {
            System.out.println(s);
        }
    }
}
```

---

## 🔍 실행 흐름 도식화

### 1️⃣ 초기화 단계
```
main() 메서드 시작
    ↓
ArrayList<Student> list 생성 (빈 리스트)
    ↓
list.add(new Student("홍길동", 20))
    ↓
list.add(new Student("김철수", 22))
    ↓
┌─────────────────────────────────────┐
│ ArrayList 내부 상태:                 │
│                                     │
│ [0] → Student{name="홍길동", age=20}│
│ [1] → Student{name="김철수", age=22}│
└─────────────────────────────────────┘
```

### 2️⃣ 전체 출력 (printAll)
```
printAll(list) 호출
    ↓
for (Student s : list) 반복
    ↓
s = list[0] → Student{name="홍길동", age=20}
    ↓
System.out.println(s) → s.toString() 호출
    ↓
출력: Student{name='홍길동', age=20}
    ↓
s = list[1] → Student{name="김철수", age=22}
    ↓
출력: Student{name='김철수', age=22}
```

### 3️⃣ 수정 (updateStudent)
```
updateStudent(list, "홍길동", 25) 호출
    ↓
for (Student s : list) 반복
    ↓
s = list[0] → Student{name="홍길동", age=20}
    ↓
s.name.equals("홍길동")? → true
    ↓
s.age = 25 (수정!)
    ↓
return true
    ↓
┌─────────────────────────────────────┐
│ ArrayList 내부 상태 (수정 후):       │
│                                     │
│ [0] → Student{name="홍길동", age=25}│ ← 수정됨
│ [1] → Student{name="김철수", age=22}│
└─────────────────────────────────────┘
```

### 4️⃣ 삭제 (deleteStudent)
```
deleteStudent(list, "김철수") 호출
    ↓
for (int i = 0; i < list.size(); i++) 반복
    ↓
i = 0: list.get(0).name = "홍길동" ≠ "김철수"
    ↓
i = 1: list.get(1).name = "김철수" = "김철수" ✓
    ↓
list.remove(1) 실행
    ↓
┌─────────────────────────────────────┐
│ ArrayList 내부 상태 (삭제 후):       │
│                                     │
│ [0] → Student{name="홍길동", age=25}│
│ (인덱스 자동 재정렬됨)                │
└─────────────────────────────────────┘
```

### 5️⃣ 검색 (findStudent)
```
findStudent(list, "홍길동") 호출
    ↓
for (Student s : list) 반복
    ↓
s = list[0] → Student{name="홍길동", age=25}
    ↓
s.name.equals("홍길동")? → true
    ↓
return s (Student 객체 반환)
    ↓
main()에서 Student s = (반환된 객체)
    ↓
System.out.println(s) → Student{name='홍길동', age=25} 출력
```

---

## 📊 메서드별 동작 원리

### 🔎 findStudent() - 검색
```java
static Student findStudent(ArrayList<Student> list, String name) {
    for (Student s : list) {          // 향상된 for문: 리스트의 각 요소를 순회
        if (s.name.equals(name))      // 이름이 일치하면
            return s;                 // 해당 Student 객체 반환
    }
    return null;                      // 찾지 못하면 null 반환
}
```

**동작 순서:**
1. 리스트의 첫 번째 요소부터 마지막까지 순회
2. 각 Student 객체의 name 필드와 검색할 name 비교
3. 일치하는 객체를 찾으면 즉시 반환 (선형 검색)
4. 찾지 못하면 null 반환

---

### ✏️ updateStudent() - 수정
```java
static boolean updateStudent(ArrayList<Student> list, String name, int newAge) {
    for (Student s : list) {          // 리스트 순회
        if (s.name.equals(name)) {    // 이름이 일치하면
            s.age = newAge;           // age 필드 직접 수정
            return true;              // 성공
        }
    }
    return false;                     // 실패 (해당 이름의 학생 없음)
}
```

**동작 순서:**
1. 리스트를 순회하며 이름이 일치하는 Student 찾기
2. 찾은 객체의 age 필드를 newAge로 변경
3. 성공/실패 여부를 boolean으로 반환

**⚠️ 주의사항:**
- ArrayList에 저장된 것은 객체의 **참조(주소)**입니다
- 참조를 통해 필드를 수정하면 원본 객체가 수정됩니다

---

### 🗑️ deleteStudent() - 삭제
```java
static boolean deleteStudent(ArrayList<Student> list, String name) {
    for (int i = 0; i < list.size(); i++) {      // 인덱스로 순회
        if (list.get(i).name.equals(name)) {      // 이름 일치 확인
            list.remove(i);                       // 인덱스 i의 요소 제거
            return true;                          // 성공
        }
    }
    return false;                                 // 실패
}
```

**동작 순서:**
1. 인덱스 0부터 시작하여 순회 (일반 for문 필요)
2. 해당 인덱스의 Student 이름이 일치하면
3. `list.remove(i)`로 해당 인덱스의 요소 제거
4. 제거 후 뒤의 요소들이 자동으로 앞으로 이동

**⚠️ 왜 일반 for문을 사용하나요?**
- 삭제 시 인덱스가 필요하기 때문
- 향상된 for문에서는 현재 인덱스를 알 수 없음

---

### 📝 printAll() - 전체 출력
```java
static void printAll(ArrayList<Student> list) {
    for (Student s : list) {          // 리스트의 모든 요소 순회
        System.out.println(s);        // toString() 자동 호출
    }
}
```

**동작 순서:**
1. 리스트의 모든 Student 객체를 순회
2. `System.out.println(s)` 호출 시 자동으로 `s.toString()` 호출
3. Student 클래스의 toString() 메서드가 정의한 형식으로 출력

---

## 🎯 핵심 개념 도식화

### 1. 객체와 참조
```
┌─────────────────────────────────────────────────────┐
│ ArrayList<Student> list                             │
│                                                     │
│  인덱스  │  참조(주소)  │  실제 객체                   │
│ ────────┼─────────────┼──────────────────────────│
│   0     │  0x1000    │  Student{name="홍길동",   │
│         │            │            age=20}        │
│   1     │  0x2000    │  Student{name="김철수",   │
│         │            │            age=22}        │
└─────────────────────────────────────────────────────┘
         │              │
         └──────┬───────┘
                │
        list.get(0)는 참조를 반환
        = 실제 Student 객체를 가리키는 주소
```

### 2. 필드 직접 접근 vs Getter/Setter
```java
// 현재 코드: 필드에 직접 접근
s.name      // public 필드 직접 접근
s.age = 25  // public 필드 직접 수정

// 캡슐화된 코드 (권장):
s.getName()      // getter 메서드로 접근
s.setAge(25)     // setter 메서드로 수정
```

### 3. ArrayList 동작 원리
```
추가 (add):
list.add(new Student("홍길동", 20))
    ↓
ArrayList 내부 배열에 추가
[null, null, null, ...] → [Student객체, null, null, ...]

삭제 (remove):
list.remove(0)
    ↓
해당 인덱스 요소 제거 후 뒤 요소들 앞으로 이동
[객체1, 객체2, 객체3] → [객체2, 객체3, null]
    ↑                    ↑
    제거됨                자동으로 앞당겨짐

검색 (get):
list.get(0)
    ↓
인덱스 0의 요소 반환 (참조 반환)
```

---

## 🔄 전체 실행 결과

```
Student{name='홍길동', age=20}
Student{name='김철수', age=22}
=== 수정 ===
Student{name='홍길동', age=25}    ← 나이가 20에서 25로 변경됨
Student{name='김철수', age=22}
=== 삭제 ===
Student{name='홍길동', age=25}    ← 김철수가 삭제되어 홍길동만 남음
=== 검색 ===
Student{name='홍길동', age=25}
```

---

## 💡 핵심 포인트

1. **ArrayList 활용**
   - 동적으로 크기가 변하는 배열
   - 객체 참조를 저장하는 리스트

2. **객체 참조**
   - ArrayList에 저장되는 것은 객체 자체가 아닌 **참조(주소)**
   - 같은 객체를 여러 리스트에서 참조 가능

3. **향상된 for문 vs 일반 for문**
   - 향상된 for문: 요소만 필요할 때 (읽기, 수정)
   - 일반 for문: 인덱스가 필요할 때 (삭제)

4. **toString() 메서드**
   - 객체를 문자열로 표현
   - `System.out.println()`에서 자동 호출

5. **문자열 비교**
   - `==` 연산자 사용 금지
   - `equals()` 메서드 사용 필수

---

## 🎓 학습 포인트

### ✅ 이해해야 할 개념
- 클래스와 객체의 관계
- ArrayList와 배열의 차이
- 객체 참조와 복사
- 메서드의 매개변수 전달 방식

### ✅ 주의할 점
1. 삭제 시 인덱스 순회 방식 사용
2. 문자열 비교는 `equals()` 사용
3. null 체크 필요 (findStudent 결과)
4. 필드 접근 제어자 (현재는 public, 실제로는 private 권장)

### ✅ 개선 가능한 부분
1. Student 클래스의 필드를 private로 변경
2. Getter/Setter 메서드 추가
3. 생성자 추가
4. 예외 처리 (null 체크 등)
