# 상속, 추상 클래스, 인터페이스

## 📌 학습 목표

- 상속의 개념과 사용법을 이해한다
- 추상 클래스와 추상 메서드를 이해한다
- 인터페이스의 개념과 구현 방법을 이해한다
- 상속, 추상 클래스, 인터페이스의 차이를 구분할 수 있다

---

## 1️⃣ 상속 (Inheritance)

### 상속이란?

**상속**은 기존 클래스의 속성(필드)과 메서드를 새로운 클래스가 **물려받는** 것입니다.

- **부모 클래스 (Parent Class)**: 상속을 제공하는 클래스 (상위 클래스, 슈퍼 클래스)
- **자식 클래스 (Child Class)**: 상속을 받는 클래스 (하위 클래스, 서브 클래스)

### 상속의 장점

1. **코드 재사용**: 기존 클래스의 코드를 재사용
2. **유지보수 용이**: 부모 클래스 수정 시 자식 클래스에 자동 반영
3. **계층 구조**: 클래스 간 관계를 명확히 표현
4. **확장성**: 기존 기능을 확장하여 새로운 클래스 생성

### 상속 문법

```java
class 자식클래스 extends 부모클래스 {
    // 자식 클래스만의 추가 필드와 메서드
}
```

### 상속 예제

```java
// 부모 클래스 (상위 클래스)
class Animal {
    String name;
    int age;
    
    public Animal(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    public void eat() {
        System.out.println(name + "이(가) 먹습니다.");
    }
    
    public void sleep() {
        System.out.println(name + "이(가) 잡니다.");
    }
    
    public void makeSound() {
        System.out.println(name + "이(가) 소리를 냅니다.");
    }
    
    public void displayInfo() {
        System.out.println("이름: " + name + ", 나이: " + age);
    }
}

// 자식 클래스 1: Dog (Animal을 상속)
class Dog extends Animal {
    String breed;  // 자식 클래스만의 추가 필드
    
    public Dog(String name, int age, String breed) {
        super(name, age);  // 부모 클래스의 생성자 호출
        this.breed = breed;
    }
    
    // 메서드 오버라이딩: 부모 클래스의 메서드를 재정의
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 멍멍 짖습니다.");
    }
    
    // 자식 클래스만의 메서드
    public void bark() {
        System.out.println(name + "이(가) 왈왈 짖습니다.");
    }
    
    @Override
    public void displayInfo() {
        super.displayInfo();  // 부모 클래스의 메서드 호출
        System.out.println("품종: " + breed);
    }
}

// 자식 클래스 2: Cat (Animal을 상속)
class Cat extends Animal {
    String color;
    
    public Cat(String name, int age, String color) {
        super(name, age);
        this.color = color;
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 야옹 웁니다.");
    }
    
    public void scratch() {
        System.out.println(name + "이(가) 할퀴고 있습니다.");
    }
}

// 사용 예제
public class InheritanceExample {
    public static void main(String[] args) {
        // 자식 클래스 객체 생성
        Dog dog = new Dog("뽀삐", 3, "골든 리트리버");
        Cat cat = new Cat("나비", 2, "검은색");
        
        // 부모 클래스의 메서드 사용
        dog.eat();        // 상속받은 메서드
        dog.sleep();      // 상속받은 메서드
        dog.makeSound();  // 오버라이딩한 메서드
        dog.bark();       // 자식 클래스만의 메서드
        dog.displayInfo();
        
        System.out.println();
        
        cat.eat();
        cat.sleep();
        cat.makeSound();
        cat.scratch();
        cat.displayInfo();
    }
}
```

### 상속의 특징

1. **단일 상속만 가능**: Java는 하나의 부모 클래스만 상속 가능
2. **super 키워드**: 부모 클래스의 생성자, 메서드, 필드 접근
3. **메서드 오버라이딩**: 자식 클래스에서 부모 클래스의 메서드 재정의
4. **모든 클래스는 Object 클래스 상속**: Java의 최상위 클래스

### super 키워드

```java
class Parent {
    String name;
    
    public Parent(String name) {
        this.name = name;
    }
    
    public void print() {
        System.out.println("부모: " + name);
    }
}

class Child extends Parent {
    int age;
    
    public Child(String name, int age) {
        super(name);  // 부모 클래스의 생성자 호출
        this.age = age;
    }
    
    @Override
    public void print() {
        super.print();  // 부모 클래스의 메서드 호출
        System.out.println("자식: " + age);
    }
}
```

### 메서드 오버라이딩 vs 오버로딩

| 구분 | 오버라이딩 (Overriding) | 오버로딩 (Overloading) |
|------|------------------------|----------------------|
| **정의** | 부모 클래스의 메서드를 재정의 | 같은 이름, 다른 매개변수 |
| **상속** | 상속 관계 필요 | 같은 클래스 내 |
| **메서드 시그니처** | 동일해야 함 | 달라야 함 |
| **@Override** | 사용 권장 | 사용 불가 |

### 상속 구조 시각화

#### 계층 구조

```
┌─────────────────────┐
│   부모 클래스        │
│    (Animal)         │
│                     │
│  + name: String     │
│  + age: int         │
│                     │
│  + eat()            │
│  + sleep()          │
│  + makeSound()      │
└──────────┬──────────┘
           │ extends
           │
    ┌──────┴──────┐
    │             │
┌───▼────┐   ┌───▼────┐
│  자식1  │   │  자식2  │
│  (Dog) │   │  (Cat) │
│        │   │        │
│ +breed │   │ +color │
│        │   │        │
│ +bark()│   │+scratch│
└────────┘   └────────┘
```

#### 메모리 구조

```
┌─────────────────────────────────────────┐
│           Heap 메모리                    │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐ │
│  │     Dog 객체                       │ │
│  │  ┌─────────────────────────────┐  │ │
│  │  │ Animal 부분 (부모)           │  │ │
│  │  │ name: "뽀삐"                │  │ │
│  │  │ age: 3                      │  │ │
│  │  └─────────────────────────────┘  │ │
│  │  ┌─────────────────────────────┐  │ │
│  │  │ Dog 부분 (자식)              │  │ │
│  │  │ breed: "골든리트리버"        │  │ │
│  │  └─────────────────────────────┘  │ │
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

#### 메서드 호출 흐름

```
dog.makeSound() 호출
    ↓
1. Dog 클래스에서 makeSound() 찾기
    ↓
2. 오버라이딩된 메서드 발견!
    ↓
3. Dog.makeSound() 실행
    └─ "뽀삐이(가) 멍멍 짖습니다."

dog.eat() 호출
    ↓
1. Dog 클래스에서 eat() 찾기
    ↓ (없음)
2. 부모 클래스 Animal에서 eat() 찾기
    ↓ (발견!)
3. Animal.eat() 실행
    └─ "뽀삐이(가) 먹습니다."
```

---

## 2️⃣ 추상 클래스 (Abstract Class)

### 추상 클래스란?

**추상 클래스**는 **추상 메서드**를 포함할 수 있는 클래스입니다. 객체를 직접 생성할 수 없고, 상속을 통해 사용합니다.

### 추상 메서드란?

**추상 메서드**는 선언만 있고 구현이 없는 메서드입니다. 자식 클래스에서 반드시 구현해야 합니다.

### 추상 클래스의 특징

1. **abstract 키워드 사용**: 클래스와 메서드에 `abstract` 사용
2. **객체 생성 불가**: `new` 키워드로 직접 생성 불가
3. **일반 메서드 포함 가능**: 추상 메서드와 일반 메서드 모두 가능
4. **상속 필수**: 추상 클래스를 상속받은 클래스는 추상 메서드를 반드시 구현해야 함

### 추상 클래스 문법

```java
abstract class 클래스명 {
    // 일반 필드
    // 일반 메서드
    // 추상 메서드
    abstract 반환타입 메서드명();
}
```

### 추상 클래스 예제

```java
// 추상 클래스
abstract class Animal {
    String name;
    int age;
    
    public Animal(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // 일반 메서드
    public void eat() {
        System.out.println(name + "이(가) 먹습니다.");
    }
    
    public void sleep() {
        System.out.println(name + "이(가) 잡니다.");
    }
    
    // 추상 메서드: 자식 클래스에서 반드시 구현해야 함
    public abstract void makeSound();
    public abstract void move();
    
    // 정보 출력 (일반 메서드)
    public void displayInfo() {
        System.out.println("이름: " + name + ", 나이: " + age);
    }
}

// 추상 클래스를 상속받는 자식 클래스 1
class Dog extends Animal {
    String breed;
    
    public Dog(String name, int age, String breed) {
        super(name, age);
        this.breed = breed;
    }
    
    // 추상 메서드 구현 (필수)
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 멍멍 짖습니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 뛰어다닙니다.");
    }
}

// 추상 클래스를 상속받는 자식 클래스 2
class Cat extends Animal {
    String color;
    
    public Cat(String name, int age, String color) {
        super(name, age);
        this.color = color;
    }
    
    // 추상 메서드 구현 (필수)
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 야옹 웁니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 조용히 걸어다닙니다.");
    }
}

// 추상 클래스를 상속받는 자식 클래스 3
class Bird extends Animal {
    boolean canFly;
    
    public Bird(String name, int age, boolean canFly) {
        super(name, age);
        this.canFly = canFly;
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 짹짹 웁니다.");
    }
    
    @Override
    public void move() {
        if (canFly) {
            System.out.println(name + "이(가) 날아다닙니다.");
        } else {
            System.out.println(name + "이(가) 걷습니다.");
        }
    }
}

// 사용 예제
public class AbstractClassExample {
    public static void main(String[] args) {
        // 추상 클래스는 객체 생성 불가
        // Animal animal = new Animal("동물", 1);  // ❌ 컴파일 에러
        
        // 자식 클래스로 객체 생성
        Dog dog = new Dog("뽀삐", 3, "골든 리트리버");
        Cat cat = new Cat("나비", 2, "검은색");
        Bird bird = new Bird("짹짹", 1, true);
        
        // 부모 클래스 타입으로 참조 (다형성)
        Animal animal1 = new Dog("멍멍", 2, "푸들");
        Animal animal2 = new Cat("야옹", 1, "흰색");
        
        // 일반 메서드 호출
        dog.eat();
        dog.sleep();
        dog.displayInfo();
        
        // 추상 메서드 호출 (구현된 메서드)
        dog.makeSound();
        dog.move();
        
        System.out.println();
        
        cat.makeSound();
        cat.move();
        
        System.out.println();
        
        bird.makeSound();
        bird.move();
        
        // 다형성 활용
        System.out.println("\n=== 다형성 ===");
        Animal[] animals = {dog, cat, bird};
        for (Animal animal : animals) {
            animal.makeSound();
            animal.move();
        }
    }
}
```

### 추상 클래스를 사용하는 이유

1. **공통 기능 제공**: 모든 자식 클래스가 공통으로 사용하는 메서드 제공
2. **강제 구현**: 추상 메서드로 자식 클래스에서 반드시 구현하도록 강제
3. **코드 재사용**: 일반 메서드를 재사용하여 코드 중복 감소
4. **계층 구조**: 관련 클래스들을 묶어 계층 구조 표현

---

## 3️⃣ 인터페이스 (Interface)

### 인터페이스란?

**인터페이스**는 클래스가 **반드시 구현해야 하는 메서드**들을 정의하는 것입니다. "계약서"와 같은 역할을 합니다.

### 인터페이스의 특징

1. **interface 키워드 사용**: 클래스가 아닌 인터페이스로 선언
2. **추상 메서드만 포함** (Java 8 이전): 모든 메서드가 추상 메서드
3. **다중 구현 가능**: 하나의 클래스가 여러 인터페이스 구현 가능
4. **상수 포함 가능**: `public static final` 상수 포함
5. **객체 생성 불가**: 직접 객체 생성 불가

### 인터페이스 문법

```java
interface 인터페이스명 {
    // 상수 (public static final 생략 가능)
    타입 상수명 = 값;
    
    // 추상 메서드 (public abstract 생략 가능)
    반환타입 메서드명();
}

// 구현
class 클래스명 implements 인터페이스명 {
    // 인터페이스의 메서드 구현 (필수)
}
```

### 인터페이스 예제

```java
// 인터페이스 1: 날 수 있는 것
interface Flyable {
    // 추상 메서드 (abstract 키워드 생략 가능)
    void fly();
    
    // 상수 (public static final 생략 가능)
    int MAX_SPEED = 100;
}

// 인터페이스 2: 수영할 수 있는 것
interface Swimmable {
    void swim();
}

// 인터페이스 3: 걸을 수 있는 것
interface Walkable {
    void walk();
}

// 클래스가 여러 인터페이스 구현
class Duck implements Flyable, Swimmable, Walkable {
    String name;
    
    public Duck(String name) {
        this.name = name;
    }
    
    @Override
    public void fly() {
        System.out.println(name + "이(가) 날아갑니다.");
    }
    
    @Override
    public void swim() {
        System.out.println(name + "이(가) 수영합니다.");
    }
    
    @Override
    public void walk() {
        System.out.println(name + "이(가) 걷습니다.");
    }
}

// 인터페이스 하나만 구현
class Fish implements Swimmable {
    String name;
    
    public Fish(String name) {
        this.name = name;
    }
    
    @Override
    public void swim() {
        System.out.println(name + "이(가) 헤엄칩니다.");
    }
}

// 인터페이스 하나만 구현
class Airplane implements Flyable {
    String model;
    
    public Airplane(String model) {
        this.model = model;
    }
    
    @Override
    public void fly() {
        System.out.println(model + "이(가) 날아갑니다. (최대 속도: " + MAX_SPEED + "km/h)");
    }
}

// 사용 예제
public class InterfaceExample {
    public static void main(String[] args) {
        // 인터페이스는 객체 생성 불가
        // Flyable flyable = new Flyable();  // ❌ 컴파일 에러
        
        // 구현 클래스로 객체 생성
        Duck duck = new Duck("도날드");
        Fish fish = new Fish("니모");
        Airplane airplane = new Airplane("보잉747");
        
        // 인터페이스 메서드 호출
        duck.fly();
        duck.swim();
        duck.walk();
        
        System.out.println();
        
        fish.swim();
        
        System.out.println();
        
        airplane.fly();
        
        // 인터페이스 타입으로 참조 (다형성)
        System.out.println("\n=== 다형성 ===");
        Flyable[] flyables = {duck, airplane};
        for (Flyable flyable : flyables) {
            flyable.fly();
        }
        
        Swimmable[] swimmables = {duck, fish};
        for (Swimmable swimmable : swimmables) {
            swimmable.swim();
        }
    }
}
```

### 인터페이스 상속 (인터페이스 확장)

```java
interface Animal {
    void eat();
    void sleep();
}

// 인터페이스가 인터페이스를 상속 (extends 사용)
interface Pet extends Animal {
    void play();
}

// Pet 인터페이스를 구현하면 Animal의 메서드도 구현해야 함
class Dog implements Pet {
    @Override
    public void eat() {
        System.out.println("먹습니다.");
    }
    
    @Override
    public void sleep() {
        System.out.println("잡니다.");
    }
    
    @Override
    public void play() {
        System.out.println("놉니다.");
    }
}
```

### 인터페이스를 사용하는 이유

1. **다중 상속 효과**: 하나의 클래스가 여러 인터페이스 구현 가능
2. **계약 정의**: 클래스가 반드시 구현해야 하는 메서드 명시
3. **느슨한 결합**: 인터페이스를 통해 클래스 간 결합도 감소
4. **표준화**: 공통 규격을 정의하여 일관성 유지

---

## 4️⃣ 비교: 상속 vs 추상 클래스 vs 인터페이스

### 비교표

| 구분 | 상속 (extends) | 추상 클래스 (abstract) | 인터페이스 (interface) |
|------|---------------|---------------------|---------------------|
| **키워드** | `extends` | `abstract class` | `interface` |
| **객체 생성** | 가능 | 불가 | 불가 |
| **일반 메서드** | 포함 가능 | 포함 가능 | Java 8+ default 메서드 |
| **추상 메서드** | 선택적 | 선택적 | 필수 (Java 8 이전) |
| **필드** | 일반 필드 | 일반 필드 | 상수만 (public static final) |
| **다중 상속** | 불가 (단일) | 불가 (단일) | 가능 (다중 구현) |
| **생성자** | 가능 | 가능 | 불가 |
| **사용 목적** | 코드 재사용 | 공통 기능 + 강제 구현 | 계약 정의, 다중 상속 |

### 언제 무엇을 사용할까?

#### ✅ 일반 클래스 상속 (extends)

- **IS-A 관계** (예: Dog IS-A Animal)
- 코드 재사용이 주 목적
- 공통 기능을 그대로 사용

```java
class Animal { }
class Dog extends Animal { }  // Dog는 Animal이다
```

#### ✅ 추상 클래스 (abstract class)

- **IS-A 관계** + **공통 기능** + **강제 구현**
- 일부 메서드는 공통 제공, 일부는 자식에서 구현
- 단일 상속

```java
abstract class Animal {
    public void eat() { }      // 공통 기능
    public abstract void makeSound();  // 강제 구현
}
class Dog extends Animal { }
```

#### ✅ 인터페이스 (interface)

- **CAN-DO 관계** (예: Duck CAN fly, swim, walk)
- 행동(능력) 정의
- 다중 구현 가능
- 느슨한 결합

```java
interface Flyable { void fly(); }
interface Swimmable { void swim(); }
class Duck implements Flyable, Swimmable { }
```

---

## 5️⃣ 종합 예제

### 상속 + 추상 클래스 + 인터페이스 함께 사용

```java
// 추상 클래스: 동물
abstract class Animal {
    protected String name;
    protected int age;
    
    public Animal(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // 일반 메서드
    public void eat() {
        System.out.println(name + "이(가) 먹습니다.");
    }
    
    public void sleep() {
        System.out.println(name + "이(가) 잡니다.");
    }
    
    // 추상 메서드
    public abstract void makeSound();
    public abstract void move();
}

// 인터페이스 1: 날 수 있는 것
interface Flyable {
    void fly();
}

// 인터페이스 2: 수영할 수 있는 것
interface Swimmable {
    void swim();
}

// 자식 클래스 1: 새 (상속 + 인터페이스 구현)
class Bird extends Animal implements Flyable {
    private String species;
    
    public Bird(String name, int age, String species) {
        super(name, age);
        this.species = species;
    }
    
    // 추상 메서드 구현
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 짹짹 웁니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 날아다닙니다.");
    }
    
    // 인터페이스 메서드 구현
    @Override
    public void fly() {
        System.out.println(name + "이(가) 하늘을 날아갑니다.");
    }
}

// 자식 클래스 2: 물고기 (상속 + 인터페이스 구현)
class Fish extends Animal implements Swimmable {
    private String habitat;
    
    public Fish(String name, int age, String habitat) {
        super(name, age);
        this.habitat = habitat;
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + "은(는) 소리를 내지 않습니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 헤엄쳐 다닙니다.");
    }
    
    @Override
    public void swim() {
        System.out.println(name + "이(가) " + habitat + "에서 수영합니다.");
    }
}

// 자식 클래스 3: 오리 (상속 + 여러 인터페이스 구현)
class Duck extends Animal implements Flyable, Swimmable {
    private String color;
    
    public Duck(String name, int age, String color) {
        super(name, age);
        this.color = color;
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 꽥꽥 웁니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 걸어다닙니다.");
    }
    
    @Override
    public void fly() {
        System.out.println(name + "이(가) 낮게 날아갑니다.");
    }
    
    @Override
    public void swim() {
        System.out.println(name + "이(가) 연못에서 수영합니다.");
    }
}

// 사용 예제
public class ComprehensiveExample {
    public static void main(String[] args) {
        Bird bird = new Bird("짹짹", 1, "참새");
        Fish fish = new Fish("니모", 2, "바다");
        Duck duck = new Duck("도날드", 3, "노란색");
        
        System.out.println("=== 새 ===");
        bird.eat();
        bird.sleep();
        bird.makeSound();
        bird.move();
        bird.fly();
        
        System.out.println("\n=== 물고기 ===");
        fish.eat();
        fish.sleep();
        fish.makeSound();
        fish.move();
        fish.swim();
        
        System.out.println("\n=== 오리 ===");
        duck.eat();
        duck.sleep();
        duck.makeSound();
        duck.move();
        duck.fly();
        duck.swim();
        
        // 다형성 활용
        System.out.println("\n=== 다형성 ===");
        Animal[] animals = {bird, fish, duck};
        for (Animal animal : animals) {
            animal.makeSound();
            animal.move();
        }
        
        Flyable[] flyables = {bird, duck};
        for (Flyable flyable : flyables) {
            flyable.fly();
        }
        
        Swimmable[] swimmables = {fish, duck};
        for (Swimmable swimmable : swimmables) {
            swimmable.swim();
        }
    }
}
```

---

## 6️⃣ 핵심 정리

### 상속 (Inheritance)

- **키워드**: `extends`
- **목적**: 코드 재사용, IS-A 관계
- **특징**: 단일 상속, 객체 생성 가능
- **예시**: `class Dog extends Animal { }`

### 추상 클래스 (Abstract Class)

- **키워드**: `abstract class`
- **목적**: 공통 기능 제공 + 강제 구현
- **특징**: 추상 메서드 포함 가능, 객체 생성 불가
- **예시**: `abstract class Animal { abstract void makeSound(); }`

### 인터페이스 (Interface)

- **키워드**: `interface`, `implements`
- **목적**: 계약 정의, CAN-DO 관계
- **특징**: 다중 구현 가능, 객체 생성 불가
- **예시**: `interface Flyable { void fly(); }`

### 선택 가이드

| 상황 | 사용할 것 |
|------|----------|
| 코드 재사용만 필요 | 일반 클래스 상속 |
| 공통 기능 + 강제 구현 | 추상 클래스 |
| 행동(능력) 정의 | 인터페이스 |
| 여러 능력 정의 | 인터페이스 (다중 구현) |
| IS-A 관계 | 상속 또는 추상 클래스 |
| CAN-DO 관계 | 인터페이스 |

---

## 7️⃣ 학습 체크리스트

- [ ] 상속의 개념을 이해했다
- [ ] `extends` 키워드로 상속을 구현할 수 있다
- [ ] `super` 키워드를 사용할 수 있다
- [ ] 메서드 오버라이딩을 이해했다
- [ ] 추상 클래스와 추상 메서드의 개념을 이해했다
- [ ] `abstract` 키워드를 사용할 수 있다
- [ ] 인터페이스의 개념을 이해했다
- [ ] `interface`와 `implements` 키워드를 사용할 수 있다
- [ ] 다중 인터페이스 구현을 이해했다
- [ ] 상속, 추상 클래스, 인터페이스의 차이를 설명할 수 있다
- [ ] 각각을 언제 사용해야 하는지 판단할 수 있다
- [ ] 다형성을 이해하고 활용할 수 있다

---

## 📚 관련 자료

- [Java 8장: 객체지향 핵심 개념](./8장_객체지향_핵심_개념.md)
- [Java 7장: 객체지향 프로그래밍 기초](./7장_객체지향_프로그래밍_기초.md)
- [예제 코드: InheritanceExample.java](./example8/InheritanceExample.java)
- [예제 코드: AbstractClassExample.java](./example8/AbstractClassExample.java)
- [예제 코드: InterfaceExample.java](./example8/InterfaceExample.java)

