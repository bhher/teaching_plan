# 상속 시각화

## 📌 상속의 계층 구조

### 기본 상속 구조

```
┌─────────────────────┐
│   부모 클래스        │
│    (Animal)         │
│                     │
│  + name: String     │
│  + age: int         │
│                     │
│  + eat()            │
│  + sleep()          │
│  + makeSound()      │
└──────────┬──────────┘
           │ extends
           │
    ┌──────┴──────┐
    │             │
┌───▼────┐   ┌───▼────┐
│  자식1  │   │  자식2  │
│  (Dog) │   │  (Cat) │
│        │   │        │
│ +breed │   │ +color │
│        │   │        │
│ +bark()│   │+scratch│
└────────┘   └────────┘
```

### 상속 예제 구조

```
┌──────────────────────────────┐
│      Animal (부모 클래스)      │
├──────────────────────────────┤
│ - name: String               │
│ - age: int                   │
├──────────────────────────────┤
│ + eat()                      │
│ + sleep()                    │
│ + makeSound()                │
│ + displayInfo()              │
└──────────┬───────────────────┘
           │
           │ extends
           │
    ┌──────┴──────┬──────────────┐
    │             │              │
┌───▼──────┐ ┌───▼──────┐ ┌────▼─────┐
│   Dog    │ │   Cat    │ │  Bird    │
│(자식클래스)│ │(자식클래스)│ │(자식클래스)│
├──────────┤ ├──────────┤ ├──────────┤
│ - breed  │ │ - color  │ │ -canFly  │
├──────────┤ ├──────────┤ ├──────────┤
│ +bark()  │ │+scratch()│ │ +fly()   │
│          │ │          │ │          │
│ @Override│ │ @Override│ │ @Override│
│ makeSound│ │ makeSound│ │ makeSound│
└──────────┘ └──────────┘ └──────────┘
```

---

## 🔍 상속의 메모리 구조

### 객체 생성 시 메모리 구조

```
┌─────────────────────────────────────────┐
│           Heap 메모리                    │
├─────────────────────────────────────────┤
│                                         │
│  ┌───────────────────────────────────┐ │
│  │     Dog 객체 (dog)                 │ │
│  │  ┌─────────────────────────────┐  │ │
│  │  │ Animal 부분 (부모 클래스)     │  │ │
│  │  │ ┌───────────────────────┐   │  │ │
│  │  │ │ name: "뽀삐"         │   │  │ │
│  │  │ │ age: 3               │   │  │ │
│  │  │ └───────────────────────┘   │  │ │
│  │  │ eat() 메서드 참조            │  │ │
│  │  │ sleep() 메서드 참조          │  │ │
│  │  └─────────────────────────────┘  │ │
│  │  ┌─────────────────────────────┐  │ │
│  │  │ Dog 부분 (자식 클래스)        │  │ │
│  │  │ ┌───────────────────────┐   │  │ │
│  │  │ │ breed: "골든리트리버"  │   │  │ │
│  │  │ └───────────────────────┘   │  │ │
│  │  │ bark() 메서드 참조           │  │ │
│  │  │ makeSound() 오버라이딩 참조  │  │ │
│  │  └─────────────────────────────┘  │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │     Cat 객체 (cat)                 │ │
│  │  ┌─────────────────────────────┐  │ │
│  │  │ Animal 부분 (부모 클래스)     │  │ │
│  │  │ name: "나비"                 │  │ │
│  │  │ age: 2                       │  │ │
│  │  └─────────────────────────────┘  │ │
│  │  ┌─────────────────────────────┐  │ │
│  │  │ Cat 부분 (자식 클래스)        │  │ │
│  │  │ color: "검은색"              │  │ │
│  │  └─────────────────────────────┘  │ │
│  └───────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Method Area (Static)            │
├─────────────────────────────────────────┤
│  Animal 클래스 정보                      │
│  - Animal.eat() 메서드                   │
│  - Animal.sleep() 메서드                 │
│  - Animal.makeSound() 메서드             │
│                                         │
│  Dog 클래스 정보                         │
│  - Dog.bark() 메서드                     │
│  - Dog.makeSound() (오버라이딩)          │
│                                         │
│  Cat 클래스 정보                         │
│  - Cat.scratch() 메서드                  │
│  - Cat.makeSound() (오버라이딩)          │
└─────────────────────────────────────────┘
```

---

## 🔄 상속의 실행 흐름

### 객체 생성 과정

```
Dog dog = new Dog("뽀삐", 3, "골든리트리버");

1단계: Dog 생성자 호출
   ↓
2단계: super(name, age) 실행
   ↓
3단계: Animal 생성자 호출
   ↓
4단계: Animal 필드 초기화
   ├─ this.name = "뽀삐"
   └─ this.age = 3
   ↓
5단계: Animal 생성자 종료
   ↓
6단계: Dog 생성자로 돌아옴
   ↓
7단계: Dog 필드 초기화
   └─ this.breed = "골든리트리버"
   ↓
8단계: Dog 객체 생성 완료
```

### 메서드 호출 과정

```
dog.makeSound();

┌─────────────────────────────────────┐
│  dog.makeSound() 호출               │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  1. Dog 클래스에서 makeSound() 찾기  │
│     → 오버라이딩된 메서드 발견!      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  2. Dog.makeSound() 실행             │
│     System.out.println(              │
│       name + "이(가) 멍멍 짖습니다." │
│     );                               │
└─────────────────────────────────────┘
```

```
dog.eat();  // 상속받은 메서드

┌─────────────────────────────────────┐
│  dog.eat() 호출                     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  1. Dog 클래스에서 eat() 찾기        │
│     → 없음                          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  2. 부모 클래스 Animal에서 eat() 찾기 │
│     → 발견!                         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  3. Animal.eat() 실행                │
│     System.out.println(              │
│       name + "이(가) 먹습니다."      │
│     );                               │
└─────────────────────────────────────┘
```

---

## 🎯 상속 관계 다이어그램

### 단일 상속 구조

```
                    Object (Java 최상위 클래스)
                          ▲
                          │
                    ┌─────┴─────┐
                    │           │
                  Animal      Vehicle
                    │           │
            ┌───────┼───────┐   │
            │       │       │   │
           Dog     Cat    Bird Car
```

### 실전 예제 구조

```
                    Animal
                      ▲
                      │ extends
          ┌───────────┼───────────┐
          │           │           │
         Dog         Cat        Bird
          │
          │ extends (만약 있다면)
          │
        Puppy
```

---

## 📦 상속과 필드/메서드 접근

### 접근 가능한 범위

```
┌────────────────────────────────────┐
│      Animal (부모 클래스)           │
├────────────────────────────────────┤
│ public String name;                │ ✅ Dog에서 접근 가능
│ protected int age;                 │ ✅ Dog에서 접근 가능
│ private double weight;             │ ❌ Dog에서 접근 불가
├────────────────────────────────────┤
│ public void eat()                  │ ✅ Dog에서 사용 가능
│ protected void sleep()             │ ✅ Dog에서 사용 가능
│ private void internalMethod()      │ ❌ Dog에서 사용 불가
└──────────────┬─────────────────────┘
               │ extends
               │
┌──────────────▼─────────────────────┐
│        Dog (자식 클래스)            │
├────────────────────────────────────┤
│ public String breed;               │ Dog만의 필드
├────────────────────────────────────┤
│ @Override                          │
│ public void makeSound()            │ 부모 메서드 오버라이딩
│                                    │
│ public void bark()                 │ Dog만의 메서드
│                                    │
│ public void useParentFields() {    │
│   System.out.println(name);        │ ✅ 부모의 public 필드
│   System.out.println(age);         │ ✅ 부모의 protected 필드
│   // System.out.println(weight);   │ ❌ private 필드 접근 불가
│   eat();                           │ ✅ 부모의 public 메서드
│   sleep();                         │ ✅ 부모의 protected 메서드
│ }                                  │
└────────────────────────────────────┘
```

---

## 🔄 super 키워드 동작

### super 사용 시나리오

```
┌─────────────────────────────────────┐
│      Animal (부모 클래스)            │
├─────────────────────────────────────┤
│ public Animal(String name, int age) │
│ {                                   │
│   this.name = name;                 │
│   this.age = age;                   │
│ }                                   │
│                                     │
│ public void displayInfo() {         │
│   System.out.println("이름: " + name);│
│   System.out.println("나이: " + age);│
│ }                                   │
└──────────────┬──────────────────────┘
               │
               │ super()
               │
┌──────────────▼──────────────────────┐
│        Dog (자식 클래스)             │
├─────────────────────────────────────┤
│ public Dog(String n, int a, String b)│
│ {                                   │
│   super(n, a);  ←──┐                │
│   this.breed = b;  │                │
│ }                   │                │
│                     │                │
│ @Override          │                │
│ public void displayInfo() {         │
│   super.displayInfo();  ←───────────┤
│   System.out.println("품종: " + breed);│
│ }                                   │
└─────────────────────────────────────┘
        ▲
        │
   super 참조
```

---

## 🎨 다형성 (Polymorphism) 시각화

### 부모 타입으로 자식 객체 참조

```
┌─────────────────────────────────────────┐
│          스택 (Stack)                   │
├─────────────────────────────────────────┤
│                                         │
│  Animal animal1 ────┐                  │
│                     │                  │
│  Animal animal2 ────┼───┐              │
│                     │   │              │
└─────────────────────┼───┼──────────────┘
                      │   │
                      │   │ 참조
                      │   │
┌─────────────────────▼───▼──────────────┐
│          힙 (Heap)                     │
├─────────────────────────────────────────┤
│                                         │
│  ┌───────────────────────┐             │
│  │  Dog 객체             │             │
│  │  (name: "뽀삐")       │             │
│  │  (breed: "골든리트리버")│             │
│  └───────────────────────┘             │
│            ▲                           │
│            │ animal1                   │
│            │                           │
│  ┌─────────┴───────────┐               │
│  │  Cat 객체            │               │
│  │  (name: "나비")      │               │
│  │  (color: "검은색")   │               │
│  └──────────────────────┘               │
│            ▲                           │
│            │ animal2                   │
│                                         │
└─────────────────────────────────────────┘

코드:
Animal animal1 = new Dog("뽀삐", 3, "골든리트리버");
Animal animal2 = new Cat("나비", 2, "검은색");

animal1.makeSound();  // "뽀삐이(가) 멍멍 짖습니다." (Dog의 메서드)
animal2.makeSound();  // "나비이(가) 야옹 웁니다." (Cat의 메서드)
```

---

## 📊 상속 관계 비교

### 상속 전 vs 상속 후

#### ❌ 상속 없이 (코드 중복)

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│     Dog      │  │     Cat      │  │    Bird      │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ name         │  │ name         │  │ name         │
│ age          │  │ age          │  │ age          │
│ breed        │  │ color        │  │ canFly       │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ eat()        │  │ eat()        │  │ eat()        │
│ sleep()      │  │ sleep()      │  │ sleep()      │
│ makeSound()  │  │ makeSound()  │  │ makeSound()  │
│ bark()       │  │ scratch()    │  │ fly()        │
└──────────────┘  └──────────────┘  └──────────────┘
    중복!             중복!             중복!
```

#### ✅ 상속 사용 (코드 재사용)

```
              ┌──────────────────┐
              │     Animal       │
              ├──────────────────┤
              │ name             │
              │ age              │
              ├──────────────────┤
              │ eat()            │
              │ sleep()          │
              │ makeSound()      │
              └────────┬─────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
    ┌────▼────┐   ┌───▼────┐   ┌───▼────┐
    │   Dog   │   │  Cat   │   │  Bird  │
    ├─────────┤   ├────────┤   ├────────┤
    │ breed   │   │ color  │   │ canFly │
    ├─────────┤   ├────────┤   ├────────┤
    │ bark()  │   │scratch │   │ fly()  │
    │         │   │  ()    │   │        │
    │ @Override   │ @Override   │ @Override
    │ makeSound   │ makeSound   │ makeSound
    └─────────┘   └────────┘   └────────┘
    
    공통 코드는 부모에서 한 번만!
```

---

## 🎯 메서드 오버라이딩 시각화

### 오버라이딩 과정

```
Animal 클래스:
┌────────────────────────────────┐
│ public void makeSound() {      │
│   System.out.println(          │
│     name + "이(가) 소리를 냅니다."│
│   );                           │
│ }                              │
└────────────────────────────────┘
           ▲
           │ 상속
           │
    ┌──────┴──────┐
    │             │
┌───▼────┐   ┌───▼────┐
│  Dog   │   │  Cat   │
├────────┤   ├────────┤
│@Override│  │@Override│
│public   │  │public   │
│void     │  │void     │
│makeSound│  │makeSound│
│() {     │  │() {     │
│  멍멍!  │  │  야옹!  │
│}        │  │}        │
└─────────┘   └────────┘

호출 시:
dog.makeSound()  → Dog의 makeSound() 실행 (오버라이딩)
cat.makeSound()  → Cat의 makeSound() 실행 (오버라이딩)
```

---

## 💡 핵심 정리

### 상속의 시각적 이해

1. **계층 구조**: 부모 → 자식 (위에서 아래로)
2. **코드 상속**: 자식이 부모의 코드를 물려받음
3. **메모리 구조**: 자식 객체에 부모 부분과 자식 부분이 모두 포함
4. **메서드 호출**: 자식에서 먼저 찾고, 없으면 부모에서 찾음
5. **오버라이딩**: 자식에서 부모 메서드를 재정의

### 상속의 흐름

```
부모 클래스 정의
      ↓
자식 클래스가 extends
      ↓
부모의 필드와 메서드 상속
      ↓
자식만의 필드/메서드 추가
      ↓
필요시 부모 메서드 오버라이딩
      ↓
자식 클래스 사용
```



