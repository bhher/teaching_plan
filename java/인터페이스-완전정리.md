# 자바 인터페이스 (Interface) 완전 정리

## 📌 학습 목표

- 인터페이스의 개념과 특징을 이해한다
- 인터페이스 정의와 구현 방법을 이해한다
- 다중 인터페이스 구현을 이해한다
- 인터페이스 상속을 이해한다
- 인터페이스를 활용한 다형성을 이해한다

---

## 1️⃣ 인터페이스란?

### 기본 개념

**인터페이스(Interface)**는 클래스가 **반드시 구현해야 하는 메서드**들을 정의하는 "계약서"와 같은 역할을 합니다.

- 클래스가 **무엇을 해야 하는지**를 정의
- 클래스가 **어떻게 해야 하는지**는 구현 클래스에서 정의
- "CAN-DO" 관계 (예: Duck CAN fly, swim, walk)

### 인터페이스의 특징

1. **interface 키워드 사용**: 클래스가 아닌 인터페이스로 선언
2. **추상 메서드 정의**: 모든 메서드는 추상 메서드 (Java 8 이전)
3. **다중 구현 가능**: 하나의 클래스가 여러 인터페이스 구현 가능
4. **상수 포함 가능**: `public static final` 상수만 포함 가능
5. **객체 생성 불가**: 직접 객체 생성 불가

---

## 2️⃣ 인터페이스 기본 문법

### 인터페이스 정의

```java
interface 인터페이스명 {
    // 상수 (public static final 생략 가능)
    타입 상수명 = 값;
    
    // 추상 메서드 (public abstract 생략 가능)
    반환타입 메서드명();
}
```

### 인터페이스 구현

```java
class 클래스명 implements 인터페이스명 {
    // 인터페이스의 모든 메서드 구현 (필수)
    @Override
    public 반환타입 메서드명() {
        // 구현 코드
    }
}
```

### 기본 예제

```java
// 인터페이스 정의
interface Drawable {
    void draw();  // 추상 메서드
}

// 인터페이스 구현
class Circle implements Drawable {
    @Override
    public void draw() {
        System.out.println("원을 그립니다.");
    }
}

class Rectangle implements Drawable {
    @Override
    public void draw() {
        System.out.println("사각형을 그립니다.");
    }
}
```

---

## 3️⃣ 인터페이스의 기본 특징

### 특징 1: 객체 생성 불가

```java
interface Flyable {
    void fly();
}

// ❌ 컴파일 에러: 인터페이스는 객체 생성 불가
Flyable flyable = new Flyable();

// ✅ 올바른 방법: 구현 클래스로 객체 생성
Flyable flyable = new Bird();  // Bird가 Flyable을 구현
```

### 특징 2: 모든 메서드는 추상 메서드

```java
interface Flyable {
    // public abstract는 생략 가능
    void fly();  // = public abstract void fly();
    
    // ❌ 일반 메서드 불가 (Java 8 이전)
    // void method() { }  // 컴파일 에러
}
```

### 특징 3: 상수만 포함 가능

```java
interface Constants {
    // public static final 생략 가능
    int MAX_SPEED = 100;  // = public static final int MAX_SPEED = 100;
    String COMPANY = "우리 회사";
    
    // ❌ 변수 불가
    // int count;  // 컴파일 에러 (초기화 필요)
}
```

### 특징 4: 다중 구현 가능

```java
interface Flyable {
    void fly();
}

interface Swimmable {
    void swim();
}

// ✅ 여러 인터페이스 구현
class Duck implements Flyable, Swimmable {
    @Override
    public void fly() {
        System.out.println("날아갑니다.");
    }
    
    @Override
    public void swim() {
        System.out.println("수영합니다.");
    }
}
```

---

## 4️⃣ 인터페이스 예제

### 예제 1: 기본 인터페이스

```java
// 인터페이스 정의
interface Animal {
    void makeSound();
    void move();
}

// 구현 클래스 1
class Dog implements Animal {
    String name;
    
    public Dog(String name) {
        this.name = name;
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 멍멍 짖습니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 뛰어다닙니다.");
    }
}

// 구현 클래스 2
class Cat implements Animal {
    String name;
    
    public Cat(String name) {
        this.name = name;
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + "이(가) 야옹 웁니다.");
    }
    
    @Override
    public void move() {
        System.out.println(name + "이(가) 조용히 걸어다닙니다.");
    }
}

// 사용 예제
public class AnimalExample {
    public static void main(String[] args) {
        Animal dog = new Dog("뽀삐");
        Animal cat = new Cat("나비");
        
        dog.makeSound();
        dog.move();
        
        cat.makeSound();
        cat.move();
    }
}
```

### 예제 2: 다중 인터페이스 구현

```java
// 인터페이스 1: 날 수 있는 것
interface Flyable {
    void fly();
    int MAX_SPEED = 100;  // 상수
}

// 인터페이스 2: 수영할 수 있는 것
interface Swimmable {
    void swim();
}

// 인터페이스 3: 걸을 수 있는 것
interface Walkable {
    void walk();
}

// 여러 인터페이스 구현
class Duck implements Flyable, Swimmable, Walkable {
    String name;
    
    public Duck(String name) {
        this.name = name;
    }
    
    @Override
    public void fly() {
        System.out.println(name + "이(가) 날아갑니다.");
    }
    
    @Override
    public void swim() {
        System.out.println(name + "이(가) 수영합니다.");
    }
    
    @Override
    public void walk() {
        System.out.println(name + "이(가) 걷습니다.");
    }
}

// 하나의 인터페이스만 구현
class Fish implements Swimmable {
    String name;
    
    public Fish(String name) {
        this.name = name;
    }
    
    @Override
    public void swim() {
        System.out.println(name + "이(가) 헤엄칩니다.");
    }
}

// 사용 예제
public class MultipleInterfaceExample {
    public static void main(String[] args) {
        Duck duck = new Duck("도날드");
        duck.fly();    // Flyable 메서드
        duck.swim();   // Swimmable 메서드
        duck.walk();   // Walkable 메서드
        
        Fish fish = new Fish("니모");
        fish.swim();   // Swimmable 메서드만 사용 가능
        // fish.fly();  // ❌ 컴파일 에러
    }
}
```

---

## 5️⃣ 인터페이스를 타입으로 사용 (다형성)

### 인터페이스 타입 참조

인터페이스를 타입으로 사용하여 **다형성**을 구현할 수 있습니다.

```java
interface Flyable {
    void fly();
}

class Bird implements Flyable {
    @Override
    public void fly() {
        System.out.println("새가 날아갑니다.");
    }
}

class Airplane implements Flyable {
    @Override
    public void fly() {
        System.out.println("비행기가 날아갑니다.");
    }
}

// 인터페이스를 타입으로 사용
public class PolymorphismExample {
    public static void main(String[] args) {
        // 인터페이스 타입으로 참조
        Flyable flyable1 = new Bird();
        Flyable flyable2 = new Airplane();
        
        flyable1.fly();  // "새가 날아갑니다."
        flyable2.fly();  // "비행기가 날아갑니다."
        
        // 배열로 관리
        Flyable[] flyables = {
            new Bird(),
            new Airplane(),
            new Bird()
        };
        
        for (Flyable flyable : flyables) {
            flyable.fly();  // 각각의 구현에 따라 동작
        }
    }
}
```

### 메서드 매개변수로 인터페이스 사용

```java
interface Playable {
    void play();
}

class Music implements Playable {
    @Override
    public void play() {
        System.out.println("음악을 재생합니다.");
    }
}

class Video implements Playable {
    @Override
    public void play() {
        System.out.println("비디오를 재생합니다.");
    }
}

public class Player {
    // 인터페이스를 매개변수로 받음
    public static void playMedia(Playable playable) {
        playable.play();  // 어떤 구현이든 play() 호출 가능
    }
    
    public static void main(String[] args) {
        playMedia(new Music());   // "음악을 재생합니다."
        playMedia(new Video());   // "비디오를 재생합니다."
    }
}
```

---

## 6️⃣ 인터페이스 상속 (인터페이스 확장)

### 인터페이스가 인터페이스를 상속

인터페이스는 다른 인터페이스를 **상속(extends)**할 수 있습니다.

```java
// 부모 인터페이스
interface Animal {
    void eat();
    void sleep();
}

// 자식 인터페이스 (인터페이스 상속)
interface Pet extends Animal {
    void play();
}

// Pet 인터페이스를 구현하면 Animal의 메서드도 구현해야 함
class Dog implements Pet {
    @Override
    public void eat() {
        System.out.println("먹습니다.");
    }
    
    @Override
    public void sleep() {
        System.out.println("잡니다.");
    }
    
    @Override
    public void play() {
        System.out.println("놉니다.");
    }
}
```

### 다중 인터페이스 상속

```java
interface A {
    void methodA();
}

interface B {
    void methodB();
}

// 인터페이스는 여러 인터페이스 상속 가능
interface C extends A, B {
    void methodC();
}

// C를 구현하면 A, B, C의 모든 메서드 구현 필요
class MyClass implements C {
    @Override
    public void methodA() { }
    
    @Override
    public void methodB() { }
    
    @Override
    public void methodC() { }
}
```

---

## 7️⃣ 인터페이스와 상속 함께 사용

클래스는 **상속(extends)과 인터페이스 구현(implements)을 함께** 사용할 수 있습니다.

```java
// 추상 클래스
abstract class Animal {
    String name;
    
    public Animal(String name) {
        this.name = name;
    }
    
    public void eat() {
        System.out.println(name + "이(가) 먹습니다.");
    }
}

// 인터페이스
interface Flyable {
    void fly();
}

// 상속 + 인터페이스 구현
class Bird extends Animal implements Flyable {
    public Bird(String name) {
        super(name);
    }
    
    @Override
    public void fly() {
        System.out.println(name + "이(가) 날아갑니다.");
    }
}

// 사용
public class CombinedExample {
    public static void main(String[] args) {
        Bird bird = new Bird("짹짹");
        bird.eat();    // Animal의 메서드
        bird.fly();    // Flyable의 메서드
    }
}
```

**주의:** 상속은 하나만 가능하지만, 인터페이스는 여러 개 구현 가능

```java
// ✅ 가능
class Bird extends Animal implements Flyable, Swimmable {
    // ...
}

// ❌ 불가능: 상속은 하나만
// class Bird extends Animal, AnotherClass {
```

---

## 8️⃣ 인터페이스 상수

### 인터페이스의 상수

인터페이스에는 `public static final` 상수만 포함할 수 있습니다.

```java
interface Constants {
    // public static final 생략 가능
    int MAX_VALUE = 100;
    String COMPANY_NAME = "우리 회사";
    double PI = 3.14159;
    
    // 위와 동일 (명시적으로 작성)
    public static final int MIN_VALUE = 0;
}
```

### 상수 사용

```java
interface Flyable {
    int MAX_SPEED = 100;  // 상수
    void fly();
}

class Airplane implements Flyable {
    @Override
    public void fly() {
        System.out.println("날아갑니다. (최대 속도: " + MAX_SPEED + "km/h)");
        // 또는 Flyable.MAX_SPEED로 접근 가능
    }
}

// 사용
public class ConstantsExample {
    public static void main(String[] args) {
        System.out.println(Flyable.MAX_SPEED);  // 100
        
        Airplane airplane = new Airplane();
        airplane.fly();
    }
}
```

---

## 9️⃣ 실전 예제

### 예제 1: 계산기 인터페이스

```java
// 계산기 인터페이스
interface Calculator {
    int add(int a, int b);
    int subtract(int a, int b);
    int multiply(int a, int b);
    int divide(int a, int b);
}

// 기본 계산기 구현
class BasicCalculator implements Calculator {
    @Override
    public int add(int a, int b) {
        return a + b;
    }
    
    @Override
    public int subtract(int a, int b) {
        return a - b;
    }
    
    @Override
    public int multiply(int a, int b) {
        return a * b;
    }
    
    @Override
    public int divide(int a, int b) {
        if (b == 0) {
            System.out.println("0으로 나눌 수 없습니다!");
            return 0;
        }
        return a / b;
    }
}

// 사용
public class CalculatorExample {
    public static void main(String[] args) {
        Calculator calc = new BasicCalculator();
        
        System.out.println("10 + 5 = " + calc.add(10, 5));
        System.out.println("10 - 5 = " + calc.subtract(10, 5));
        System.out.println("10 * 5 = " + calc.multiply(10, 5));
        System.out.println("10 / 5 = " + calc.divide(10, 5));
    }
}
```

### 예제 2: 도형 인터페이스

```java
// 도형 인터페이스
interface Shape {
    double calculateArea();
    double calculatePerimeter();
    void draw();
}

// 원 클래스
class Circle implements Shape {
    double radius;
    
    public Circle(double radius) {
        this.radius = radius;
    }
    
    @Override
    public double calculateArea() {
        return Math.PI * radius * radius;
    }
    
    @Override
    public double calculatePerimeter() {
        return 2 * Math.PI * radius;
    }
    
    @Override
    public void draw() {
        System.out.println("원을 그립니다. (반지름: " + radius + ")");
    }
}

// 직사각형 클래스
class Rectangle implements Shape {
    double width;
    double height;
    
    public Rectangle(double width, double height) {
        this.width = width;
        this.height = height;
    }
    
    @Override
    public double calculateArea() {
        return width * height;
    }
    
    @Override
    public double calculatePerimeter() {
        return 2 * (width + height);
    }
    
    @Override
    public void draw() {
        System.out.println("직사각형을 그립니다. (가로: " + width + ", 세로: " + height + ")");
    }
}

// 사용
public class ShapeExample {
    public static void printShapeInfo(Shape shape) {
        shape.draw();
        System.out.println("넓이: " + shape.calculateArea());
        System.out.println("둘레: " + shape.calculatePerimeter());
        System.out.println();
    }
    
    public static void main(String[] args) {
        Shape circle = new Circle(5.0);
        Shape rectangle = new Rectangle(4.0, 6.0);
        
        printShapeInfo(circle);
        printShapeInfo(rectangle);
    }
}
```

### 예제 3: 데이터베이스 접근 인터페이스

```java
// 데이터베이스 접근 인터페이스
interface Database {
    void connect();
    void disconnect();
    void insert(String data);
    void update(String data);
    void delete(String id);
}

// MySQL 구현
class MySQLDatabase implements Database {
    @Override
    public void connect() {
        System.out.println("MySQL에 연결되었습니다.");
    }
    
    @Override
    public void disconnect() {
        System.out.println("MySQL 연결이 끊어졌습니다.");
    }
    
    @Override
    public void insert(String data) {
        System.out.println("MySQL에 데이터 삽입: " + data);
    }
    
    @Override
    public void update(String data) {
        System.out.println("MySQL 데이터 수정: " + data);
    }
    
    @Override
    public void delete(String id) {
        System.out.println("MySQL에서 데이터 삭제: " + id);
    }
}

// Oracle 구현
class OracleDatabase implements Database {
    @Override
    public void connect() {
        System.out.println("Oracle에 연결되었습니다.");
    }
    
    @Override
    public void disconnect() {
        System.out.println("Oracle 연결이 끊어졌습니다.");
    }
    
    @Override
    public void insert(String data) {
        System.out.println("Oracle에 데이터 삽입: " + data);
    }
    
    @Override
    public void update(String data) {
        System.out.println("Oracle 데이터 수정: " + data);
    }
    
    @Override
    public void delete(String id) {
        System.out.println("Oracle에서 데이터 삭제: " + id);
    }
}

// 사용
public class DatabaseExample {
    public static void useDatabase(Database db) {
        db.connect();
        db.insert("새로운 데이터");
        db.update("수정된 데이터");
        db.delete("123");
        db.disconnect();
    }
    
    public static void main(String[] args) {
        Database mysql = new MySQLDatabase();
        Database oracle = new OracleDatabase();
        
        System.out.println("=== MySQL 사용 ===");
        useDatabase(mysql);
        
        System.out.println("\n=== Oracle 사용 ===");
        useDatabase(oracle);
    }
}
```

---

## 🔟 인터페이스 vs 추상 클래스

### 비교표

| 구분 | 인터페이스 (Interface) | 추상 클래스 (Abstract Class) |
|------|----------------------|---------------------------|
| **키워드** | `interface` | `abstract class` |
| **상속** | `implements` (구현) | `extends` (상속) |
| **다중 상속** | 가능 (다중 구현) | 불가능 (단일 상속) |
| **메서드** | 추상 메서드만 (Java 8 이전) | 추상 + 일반 메서드 모두 가능 |
| **필드** | 상수만 (`public static final`) | 일반 필드 가능 |
| **생성자** | 불가 | 가능 |
| **접근 제어자** | `public`만 (생략 가능) | 모든 접근 제어자 가능 |
| **사용 목적** | 계약 정의, 행동(능력) | 공통 기능 + 강제 구현 |

### 언제 무엇을 사용할까?

#### ✅ 인터페이스를 사용해야 할 때

- **다중 상속 효과**가 필요할 때
- **행동(능력)**을 정의할 때
- **느슨한 결합**이 필요할 때
- **표준 규격**을 정의할 때

```java
// CAN-DO 관계
interface Flyable { void fly(); }
interface Swimmable { void swim(); }

class Duck implements Flyable, Swimmable { }
```

#### ✅ 추상 클래스를 사용해야 할 때

- **공통 기능**을 제공하면서 **일부만 강제 구현**할 때
- **IS-A 관계**가 명확할 때
- **단일 상속**으로 충분할 때

```java
// IS-A 관계
abstract class Animal {
    public void eat() { }      // 공통 기능
    public abstract void makeSound();  // 강제 구현
}
```

---

## 1️⃣1️⃣ 인터페이스 사용 이유

### 1. 다중 상속 효과

```java
// Java는 클래스 다중 상속 불가
// 하지만 인터페이스는 다중 구현 가능

interface Flyable { void fly(); }
interface Swimmable { void swim(); }

class Duck implements Flyable, Swimmable {
    // 여러 능력을 가질 수 있음
}
```

### 2. 느슨한 결합 (Loose Coupling)

```java
// 인터페이스를 사용하면 구현체를 쉽게 교체 가능
interface Database {
    void connect();
}

class Application {
    private Database db;  // 인터페이스 타입 사용
    
    public Application(Database db) {
        this.db = db;
    }
    
    public void run() {
        db.connect();
        // MySQL이든 Oracle이든 상관없이 작동
    }
}
```

### 3. 표준 규격 정의

```java
// 모든 계산기가 반드시 구현해야 하는 메서드 정의
interface Calculator {
    int add(int a, int b);
    int subtract(int a, int b);
}

// 어떤 구현이든 Calculator 인터페이스를 따르면 사용 가능
```

### 4. 다형성 구현

```java
// 인터페이스 타입으로 다양한 구현을 하나로 다룸
Shape[] shapes = {
    new Circle(5),
    new Rectangle(4, 6),
    new Triangle(3, 4, 5)
};

for (Shape shape : shapes) {
    shape.draw();  // 각각의 구현에 따라 동작
}
```

---

## 1️⃣2️⃣ 핵심 정리

### 인터페이스 문법 요약

```java
// 1. 인터페이스 정의
interface 인터페이스명 {
    // 상수
    타입 상수명 = 값;
    
    // 추상 메서드
    반환타입 메서드명();
}

// 2. 인터페이스 구현
class 클래스명 implements 인터페이스명 {
    // 모든 메서드 구현 (필수)
}

// 3. 다중 구현
class 클래스명 implements 인터페이스1, 인터페이스2 {
    // 모든 인터페이스의 메서드 구현
}

// 4. 상속 + 구현
class 클래스명 extends 부모클래스 implements 인터페이스 {
    // ...
}

// 5. 인터페이스 상속
interface 자식인터페이스 extends 부모인터페이스 {
    // ...
}
```

### 인터페이스의 특징 요약

1. ✅ **다중 구현 가능**
2. ✅ **상수 포함 가능** (`public static final`)
3. ✅ **추상 메서드만** (Java 8 이전)
4. ❌ **객체 생성 불가**
5. ❌ **일반 필드 불가**
6. ❌ **생성자 불가**

### 인터페이스 사용 가이드

| 상황 | 사용할 것 |
|------|----------|
| 여러 능력 정의 | 인터페이스 (다중 구현) |
| 행동(능력) 정의 | 인터페이스 |
| 표준 규격 정의 | 인터페이스 |
| 공통 기능 + 강제 구현 | 추상 클래스 |
| IS-A 관계 | 추상 클래스 또는 일반 클래스 |

---

## 📚 관련 자료

- [상속, 추상 클래스, 인터페이스](./상속-추상클래스-인터페이스.md)
- [Java 8장: 객체지향 핵심 개념](./8장_객체지향_핵심_개념.md)
- [예제 코드: InterfaceExample.java](./example8/InterfaceExample.java)


