# 차량 렌트 시스템 - 상세 설명

## 📋 프로그램 개요

도서 대여 시스템과 동일한 구조로 구현된 차량 렌트 관리 시스템입니다. 
ArrayList를 활용하여 차량 정보를 관리하고, 대여/반납 기능을 제공합니다.

---

## 🏗 프로그램 구조

```
차량 렌트 시스템
├── Vehicle.java          (도메인 클래스 - 차량 정보)
├── VehicleManager.java   (관리 클래스 - CRUD 기능)
└── RentSystem.java       (실행 클래스 - 메뉴 시스템)
```

### 클래스 관계도
```
RentSystem (실행)
    │
    └── VehicleManager (관리)
            │
            ├── ArrayList<Vehicle> vehicles
            └── ArrayList<Vehicle> rentedVehicles
                    │
                    └── Vehicle (도메인)
                            ├── String model
                            ├── String manufacturer
                            ├── String location
                            ├── String plateNumber
                            └── boolean available
```

---

## 📦 1. Vehicle 클래스 (도메인 클래스)

### 역할
차량의 정보를 담는 데이터 클래스입니다. 하나의 차량 객체가 하나의 차량 정보를 나타냅니다.

### 주요 구성

#### 필드 (Fields)
```java
private String model;           // 모델명 (예: "소나타")
private String manufacturer;    // 제조사 (예: "현대")
private String location;        // 위치 (예: "A구역")
private String plateNumber;     // 번호판 (예: "12가3456")
private boolean available;      // 대여 가능 여부
```

#### 생성자 (Constructors)

**1. 기본 생성자**
```java
public Vehicle() {
}
```
- 모든 필드를 기본값(null, false)으로 초기화

**2. 매개변수 생성자**
```java
public Vehicle(String model, String manufacturer, String location, String plateNumber) {
    this.model = model;
    this.manufacturer = manufacturer;
    this.location = location;
    this.plateNumber = plateNumber;
    this.available = true;  // 새 차량은 항상 대여 가능
}
```
- 차량 정보를 받아서 초기화
- **중요**: `available = true`로 자동 설정 (새 차량은 대여 가능)

#### 주요 메서드

**1. `void rent()`**
```java
public void rent() {
    this.available = false;
}
```
- 차량을 대여 처리하는 메서드
- `available`을 `false`로 변경하여 대여 불가능 상태로 만듦

**2. `String toString()`**
```java
@Override
public String toString() {
    return "모델 : " + model + ", 제조사 : " + manufacturer + ", 위치 : " + location 
            + ", 번호판 : " + plateNumber + ", 대여여부 : " + (available ? "대여가능" : "대여불가능");
}
```
- 차량 정보를 문자열로 반환
- 삼항 연산자 `(available ? "대여가능" : "대여불가능")` 사용

---

## 🗂 2. VehicleManager 클래스 (관리 클래스)

### 역할
차량 리스트를 관리하고, CRUD(생성, 읽기, 수정, 삭제) 기능을 제공하는 클래스입니다.

### 주요 구성

#### 필드
```java
private ArrayList<Vehicle> vehicles;        // 전체 차량 리스트
private ArrayList<Vehicle> rentedVehicles;  // 대여 중인 차량 리스트
```

#### 생성자
```java
public VehicleManager() {
    vehicles = new ArrayList<>();
    rentedVehicles = new ArrayList<>();
    
    // 더미 데이터 추가
    vehicles.add(new Vehicle("소나타", "현대", "A구역", "12가3456"));
    vehicles.add(new Vehicle("캠리", "토요타", "B구역", "34나5678"));
    vehicles.add(new Vehicle("벤츠 E클래스", "메르세데스", "C구역", "56다7890"));
}
```

### 메서드 상세 설명

#### 1. `void showAvailableVehicles()` - 대여 가능한 차량 보기

```java
public void showAvailableVehicles() {
    System.out.println("대여 가능한 차량 보기");
    for (Vehicle vehicle : vehicles) {
        if (vehicle.isAvailable()) {
            System.out.println(vehicle);
        }
    }
}
```

**동작 원리:**
1. `vehicles` 리스트를 순회
2. 각 차량의 `available`이 `true`인지 확인
3. 대여 가능한 차량만 출력

**향상된 for문 (Enhanced for loop)**
```java
for (Vehicle vehicle : vehicles) {
    // vehicles의 각 요소를 vehicle 변수에 할당하며 반복
}
```

#### 2. `boolean rentVehicle(String modelName)` - 차량 대여하기

```java
public boolean rentVehicle(String modelName) {
    for (Vehicle vehicle : vehicles) {
        if (vehicle.getModel().equalsIgnoreCase(modelName) && vehicle.isAvailable()) {
            vehicle.rent();              // 대여 처리
            rentedVehicles.add(vehicle); // 대여 목록에 추가
            return true;
        }
    }
    return false;
}
```

**동작 원리:**
1. `vehicles` 리스트를 순회
2. 모델명이 일치하고 (`equalsIgnoreCase`) 대여 가능한 차량 찾기
3. 찾으면:
   - `rent()` 호출하여 `available = false`로 변경
   - `rentedVehicles` 리스트에 추가
   - `true` 반환
4. 못 찾으면 `false` 반환

**`equalsIgnoreCase()` 설명:**
```java
"소나타".equalsIgnoreCase("소나타")  // true
"소나타".equalsIgnoreCase("SONATA")  // false (한글은 대소문자 구분 없음)
"SONATA".equalsIgnoreCase("sonata")  // true (영문 대소문자 무시)
```

**중요 포인트:**
- 같은 `Vehicle` 객체를 두 리스트(`vehicles`, `rentedVehicles`)에서 공유
- 객체 참조 개념 이해 필요

#### 3. `void showRentedVehicles()` - 대여 중인 차량 보기

```java
public void showRentedVehicles() {
    System.out.println("대여 중인 차량 보기");
    for (Vehicle vehicle : rentedVehicles) {
        System.out.println(vehicle);
    }
}
```

**동작 원리:**
- `rentedVehicles` 리스트의 모든 차량 출력

#### 4. `void addVehicle(...)` - 차량 추가하기

```java
public void addVehicle(String model, String manufacturer, String location, String plateNumber) {
    vehicles.add(new Vehicle(model, manufacturer, location, plateNumber));
}
```

**동작 원리:**
1. 매개변수로 받은 정보로 새로운 `Vehicle` 객체 생성
2. `vehicles` 리스트에 추가
3. 생성 시 `available = true`로 자동 설정됨

#### 5. `void removeVehicle(String modelName)` - 차량 삭제하기

```java
public void removeVehicle(String modelName) {
    boolean result = false;
    for (Vehicle vehicle : vehicles) {
        if (vehicle.getModel().equalsIgnoreCase(modelName)) {
            if (vehicle.isAvailable()) {  // 대여 중이 아닐 때만 삭제 가능
                vehicles.remove(vehicle);
                result = true;
                break;
            } else {
                result = false;
                break;
            }
        }
    }
    if (result) {
        System.out.println("삭제됨");
    } else {
        System.out.println("삭제 안됨");
    }
}
```

**동작 원리:**
1. 모델명이 일치하는 차량 찾기
2. 찾은 차량이 대여 가능(`available == true`)한지 확인
3. 대여 가능하면 삭제, 대여 중이면 삭제 불가
4. 결과 출력

**주의사항:**
- 향상된 for문에서 직접 `remove()`를 사용하면 `ConcurrentModificationException` 발생 가능
- 안전한 방법: Iterator 사용 또는 인덱스 기반 반복문 사용

**개선된 코드 (Iterator 사용):**
```java
Iterator<Vehicle> iterator = vehicles.iterator();
while (iterator.hasNext()) {
    Vehicle vehicle = iterator.next();
    if (vehicle.getModel().equalsIgnoreCase(modelName)) {
        if (vehicle.isAvailable()) {
            iterator.remove();  // 안전한 삭제
            result = true;
            break;
        }
    }
}
```

#### 6. `void updateVehicle(String modelName)` - 차량 정보 수정하기

```java
public void updateVehicle(String modelName) {
    int index = -1;
    Vehicle targetVehicle = null;
    Scanner sc = new Scanner(System.in);

    // 수정할 차량 찾기
    for (int i = 0; i < vehicles.size(); i++) {
        if (vehicles.get(i).getModel().equalsIgnoreCase(modelName)) {
            index = i;
            targetVehicle = vehicles.get(i);
            break;
        }
    }

    if (index != -1) {
        // 수정 메뉴
        System.out.print("무엇을 수정할까요?\n 1.모델명 \t 2.제조사 \t 3.위치 \t 4.번호판 \n >> ");
        int menu = sc.nextInt();
        sc.nextLine();

        switch (menu) {
            case 1:
                System.out.print("수정할 모델명: ");
                targetVehicle.setModel(sc.nextLine());
                vehicles.set(index, targetVehicle);
                break;
            // ... 다른 케이스들
        }
    }
}
```

**동작 원리:**
1. 모델명으로 차량 찾기 (인덱스 저장)
2. 찾은 차량의 참조를 `targetVehicle`에 저장
3. 수정할 항목 선택
4. 해당 필드만 setter로 수정
5. `vehicles.set(index, targetVehicle)`로 리스트에 반영

**중요 포인트:**
- `targetVehicle`은 `vehicles`의 실제 객체 참조
- setter로 수정하면 원본 객체가 변경됨
- `vehicles.set(index, targetVehicle)`는 사실 불필요할 수 있지만, 명시적으로 반영

#### 7. `void showVehicleInfo(String modelName)` - 차량 정보 조회

```java
public void showVehicleInfo(String modelName) {
    for (Vehicle vehicle : vehicles) {
        if (vehicle.getModel().equalsIgnoreCase(modelName)) {
            System.out.println(vehicle.toString());
        }
    }
}
```

**동작 원리:**
- 모델명이 일치하는 차량의 정보 출력

---

## 🖥 3. RentSystem 클래스 (실행 클래스)

### 역할
사용자와 상호작용하는 메인 프로그램입니다. 메뉴를 제공하고 사용자 입력을 처리합니다.

### 주요 구성

#### 메인 루프
```java
boolean flag = true;
while (flag) {
    // 메뉴 출력
    // 사용자 입력 받기
    // switch-case로 메뉴 처리
    // 8번 선택 시 flag = false로 종료
}
```

#### Scanner 사용 주의사항

**문제 상황:**
```java
int choice = sc.nextInt();  // 숫자만 읽음, 엔터는 버퍼에 남음
String name = sc.nextLine(); // 버퍼에 남은 엔터를 읽어서 빈 문자열이 됨
```

**해결 방법:**
```java
int choice = sc.nextInt();
sc.nextLine(); // 버퍼 초기화 (엔터 제거)
String name = sc.nextLine(); // 정상적으로 입력 받음
```

### 메뉴별 처리

#### 메뉴 1: 대여 가능한 차량 보기
```java
manager.showAvailableVehicles();
```
- 단순히 메서드 호출

#### 메뉴 2: 차량 대여하기
```java
String modelName = sc.nextLine();
if (manager.rentVehicle(modelName)) {
    System.out.println("차량이 성공적으로 대여되었습니다.");
} else {
    System.out.println("차량이 존재하지 않거나 대여 불가능합니다.");
}
```
- 반환값(`true/false`)에 따라 성공/실패 메시지 출력

#### 메뉴 5, 6, 7: 빈 문자열 체크
```java
String dname = sc.nextLine();
if (dname.equals("")) {
    System.out.println("다시 입력 해주세요");
    dname = sc.nextLine();
}
```
- 빈 문자열 입력 방지

---

## 🔍 핵심 개념 설명

### 1. ArrayList의 동작 원리

**추가 (add)**
```java
vehicles.add(new Vehicle(...));  // 리스트 끝에 추가
```

**조회 (get)**
```java
Vehicle v = vehicles.get(0);  // 인덱스 0번 요소 가져오기
```

**삭제 (remove)**
```java
vehicles.remove(vehicle);  // 객체로 삭제
vehicles.remove(0);        // 인덱스로 삭제
```

**수정 (set)**
```java
vehicles.set(index, newVehicle);  // 해당 인덱스의 요소 교체
```

**크기 (size)**
```java
int count = vehicles.size();  // 리스트 크기 반환
```

### 2. 객체 참조 (Reference)

```java
ArrayList<Vehicle> vehicles = new ArrayList<>();
ArrayList<Vehicle> rentedVehicles = new ArrayList<>();

Vehicle car = new Vehicle("소나타", ...);
vehicles.add(car);          // vehicles 리스트에 추가
rentedVehicles.add(car);    // rentedVehicles 리스트에도 같은 객체 추가

// car 객체는 두 리스트에서 공유됨
car.setAvailable(false);    // 두 리스트의 같은 객체가 변경됨
```

**시각적 표현:**
```
vehicles          rentedVehicles
    │                   │
    └─── Vehicle ───────┘
         (소나타)
```

### 3. 상태 관리 패턴

```java
// 상태 확인
if (vehicle.isAvailable()) {
    // 대여 가능한 경우
}

// 상태 변경
vehicle.rent();  // available = false
```

### 4. 문자열 비교

```java
// ❌ 잘못된 방법
if (str1 == str2) { ... }  // 주소 비교 (의도한 대로 동작 안 함)

// ✅ 올바른 방법
if (str1.equals(str2)) { ... }           // 대소문자 구분
if (str1.equalsIgnoreCase(str2)) { ... } // 대소문자 무시
```

### 5. 향상된 for문 vs 일반 for문

**향상된 for문 (Enhanced for loop)**
```java
for (Vehicle vehicle : vehicles) {
    // vehicle은 vehicles의 각 요소
    // 인덱스 접근 불가
    // 삭제 시 주의 필요
}
```

**일반 for문 (Index-based)**
```java
for (int i = 0; i < vehicles.size(); i++) {
    Vehicle vehicle = vehicles.get(i);
    // 인덱스 접근 가능 (i)
    // 삭제 시 인덱스 조정 필요
}
```

---

## 🎯 학습 포인트 정리

### 객체 지향 프로그래밍
- [ ] 클래스와 객체의 개념 이해
- [ ] 캡슐화 (private 필드, public 메서드)
- [ ] 생성자 오버로딩
- [ ] Getter/Setter 패턴
- [ ] `toString()` 메서드 오버라이딩

### ArrayList 활용
- [ ] ArrayList 생성 및 초기화
- [ ] 요소 추가 (`add()`)
- [ ] 요소 조회 (`get()`)
- [ ] 요소 수정 (`set()`)
- [ ] 요소 삭제 (`remove()`)
- [ ] 리스트 순회 (for문, 향상된 for문)

### 문자열 처리
- [ ] `equals()` vs `==`
- [ ] `equalsIgnoreCase()` 사용
- [ ] 빈 문자열 체크

### 제어문
- [ ] while 루프 (메뉴 시스템)
- [ ] switch-case문
- [ ] if-else문
- [ ] break 문

### 예외 처리 기초
- [ ] 빈 문자열 체크
- [ ] Scanner 버퍼 처리 (`nextLine()`)

---

## 💡 개선 가능한 사항

### 1. 반납 기능 추가
```java
public boolean returnVehicle(String modelName) {
    for (Vehicle vehicle : rentedVehicles) {
        if (vehicle.getModel().equalsIgnoreCase(modelName)) {
            vehicle.setAvailable(true);
            rentedVehicles.remove(vehicle);
            return true;
        }
    }
    return false;
}
```

### 2. 예외 처리 강화
```java
try {
    int choice = sc.nextInt();
} catch (InputMismatchException e) {
    System.out.println("숫자를 입력하세요.");
    sc.nextLine(); // 버퍼 초기화
}
```

### 3. 중복 체크
```java
public boolean addVehicle(...) {
    // 번호판 중복 체크
    for (Vehicle v : vehicles) {
        if (v.getPlateNumber().equals(plateNumber)) {
            return false; // 중복
        }
    }
    vehicles.add(new Vehicle(...));
    return true;
}
```

### 4. 파일 저장/불러오기
- 차량 정보를 파일에 저장하고 프로그램 시작 시 불러오기

---

## 📚 관련 개념

- **ArrayList**: [배열-vs-ArrayList-비교.md](./배열-vs-ArrayList-비교.md)
- **생성자/Getter/Setter**: [생성자-Getter-Setter-오버로딩-오버라이딩-toString-완전정리.md](./생성자-Getter-Setter-오버로딩-오버라이딩-toString-완전정리.md)
- **예외 처리**: [10장_예외_처리.md](./10장_예외_처리.md)

