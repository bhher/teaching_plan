# 자바 작업형 평가 문제 정답

---

## 문제 1: 학생 관리 시스템 - 메뉴 기반 정답

### 📁 Student.java

```java
public class Student {
    String name;   // 학생 이름
    int age;       // 학생 나이
    
    // 생성자
    public Student(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // toString() 메서드: 객체를 문자열로 변환
    @Override
    public String toString() {
        return "Student{name='" + name + "', age=" + age + "}";
    }
}
```

---

### 📁 StudentApp.java

```java
import java.util.ArrayList;
import java.util.Scanner;

public class StudentApp {
    public static void main(String[] args) {

        ArrayList<Student> list = new ArrayList<>();
        Scanner sc = new Scanner(System.in);
        boolean run = true;

        while (run) {
            System.out.println("------------------------");
            System.out.println("1. 학생 등록");
            System.out.println("2. 학생 검색");
            System.out.println("3. 학생 수정");
            System.out.println("4. 학생 삭제");
            System.out.println("5. 전체 출력");
            System.out.println("6. 종료");
            System.out.print("선택 > ");

            int menu;
            try {
                menu = Integer.parseInt(sc.nextLine());
            } catch (Exception e) {
                System.out.println("숫자만 입력하세요");
                continue;
            }

            switch (menu) {
                case 1: // 등록
                    System.out.print("이름 입력 : ");
                    String name = sc.nextLine();

                    int age;
                    while (true) {
                        try {
                            System.out.print("나이 입력 : ");
                            age = Integer.parseInt(sc.nextLine());
                            break;
                        } catch (Exception e) {
                            System.out.println("나이는 숫자로 입력하세요");
                        }
                    }

                    list.add(new Student(name, age));
                    System.out.println("학생 등록 완료");
                    break;

                case 2: // 검색
                    System.out.print("검색할 이름 : ");
                    name = sc.nextLine();

                    Student s = findStudent(list, name);
                    if (s != null) {
                        System.out.println(s);
                    } else {
                        System.out.println("학생이 없습니다");
                    }
                    break;

                case 3: // 수정
                    System.out.print("수정할 이름 : ");
                    name = sc.nextLine();

                    int newAge;
                    while (true) {
                        try {
                            System.out.print("새 나이 입력 : ");
                            newAge = Integer.parseInt(sc.nextLine());
                            break;
                        } catch (Exception e) {
                            System.out.println("나이는 숫자로 입력하세요");
                        }
                    }

                    if (updateStudent(list, name, newAge)) {
                        System.out.println("수정 완료");
                    } else {
                        System.out.println("해당 학생이 없습니다");
                    }
                    break;

                case 4: // 삭제
                    System.out.print("삭제할 이름 : ");
                    name = sc.nextLine();

                    if (deleteStudent(list, name)) {
                        System.out.println("삭제 완료");
                    } else {
                        System.out.println("삭제할 학생이 없습니다");
                    }
                    break;

                case 5: // 전체 출력
                    if (list.isEmpty()) {
                        System.out.println("등록된 학생이 없습니다");
                    } else {
                        for (Student student : list) {
                            System.out.println(student);
                        }
                    }
                    break;

                case 6: // 종료
                    run = false;
                    System.out.println("프로그램 종료");
                    break;

                default:
                    System.out.println("1~6번 중에 선택하세요");
            }
        }
        sc.close();
    }

    // 학생 검색
    static Student findStudent(ArrayList<Student> list, String name) {
        for (Student s : list) {
            if (s.name.equals(name)) {
                return s;
            }
        }
        return null;
    }

    // 학생 수정
    static boolean updateStudent(ArrayList<Student> list, String name, int newAge) {
        for (Student s : list) {
            if (s.name.equals(name)) {
                s.age = newAge;
                return true;
            }
        }
        return false;
    }

    // 학생 삭제
    static boolean deleteStudent(ArrayList<Student> list, String name) {
        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).name.equals(name)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }
}
```

---

## 문제 2: Product 클래스 작성 정답

### 📁 Product.java

```java
public class Product {
    String name;     // 상품 이름
    int price;       // 상품 가격
    int quantity;    // 상품 수량
    
    // 기본 생성자
    public Product() {
    }
    
    // 매개변수 생성자
    public Product(String name, int price, int quantity) {
        this.name = name;
        this.price = price;
        this.quantity = quantity;
    }
    
    // toString() 메서드: 상품 정보를 문자열로 반환
    @Override
    public String toString() {
        return "상품명: " + name + ", 가격: " + price + "원, 수량: " + quantity + "개";
    }
    
    // 총 가격 계산 메서드
    public int getTotalPrice() {
        return price * quantity;
    }
}
```

---

### 📁 Problem2.java (실행 파일)

```java
public class Problem2 {
    public static void main(String[] args) {
        Product p1 = new Product("노트북", 1000000, 2);
        Product p2 = new Product("마우스", 15000, 5);
        
        System.out.println(p1);
        System.out.println("총 가격: " + p1.getTotalPrice() + "원");
        System.out.println(p2);
        System.out.println("총 가격: " + p2.getTotalPrice() + "원");
    }
}
```

---

### 🔍 코드 설명

#### Product 클래스 구조

**필드:**
- `name` (String): 상품 이름 저장
- `price` (int): 상품 가격 저장
- `quantity` (int): 상품 수량 저장

**생성자:**
- 기본 생성자: 매개변수 없이 객체 생성 가능
- 매개변수 생성자: 이름, 가격, 수량을 받아 객체 초기화

**메서드:**
- `toString()`: 객체를 문자열로 표현 (자동 호출)
- `getTotalPrice()`: 가격과 수량을 곱한 총 가격 반환

---

### 📐 메서드별 상세 설명

#### 📝 toString() 메서드

```java
@Override
public String toString() {
    return "상품명: " + name + ", 가격: " + price + "원, 수량: " + quantity + "개";
}
```

**동작 원리:**
1. `@Override` 어노테이션: 부모 클래스(Object)의 toString() 메서드를 재정의
2. 문자열 연결: `+` 연산자로 문자열과 변수값을 연결
3. 자동 호출: `System.out.println(p1)` 실행 시 자동으로 toString() 호출

---

#### 💰 getTotalPrice() 메서드

```java
public int getTotalPrice() {
    return price * quantity;
}
```

**동작 원리:**
1. 가격과 수량을 곱하여 총 가격 계산
2. int 타입으로 반환

**계산 예시:**
```
노트북: 가격 1000000원 × 수량 2개 = 2000000원
마우스: 가격 15000원 × 수량 5개 = 75000원
```

---

## 문제 3: ArrayList를 이용한 숫자 관리 정답

### 📁 NumberManager.java

```java
import java.util.ArrayList;

public class NumberManager {
    
    // 숫자 추가
    public static void addNumber(ArrayList<Integer> list, int num) {
        list.add(num);
    }
    
    // 최댓값 찾기
    public static int findMax(ArrayList<Integer> list) {
        if (list.isEmpty()) {
            return -1;
        }
        
        int max = list.get(0);
        for (int i = 1; i < list.size(); i++) {
            if (list.get(i) > max) {
                max = list.get(i);
            }
        }
        return max;
    }
    
    // 최솟값 찾기
    public static int findMin(ArrayList<Integer> list) {
        if (list.isEmpty()) {
            return -1;
        }
        
        int min = list.get(0);
        for (int i = 1; i < list.size(); i++) {
            if (list.get(i) < min) {
                min = list.get(i);
            }
        }
        return min;
    }
    
    // 합계 계산
    public static int getSum(ArrayList<Integer> list) {
        int sum = 0;
        for (int num : list) {
            sum += num;
        }
        return sum;
    }
}
```

---

### 📁 Problem3.java (실행 파일)

```java
import java.util.ArrayList;

public class Problem3 {
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();
        
        NumberManager.addNumber(list, 10);
        NumberManager.addNumber(list, 5);
        NumberManager.addNumber(list, 20);
        NumberManager.addNumber(list, 8);
        NumberManager.addNumber(list, 15);
        
        System.out.println("최댓값: " + NumberManager.findMax(list));
        System.out.println("최솟값: " + NumberManager.findMin(list));
        System.out.println("합계: " + NumberManager.getSum(list));
    }
}
```

---

### 🔍 코드 설명

#### NumberManager 클래스 구조

**메서드:**
- `addNumber()`: 숫자를 리스트에 추가
- `findMax()`: 최댓값 찾기
- `findMin()`: 최솟값 찾기
- `getSum()`: 합계 계산

**특징:**
- 모든 메서드가 `static`으로 선언됨 (객체 생성 없이 호출 가능)
- ArrayList<Integer>를 매개변수로 받아 처리

---

### 📐 메서드별 상세 설명

#### ➕ addNumber() - 숫자 추가

```java
public static void addNumber(ArrayList<Integer> list, int num) {
    list.add(num);
}
```

**동작 순서:**
1. `list.add(num)`로 ArrayList에 숫자 추가
2. 리스트 크기가 자동으로 증가

---

#### 📊 findMax() - 최댓값 찾기

```java
public static int findMax(ArrayList<Integer> list) {
    if (list.isEmpty()) {              // 리스트가 비어있으면
        return -1;                     // -1 반환
    }
    
    int max = list.get(0);             // 첫 번째 값을 최댓값으로 초기화
    for (int i = 1; i < list.size(); i++) {  // 두 번째부터 순회
        if (list.get(i) > max) {       // 현재 값이 최댓값보다 크면
            max = list.get(i);         // 최댓값 갱신
        }
    }
    return max;                        // 최댓값 반환
}
```

**동작 순서:**
1. 빈 리스트 체크 (`isEmpty()`)
2. 첫 번째 값을 최댓값으로 초기화
3. 두 번째 값부터 마지막까지 순회하며 비교
4. 더 큰 값을 찾으면 최댓값 갱신
5. 최댓값 반환

---

#### 📉 findMin() - 최솟값 찾기

```java
public static int findMin(ArrayList<Integer> list) {
    if (list.isEmpty()) {              // 리스트가 비어있으면
        return -1;                     // -1 반환
    }
    
    int min = list.get(0);             // 첫 번째 값을 최솟값으로 초기화
    for (int i = 1; i < list.size(); i++) {  // 두 번째부터 순회
        if (list.get(i) < min) {       // 현재 값이 최솟값보다 작으면
            min = list.get(i);         // 최솟값 갱신
        }
    }
    return min;                        // 최솟값 반환
}
```

**동작 순서:**
1. 빈 리스트 체크
2. 첫 번째 값을 최솟값으로 초기화
3. 두 번째 값부터 마지막까지 순회하며 비교
4. 더 작은 값을 찾으면 최솟값 갱신
5. 최솟값 반환

---

#### ➕ getSum() - 합계 계산

```java
public static int getSum(ArrayList<Integer> list) {
    int sum = 0;                       // 합계 변수 초기화
    for (int num : list) {             // 향상된 for문으로 모든 요소 순회
        sum += num;                     // 각 숫자를 합계에 누적
    }
    return sum;                        // 합계 반환
}
```

**동작 순서:**
1. 합계를 저장할 변수 초기화 (0)
2. 모든 숫자를 순회하며 합산
3. 합계 반환

**계산 예시:**
```
리스트: [10, 5, 20, 8, 15]
합계: 10 + 5 + 20 + 8 + 15 = 58
```

---

## 문제 4: 문자열 처리 유틸리티 정답

### 📁 StringUtil.java

```java
public class StringUtil {
    
    // 문자 개수 세기
    public static int countChar(String text, char ch) {
        if (text == null) {
            return 0;
        }
        
        int count = 0;
        for (int i = 0; i < text.length(); i++) {
            if (text.charAt(i) == ch) {
                count++;
            }
        }
        return count;
    }
    
    // 문자열 뒤집기
    public static String reverseString(String text) {
        if (text == null) {
            return null;
        }
        
        StringBuilder sb = new StringBuilder();
        for (int i = text.length() - 1; i >= 0; i--) {
            sb.append(text.charAt(i));
        }
        return sb.toString();
    }
    
    // 대문자 변환
    public static String toUpperCase(String text) {
        if (text == null) {
            return null;
        }
        
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < text.length(); i++) {
            char ch = text.charAt(i);
            if (ch >= 'a' && ch <= 'z') {
                sb.append((char)(ch - 32));  // 소문자를 대문자로 변환
            } else {
                sb.append(ch);
            }
        }
        return sb.toString();
    }
    
    // 공백 제거
    public static String removeSpaces(String text) {
        if (text == null) {
            return null;
        }
        
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < text.length(); i++) {
            char ch = text.charAt(i);
            if (ch != ' ') {
                sb.append(ch);
            }
        }
        return sb.toString();
    }
}
```

---

### 📁 Problem4.java (실행 파일)

```java
public class Problem4 {
    public static void main(String[] args) {
        // 1. 문자 개수 세기
        System.out.println("'l' 개수: " + StringUtil.countChar("Hello", 'l'));
        
        // 2. 문자열 뒤집기
        System.out.println("뒤집기: " + StringUtil.reverseString("자바"));
        
        // 3. 회문 확인
        System.out.println("level 회문: " + StringUtil.isPalindrome("level"));
        System.out.println("hello 회문: " + StringUtil.isPalindrome("hello"));
        
        // 4. 공백 제거
        System.out.println("공백 제거: " + StringUtil.removeSpaces("Hello World"));
    }
}
```

---

### 🔍 코드 설명

#### StringUtil 클래스 구조

**메서드:**
- `countChar()`: 문자열에서 특정 문자 개수 세기
- `reverseString()`: 문자열 뒤집기
- `toUpperCase()`: 문자열을 대문자로 변환
- `removeSpaces()`: 공백 제거

**특징:**
- 모든 메서드가 `static`으로 선언됨
- null 체크 포함

---

### 📐 메서드별 상세 설명

#### 🔢 countChar() - 문자 개수 세기

```java
public static int countChar(String text, char ch) {
    if (text == null) {
        return 0;
    }
    
    int count = 0;
    for (int i = 0; i < text.length(); i++) {
        if (text.charAt(i) == ch) {
            count++;
        }
    }
    return count;
}
```

**동작 순서:**
1. null 체크
2. 카운터 변수 초기화 (0)
3. 문자열의 모든 문자를 순회
4. 각 문자가 찾는 문자와 같으면 카운터 증가
5. 카운터 반환

**예시:**
```java
countChar("Hello", 'l')  // → 2 ('l'이 2번 나타남)
```

---

#### 🔄 reverseString() - 문자열 뒤집기

```java
public static String reverseString(String text) {
    if (text == null) {
        return null;
    }
    
    StringBuilder sb = new StringBuilder();
    for (int i = text.length() - 1; i >= 0; i--) {
        sb.append(text.charAt(i));
    }
    return sb.toString();
}
```

**동작 순서:**
1. null 체크
2. StringBuilder 객체 생성
3. 문자열의 마지막 문자부터 첫 번째 문자까지 역순으로 추가
4. StringBuilder를 String으로 변환하여 반환

**예시:**
```java
reverseString("자바")  // → "바자"
reverseString("Hello")  // → "olleH"
```

---

#### 🔤 toUpperCase() - 대문자 변환

```java
public static String toUpperCase(String text) {
    if (text == null) {
        return null;
    }
    
    StringBuilder sb = new StringBuilder();
    for (int i = 0; i < text.length(); i++) {
        char ch = text.charAt(i);
        if (ch >= 'a' && ch <= 'z') {
            sb.append((char)(ch - 32));  // 소문자를 대문자로 변환
        } else {
            sb.append(ch);
        }
    }
    return sb.toString();
}
```

**동작 순서:**
1. null 체크
2. StringBuilder 객체 생성
3. 문자열의 모든 문자를 순회
4. 소문자('a'~'z')인 경우 아스키 코드 값에서 32를 빼서 대문자로 변환
5. 대문자나 다른 문자는 그대로 추가
6. StringBuilder를 String으로 변환하여 반환

**아스키 코드:**
- 소문자 'a' = 97, 대문자 'A' = 65 (차이: 32)
- 소문자 'z' = 122, 대문자 'Z' = 90 (차이: 32)

**예시:**
```java
toUpperCase("hello")  // → "HELLO"
toUpperCase("Java")  // → "JAVA"
toUpperCase("Hello World")  // → "HELLO WORLD"
```

---

#### ✂️ removeSpaces() - 공백 제거

```java
public static String removeSpaces(String text) {
    if (text == null) {
        return null;
    }
    
    StringBuilder sb = new StringBuilder();
    for (int i = 0; i < text.length(); i++) {
        char ch = text.charAt(i);
        if (ch != ' ') {              // 공백이 아니면
            sb.append(ch);            // StringBuilder에 추가
        }
    }
    return sb.toString();
}
```

**동작 순서:**
1. null 체크
2. StringBuilder 객체 생성
3. 문자열의 모든 문자를 순회
4. 공백이 아닌 문자만 StringBuilder에 추가
5. StringBuilder를 String으로 변환하여 반환

**예시:**
```java
removeSpaces("Hello World")  // → "HelloWorld"
removeSpaces("  안녕  하세요  ")  // → "안녕하세요"
```

---

## 전체 실행 파일 (통합 테스트용)

### Problem1.java
```java
import java.util.ArrayList;
import java.util.Scanner;

public class Problem1 {
    // Student 클래스와 StudentApp 클래스는 위의 정답 코드 참조
    // (Student.java와 StudentApp.java의 내용을 그대로 사용)
}
```

### Problem2.java
```java
public class Problem2 {
    public static void main(String[] args) {
        Product p1 = new Product("노트북", 1000000, 2);
        Product p2 = new Product("마우스", 15000, 5);
        
        System.out.println(p1);
        System.out.println("총 가격: " + p1.getTotalPrice() + "원");
        System.out.println(p2);
        System.out.println("총 가격: " + p2.getTotalPrice() + "원");
    }
}
```

### Problem3.java
```java
import java.util.ArrayList;

public class Problem3 {
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();
        
        NumberManager.addNumber(list, 10);
        NumberManager.addNumber(list, 5);
        NumberManager.addNumber(list, 20);
        NumberManager.addNumber(list, 8);
        NumberManager.addNumber(list, 15);
        
        System.out.println("최댓값: " + NumberManager.findMax(list));
        System.out.println("최솟값: " + NumberManager.findMin(list));
        System.out.println("합계: " + NumberManager.getSum(list));
    }
}
```

### Problem4.java
```java
public class Problem4 {
    public static void main(String[] args) {
        // 1. 문자 개수 세기
        System.out.println("'l' 개수: " + StringUtil.countChar("Hello", 'l'));
        
        // 2. 문자열 뒤집기
        System.out.println("뒤집기: " + StringUtil.reverseString("자바"));
        
        // 3. 대문자 변환
        System.out.println("대문자: " + StringUtil.toUpperCase("hello"));
        System.out.println("대문자: " + StringUtil.toUpperCase("Java"));
        
        // 4. 공백 제거
        System.out.println("공백 제거: " + StringUtil.removeSpaces("Hello World"));
    }
}
```

---

## 채점 기준 상세

### 문제 1 (20점)
- Student 클래스 작성: 5점
- 메뉴 시스템 구현: 5점
- findStudent() 메서드: 3점
- updateStudent() 메서드: 3점
- deleteStudent() 메서드: 2점
- 예외 처리: 2점

### 문제 2 (20점)
- 필드 선언 (name, price, quantity): 5점
- 기본 생성자: 2점
- 매개변수 생성자: 3점
- toString() 메서드: 5점
- getTotalPrice() 메서드: 5점

### 문제 3 (20점)
- addNumber() 메서드: 5점
- findMax() 메서드 (빈 리스트 체크, 최댓값 찾기): 5점
- findMin() 메서드 (빈 리스트 체크, 최솟값 찾기): 5점
- getSum() 메서드: 5점

### 문제 4 (20점)
- countChar() 메서드 (null 체크, 문자 개수 세기): 5점
- reverseString() 메서드 (null 체크, 문자열 뒤집기): 5점
- toUpperCase() 메서드 (null 체크, 대문자 변환): 5점
- removeSpaces() 메서드 (null 체크, 공백 제거): 5점

---

## 추가 설명

### 문제 1 - 예외 처리
- 메뉴 입력: `try-catch`로 숫자 입력 예외 처리
- 나이 입력: `while(true)` 루프로 잘못된 입력 반복 처리
- `continue`: 예외 발생 시 다시 메뉴로 돌아가기

### 문제 2 - getTotalPrice() 메서드
- 간단한 곱셈 연산: `price * quantity`
- 반환 타입: int

### 문제 3 - 최댓값/최솟값 찾기
- 빈 리스트 체크 필수 (`isEmpty()`)
- 첫 번째 값을 초기값으로 설정
- 순회하며 비교하여 갱신

### 문제 4 - toUpperCase() 메서드
- 아스키 코드를 이용한 소문자→대문자 변환
- 소문자 'a'~'z'는 97~122, 대문자 'A'~'Z'는 65~90
- 소문자에서 32를 빼면 대문자가 됨: `(char)(ch - 32)`
- 이미 대문자이거나 다른 문자는 그대로 유지

### 문제 4 - removeSpaces() 메서드
- `charAt()`으로 각 문자 확인
- 공백(' ')이 아닌 문자만 StringBuilder에 추가
- `replaceAll(" ", "")` 메서드도 사용 가능하지만 직접 구현하는 것이 요구사항
