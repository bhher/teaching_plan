# 결제 시스템 - 업캐스팅 문제

## 목차

1. [기본 문제 (1-5번)](#기본-문제-1-5번)
2. [중급 문제 (6-10번)](#중급-문제-6-10번)
3. [고급 문제 (11-15번)](#고급-문제-11-15번)
4. [정답 및 해설](#정답-및-해설)

---

## 기본 문제 (1-5번)

### 문제 1: 기본 업캐스팅과 다형성

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    double getTotalAmount() {
        return amount + calculateFee();
    }
}

class CreditCard extends PaymentMethod {
    private static final double FEE_RATE = 0.03;
    
    CreditCard(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * FEE_RATE;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod payment = new CreditCard(100000);
        System.out.println("수수료: " + (int)payment.calculateFee());
        System.out.println("총액: " + (int)payment.getTotalAmount());
    }
}
```

---

### 문제 2: 배열과 업캐스팅

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    protected double amount;
    
    PaymentMethod(String methodName, double amount) {
        this.methodName = methodName;
        this.amount = amount;
    }
    
    abstract double calculateFee();
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super("신용카드", amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

class CashPayment extends PaymentMethod {
    CashPayment(double amount) {
        super("현금", amount);
    }
    
    @Override
    double calculateFee() {
        return 0;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[3];
        payments[0] = new CreditCard(100000);
        payments[1] = new CashPayment(50000);
        payments[2] = new CreditCard(200000);
        
        double totalFee = 0;
        for (PaymentMethod p : payments) {
            totalFee += p.calculateFee();
        }
        System.out.println("총 수수료: " + (int)totalFee);
    }
}
```

---

### 문제 3: 메서드 오버라이딩

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    
    PaymentMethod(String methodName) {
        this.methodName = methodName;
    }
    
    void printInfo() {
        System.out.println("결제수단: " + methodName);
    }
    
    abstract void processPayment();
}

class CreditCard extends PaymentMethod {
    CreditCard() {
        super("신용카드");
    }
    
    @Override
    void processPayment() {
        System.out.println("카드 결제 처리");
    }
}

class BankTransfer extends PaymentMethod {
    BankTransfer() {
        super("계좌이체");
    }
    
    @Override
    void processPayment() {
        System.out.println("계좌 이체 처리");
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod p1 = new CreditCard();
        PaymentMethod p2 = new BankTransfer();
        
        p1.printInfo();
        p1.processPayment();
        p2.printInfo();
        p2.processPayment();
    }
}
```

---

### 문제 4: 수수료 계산

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;  // 3%
    }
}

class MobilePayment extends PaymentMethod {
    MobilePayment(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.02;  // 2%
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod p1 = new CreditCard(100000);
        PaymentMethod p2 = new MobilePayment(100000);
        
        System.out.println("카드 수수료: " + (int)p1.calculateFee());
        System.out.println("모바일 수수료: " + (int)p2.calculateFee());
    }
}
```

---

### 문제 5: 빈칸 채우기

**문제:** 다음 코드에서 빈칸에 들어갈 알맞은 코드를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        ______________;  // 빈칸
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod payment = ______________;  // 빈칸
        System.out.println((int)payment.calculateFee());
    }
}
```

**출력 결과:**
```
3000
```

---

## 중급 문제 (6-10번)

### 문제 6: 다형성과 반복문

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    protected double amount;
    
    PaymentMethod(String methodName, double amount) {
        this.methodName = methodName;
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    void printInfo() {
        System.out.println(methodName + ": " + (int)amount + "원, 수수료: " + (int)calculateFee() + "원");
    }
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super("신용카드", amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

class CashPayment extends PaymentMethod {
    CashPayment(double amount) {
        super("현금", amount);
    }
    
    @Override
    double calculateFee() {
        return 0;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[4];
        payments[0] = new CreditCard(100000);
        payments[1] = new CashPayment(50000);
        payments[2] = new CreditCard(200000);
        payments[3] = new CashPayment(30000);
        
        for (PaymentMethod p : payments) {
            p.printInfo();
        }
    }
}
```

---

### 문제 7: 총액 계산

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    double getTotalAmount() {
        return amount + calculateFee();
    }
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

class BankTransfer extends PaymentMethod {
    BankTransfer(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.001;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[3];
        payments[0] = new CreditCard(100000);
        payments[1] = new BankTransfer(50000);
        payments[2] = new CreditCard(200000);
        
        double total = 0;
        for (PaymentMethod p : payments) {
            total += p.getTotalAmount();
        }
        System.out.println("총액: " + (int)total);
    }
}
```

---

### 문제 8: instanceof와 다운캐스팅

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    
    PaymentMethod(String methodName) {
        this.methodName = methodName;
    }
    
    abstract void processPayment();
}

class CreditCard extends PaymentMethod {
    CreditCard() {
        super("신용카드");
    }
    
    @Override
    void processPayment() {
        System.out.println("카드 결제");
    }
    
    void checkLimit() {
        System.out.println("한도 확인");
    }
}

class CashPayment extends PaymentMethod {
    CashPayment() {
        super("현금");
    }
    
    @Override
    void processPayment() {
        System.out.println("현금 결제");
    }
    
    void giveChange() {
        System.out.println("거스름돈");
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[3];
        payments[0] = new CreditCard();
        payments[1] = new CashPayment();
        payments[2] = new CreditCard();
        
        for (PaymentMethod p : payments) {
            p.processPayment();
            if (p instanceof CreditCard) {
                CreditCard cc = (CreditCard) p;
                cc.checkLimit();
            } else if (p instanceof CashPayment) {
                CashPayment cp = (CashPayment) p;
                cp.giveChange();
            }
        }
    }
}
```

---

### 문제 9: 추상 메서드와 오버라이딩

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    void printFee() {
        System.out.println("수수료: " + (int)calculateFee());
    }
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

class MobilePayment extends PaymentMethod {
    MobilePayment(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.02;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod p1 = new CreditCard(100000);
        PaymentMethod p2 = new MobilePayment(100000);
        
        p1.printFee();
        p2.printFee();
    }
}
```

---

### 문제 10: 빈칸 채우기

**문제:** 다음 코드에서 빈칸에 들어갈 알맞은 코드를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[2];
        payments[0] = ______________;  // 빈칸 1
        payments[1] = ______________;  // 빈칸 2
        
        double totalFee = 0;
        for (PaymentMethod p : payments) {
            totalFee += ______________;  // 빈칸 3
        }
        System.out.println("총 수수료: " + (int)totalFee);
    }
}
```

**출력 결과:**
```
총 수수료: 9000
```

---

## 고급 문제 (11-15번)

### 문제 11: 복합 문제 - 결제 통계

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    protected double amount;
    
    PaymentMethod(String methodName, double amount) {
        this.methodName = methodName;
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    double getTotalAmount() {
        return amount + calculateFee();
    }
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super("신용카드", amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

class BankTransfer extends PaymentMethod {
    BankTransfer(double amount) {
        super("계좌이체", amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.001;
    }
}

class CashPayment extends PaymentMethod {
    CashPayment(double amount) {
        super("현금", amount);
    }
    
    @Override
    double calculateFee() {
        return 0;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[5];
        payments[0] = new CreditCard(100000);
        payments[1] = new BankTransfer(50000);
        payments[2] = new CashPayment(30000);
        payments[3] = new CreditCard(200000);
        payments[4] = new BankTransfer(100000);
        
        double totalAmount = 0;
        double totalFee = 0;
        
        for (PaymentMethod p : payments) {
            totalAmount += p.amount;
            totalFee += p.calculateFee();
        }
        
        System.out.println("총 결제금액: " + (int)totalAmount);
        System.out.println("총 수수료: " + (int)totalFee);
        System.out.println("총액: " + (int)(totalAmount + totalFee));
    }
}
```

---

### 문제 12: 메서드 체이닝

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    PaymentMethod printAmount() {
        System.out.println("금액: " + (int)amount);
        return this;
    }
    
    PaymentMethod printFee() {
        System.out.println("수수료: " + (int)calculateFee());
        return this;
    }
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        super(amount);
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod payment = new CreditCard(100000);
        payment.printAmount().printFee();
    }
}
```

---

### 문제 13: 타입별 처리

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    
    PaymentMethod(String methodName) {
        this.methodName = methodName;
    }
    
    abstract void processPayment();
}

class CreditCard extends PaymentMethod {
    CreditCard() {
        super("신용카드");
    }
    
    @Override
    void processPayment() {
        System.out.println("카드 결제 처리");
    }
    
    void checkLimit() {
        System.out.println("카드 한도 확인");
    }
}

class MobilePayment extends PaymentMethod {
    MobilePayment() {
        super("모바일결제");
    }
    
    @Override
    void processPayment() {
        System.out.println("모바일 결제 처리");
    }
    
    void sendNotification() {
        System.out.println("알림 전송");
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[4];
        payments[0] = new CreditCard();
        payments[1] = new MobilePayment();
        payments[2] = new CreditCard();
        payments[3] = new MobilePayment();
        
        int cardCount = 0;
        int mobileCount = 0;
        
        for (PaymentMethod p : payments) {
            if (p instanceof CreditCard) {
                cardCount++;
            } else if (p instanceof MobilePayment) {
                mobileCount++;
            }
        }
        
        System.out.println("신용카드: " + cardCount + "건");
        System.out.println("모바일결제: " + mobileCount + "건");
    }
}
```

---

### 문제 14: 빈칸 채우기 (복합)

**문제:** 다음 코드에서 빈칸에 들어갈 알맞은 코드를 쓰시오.

```java
abstract class PaymentMethod {
    protected double amount;
    
    PaymentMethod(double amount) {
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    void printInfo() {
        System.out.println("금액: " + (int)amount + ", 수수료: " + (int)calculateFee());
    }
}

class CreditCard extends PaymentMethod {
    CreditCard(double amount) {
        ______________;  // 빈칸 1
    }
    
    @Override
    double calculateFee() {
        return ______________;  // 빈칸 2 (3% 수수료)
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod payment = ______________;  // 빈칸 3
        payment.printInfo();
    }
}
```

**출력 결과:**
```
금액: 100000, 수수료: 3000
```

---

### 문제 15: 실전 문제 - 결제 시스템

**문제:** 다음 코드의 출력 결과를 쓰시오.

```java
abstract class PaymentMethod {
    protected String methodName;
    protected double amount;
    
    PaymentMethod(String methodName, double amount) {
        this.methodName = methodName;
        this.amount = amount;
    }
    
    abstract double calculateFee();
    
    abstract void processPayment();
    
    void printPaymentInfo() {
        System.out.println(methodName + " - 금액: " + (int)amount + 
                          ", 수수료: " + (int)calculateFee());
    }
}

class CreditCard extends PaymentMethod {
    private String cardCompany;
    
    CreditCard(double amount, String cardCompany) {
        super("신용카드", amount);
        this.cardCompany = cardCompany;
    }
    
    @Override
    double calculateFee() {
        return amount * 0.03;
    }
    
    @Override
    void processPayment() {
        System.out.println(cardCompany + " 카드로 결제");
    }
    
    void checkLimit() {
        System.out.println("한도 확인");
    }
}

class BankTransfer extends PaymentMethod {
    private String bankName;
    
    BankTransfer(double amount, String bankName) {
        super("계좌이체", amount);
        this.bankName = bankName;
    }
    
    @Override
    double calculateFee() {
        return amount * 0.001;
    }
    
    @Override
    void processPayment() {
        System.out.println(bankName + " 계좌로 이체");
    }
}

public class Main {
    public static void main(String[] args) {
        PaymentMethod[] payments = new PaymentMethod[3];
        payments[0] = new CreditCard(100000, "삼성카드");
        payments[1] = new BankTransfer(50000, "국민은행");
        payments[2] = new CreditCard(200000, "신한카드");
        
        System.out.println("=== 결제 정보 ===");
        for (PaymentMethod p : payments) {
            p.printPaymentInfo();
        }
        
        System.out.println("\n=== 결제 처리 ===");
        for (PaymentMethod p : payments) {
            p.processPayment();
            if (p instanceof CreditCard) {
                CreditCard cc = (CreditCard) p;
                cc.checkLimit();
            }
        }
    }
}
```

---

## 정답 및 해설

### 문제 1 정답

**답:**
```
수수료: 3000
총액: 103000
```

**해설:**
- `PaymentMethod payment = new CreditCard(100000);`: 업캐스팅
- `payment.calculateFee()`: CreditCard의 `calculateFee()` 호출 → 100000 × 0.03 = 3000
- `payment.getTotalAmount()`: 100000 + 3000 = 103000

---

### 문제 2 정답

**답:** `총 수수료: 9000`

**해설:**
- payments[0]: CreditCard(100000) → 수수료 3000
- payments[1]: CashPayment(50000) → 수수료 0
- payments[2]: CreditCard(200000) → 수수료 6000
- 총 수수료: 3000 + 0 + 6000 = 9000

---

### 문제 3 정답

**답:**
```
결제수단: 신용카드
카드 결제 처리
결제수단: 계좌이체
계좌 이체 처리
```

**해설:**
- `p1.printInfo()`: 부모 클래스의 메서드 호출
- `p1.processPayment()`: 오버라이딩된 메서드 호출 (동적 바인딩)
- `p2`도 동일한 방식으로 동작

---

### 문제 4 정답

**답:**
```
카드 수수료: 3000
모바일 수수료: 2000
```

**해설:**
- CreditCard: 100000 × 0.03 = 3000
- MobilePayment: 100000 × 0.02 = 2000
- 각 객체의 실제 타입에 따라 다른 메서드 실행

---

### 문제 5 정답

**답:**
- 빈칸 1: `super(amount)`
- 빈칸 2: `new CreditCard(100000)`

**해설:**
- `super(amount)`: 부모 생성자 호출
- `new CreditCard(100000)`: CreditCard 객체 생성 후 업캐스팅
- 수수료: 100000 × 0.03 = 3000

---

### 문제 6 정답

**답:**
```
신용카드: 100000원, 수수료: 3000원
현금: 50000원, 수수료: 0원
신용카드: 200000원, 수수료: 6000원
현금: 30000원, 수수료: 0원
```

**해설:**
- 각 객체의 `printInfo()` 호출
- 내부에서 `calculateFee()` 호출 시 실제 객체 타입에 맞는 메서드 실행
- 다형성의 예시

---

### 문제 7 정답

**답:** `총액: 359050`

**해설:**
- payments[0]: CreditCard(100000) → 총액 103000
- payments[1]: BankTransfer(50000) → 총액 50050
- payments[2]: CreditCard(200000) → 총액 206000
- 총액: 103000 + 50050 + 206000 = 359050

**계산 과정:**
- CreditCard 수수료: 3%
- BankTransfer 수수료: 0.1%
- 각 결제의 총액 = 금액 + 수수료

---

### 문제 8 정답

**답:**
```
카드 결제
한도 확인
현금 결제
거스름돈
카드 결제
한도 확인
```

**해설:**
- `processPayment()`: 오버라이딩된 메서드 호출
- `instanceof`로 타입 확인 후 다운캐스팅
- 타입별 특수 메서드 호출

---

### 문제 9 정답

**답:**
```
수수료: 3000
수수료: 2000
```

**해설:**
- `printFee()`는 부모 클래스의 메서드
- 내부에서 `calculateFee()` 호출 시 실제 객체 타입에 맞는 메서드 실행
- CreditCard: 100000 × 0.03 = 3000
- MobilePayment: 100000 × 0.02 = 2000

---

### 문제 10 정답

**답:**
- 빈칸 1: `new CreditCard(100000)`
- 빈칸 2: `new CreditCard(200000)`
- 빈칸 3: `p.calculateFee()`

**해설:**
- 총 수수료 9000원이 나오려면:
  - 100000 × 0.03 = 3000
  - 200000 × 0.03 = 6000
  - 합계: 9000

---

### 문제 11 정답

**답:**
```
총 결제금액: 480000
총 수수료: 9150
총액: 489150
```

**해설:**
- payments[0]: CreditCard(100000) → 수수료 3000
- payments[1]: BankTransfer(50000) → 수수료 50
- payments[2]: CashPayment(30000) → 수수료 0
- payments[3]: CreditCard(200000) → 수수료 6000
- payments[4]: BankTransfer(100000) → 수수료 100
- 총 결제금액: 100000 + 50000 + 30000 + 200000 + 100000 = 480000
- 총 수수료: 3000 + 50 + 0 + 6000 + 100 = 9150
- 총액: 480000 + 9150 = 489150

**상세 계산:**
- CreditCard(100000): 수수료 100000 × 0.03 = 3000
- BankTransfer(50000): 수수료 50000 × 0.001 = 50
- CashPayment(30000): 수수료 0
- CreditCard(200000): 수수료 200000 × 0.03 = 6000
- BankTransfer(100000): 수수료 100000 × 0.001 = 100

---

### 문제 12 정답

**답:**
```
금액: 100000
수수료: 3000
```

**해설:**
- 메서드 체이닝: `printAmount()`가 `this`를 반환하여 연속 호출 가능
- `payment.printAmount().printFee()`: 먼저 금액 출력, 그 다음 수수료 출력
- `printFee()`에서 `calculateFee()` 호출 시 실제 객체 타입에 맞는 메서드 실행

---

### 문제 13 정답

**답:**
```
신용카드: 2건
모바일결제: 2건
```

**해설:**
- `instanceof`로 각 타입의 개수 카운트
- payments[0], payments[2]: CreditCard → cardCount = 2
- payments[1], payments[3]: MobilePayment → mobileCount = 2

---

### 문제 14 정답

**답:**
- 빈칸 1: `super(amount)`
- 빈칸 2: `amount * 0.03`
- 빈칸 3: `new CreditCard(100000)`

**해설:**
- `super(amount)`: 부모 생성자 호출
- `amount * 0.03`: 3% 수수료 계산
- `new CreditCard(100000)`: CreditCard 객체 생성 후 업캐스팅

---

### 문제 15 정답

**답:**
```
=== 결제 정보 ===
신용카드 - 금액: 100000, 수수료: 3000
계좌이체 - 금액: 50000, 수수료: 50
신용카드 - 금액: 200000, 수수료: 6000

=== 결제 처리 ===
삼성카드 카드로 결제
한도 확인
국민은행 계좌로 이체
신한카드 카드로 결제
한도 확인
```

**해설:**
- 첫 번째 반복문: 모든 결제 정보 출력 (다형성 활용)
- 두 번째 반복문: 결제 처리 및 타입별 특수 기능
- `instanceof`로 CreditCard 확인 후 다운캐스팅하여 `checkLimit()` 호출

---

## 핵심 개념 정리

### 1. 추상 클래스와 추상 메서드

- `abstract class`: 인스턴스 생성 불가
- `abstract method`: 자식 클래스에서 반드시 구현해야 함
- 다형성의 기반이 됨

### 2. 업캐스팅과 다형성

- 자식 객체를 부모 타입으로 참조
- 오버라이딩된 메서드는 실제 객체 타입에 따라 호출됨
- 배열에 다양한 타입 저장 가능

### 3. 수수료 계산의 다형성

- 각 결제 수단마다 다른 수수료율
- 같은 `calculateFee()` 호출이 타입에 따라 다른 결과
- 코드 중복 제거

### 4. instanceof와 다운캐스팅

- 타입 확인 후 안전한 다운캐스팅
- 타입별 특수 기능 처리

---

**작성일:** 2026-01-30  
**범위:** 결제 시스템 - 업캐스팅 문제
