# 학생 관리 시스템 - 메뉴 기반 정답 (캡슐화)

## Student.java

```java
package a0120.student;

public class Student {
    private String name;
    private int score;

    public Student(String name, int score) {
        this.name = name;
        this.score = score;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public int getScore() {
        return score;
    }

    public void setScore(int score) {
        this.score = score;
    }

    @Override
    public String toString() {
        return "Student{name='" + name + "', score=" + score + "}";
    }
}
```

---

## StudentApp.java

```java
package a0120.student;

import java.util.ArrayList;
import java.util.Scanner;

public class StudentApp {
    public static void main(String[] args) {
        ArrayList<Student> list = new ArrayList<>();
        Scanner sc = new Scanner(System.in);
        boolean run = true;

        // 더미 데이터
        list.add(new Student("홍길동", 90));
        list.add(new Student("김영희", 85));
        list.add(new Student("이철수", 78));

        while (run) {
            System.out.println("------------------------");
            System.out.println("1. 학생 등록");
            System.out.println("2. 학생 검색");
            System.out.println("3. 학생 수정");
            System.out.println("4. 학생 삭제");
            System.out.println("5. 전체 출력");
            System.out.println("6. 종료");
            System.out.print("선택 > ");

            int menu;

            try {
                menu = Integer.parseInt(sc.nextLine());
            } catch (NumberFormatException e) {
                System.out.println("숫자만 입력하세요");
                continue;
            }

            switch (menu) {
                case 1:
                    System.out.print("이름 입력 : ");
                    String name = sc.nextLine();

                    System.out.print("점수 입력 : ");
                    int score;

                    try {
                        score = Integer.parseInt(sc.nextLine());
                    } catch (NumberFormatException e) {
                        System.out.println("점수는 숫자로 입력하세요");
                        break;
                    }

                    list.add(new Student(name, score));
                    System.out.println("학생 등록 완료");
                    break;

                case 2:
                    System.out.print("검색할 이름 : ");
                    name = sc.nextLine();

                    Student student = findStudent(list, name);

                    if (student != null) {
                        System.out.println(student);
                    } else {
                        System.out.println("학생이 없습니다");
                    }
                    break;

                case 3:
                    System.out.print("수정할 학생 이름 : ");
                    name = sc.nextLine();

                    System.out.print("새 점수 입력 : ");
                    int newScore;

                    try {
                        newScore = Integer.parseInt(sc.nextLine());
                    } catch (NumberFormatException e) {
                        System.out.println("점수는 숫자로 입력하세요");
                        break;
                    }

                    if (updateStudent(list, name, newScore)) {
                        System.out.println("수정 완료");
                    } else {
                        System.out.println("해당 학생이 없습니다");
                    }
                    break;

                case 4:
                    System.out.print("삭제할 학생 이름 : ");
                    name = sc.nextLine();

                    if (deleteStudent(list, name)) {
                        System.out.println("삭제 완료");
                    } else {
                        System.out.println("삭제할 학생이 없습니다");
                    }
                    break;

                case 5:
                    if (list.isEmpty()) {
                        System.out.println("등록된 학생이 없습니다");
                    } else {
                        for (Student s : list) {
                            System.out.println(s);
                        }
                    }
                    break;

                case 6:
                    run = false;
                    System.out.println("프로그램 종료");
                    break;

                default:
                    System.out.println("1~6번 중에 선택하세요");
            }
        }

        sc.close();
    }

    // 학생 검색
    static Student findStudent(ArrayList<Student> list, String name) {
        for (Student student : list) {
            if (student.getName().equals(name)) {
                return student;
            }
        }
        return null;
    }

    // 학생 점수 수정
    static boolean updateStudent(
            ArrayList<Student> list,
            String name,
            int newScore) {

        for (Student student : list) {
            if (student.getName().equals(name)) {
                student.setScore(newScore);
                return true;
            }
        }
        return false;
    }

    // 학생 삭제
    static boolean deleteStudent(
            ArrayList<Student> list,
            String name) {

        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).getName().equals(name)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }
}
```

---

## 도서 관리 시스템과 비교

| 도서 관리 | 학생 관리 |
|-----------|-----------|
| `Book` | `Student` |
| `title` | `name` |
| `author` | `score` |
| `findBook()` | `findStudent()` |
| `updateBook()` | `updateStudent()` |
| `deleteBook()` | `deleteStudent()` |

---

## 캡슐화 적용 부분

```java
private String name;
private int score;
```

필드를 읽을 때는 Getter를 사용합니다.

```java
student.getName()
student.getScore()
```

필드를 수정할 때는 Setter를 사용합니다.

```java
student.setName(newName);
student.setScore(newScore);
```

---

## 핵심 메서드

### 검색

```java
if (student.getName().equals(name)) {
    return student;
}
```

### 수정

```java
if (student.getName().equals(name)) {
    student.setScore(newScore);
    return true;
}
```

### 삭제

```java
if (list.get(i).getName().equals(name)) {
    list.remove(i);
    return true;
}
```

---

## 실행 결과 예시

```text
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 5
Student{name='홍길동', score=90}
Student{name='김영희', score=85}
Student{name='이철수', score=78}
```

---

## 관련 파일

- 문제: [학생관리시스템-메뉴기반-캡슐화-문제.md](./학생관리시스템-메뉴기반-캡슐화-문제.md)
- 원본: [도서관리시스템-메뉴기반-정답2.md](./도서관리시스템-메뉴기반-정답2.md)
