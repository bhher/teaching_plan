# Comparator 정렬 — 정답

문제: [문제.md](./문제.md)

---

## 핵심

| 번호 | 기준 | 작성 방식 | 비교식 |
|------|------|-----------|--------|
| ① | 이름 오름차순 | 익명 클래스 | `a.name.compareTo(b.name)` |
| ② | 나이 오름차순 | 람다 | `a.age - b.age` |
| ③ | 점수 내림차순 | 람다 | `b.score - a.score` |
| ④ | 점수↓, 같으면 이름↑ | 람다 | 점수 다르면 `b-a`, 같으면 `name.compareTo` |

---

## Student.java

```java
public class Student {
    String name;
    int age;
    int score;

    public Student(String name, int age, int score) {
        this.name = name;
        this.age = age;
        this.score = score;
    }

    @Override
    public String toString() {
        return name + "(나이:" + age + ", 점수:" + score + ")";
    }
}
```

---

## ComparatorPractice.java (완성)

```java
import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;

public class ComparatorPractice {
    public static void main(String[] args) {
        ArrayList<Student> list = new ArrayList<>();
        list.add(new Student("홍길동", 20, 85));
        list.add(new Student("김철수", 22, 92));
        list.add(new Student("이영희", 21, 78));
        list.add(new Student("박민수", 19, 92));

        System.out.println("=== 이름 오름차순 ===");
        Collections.sort(list, new Comparator<Student>() {
            @Override
            public int compare(Student a, Student b) {
                return a.name.compareTo(b.name);
            }
        });
        print(list);

        System.out.println("\n=== 나이 오름차순 (람다) ===");
        Collections.sort(list, (a, b) -> a.age - b.age);
        print(list);

        System.out.println("\n=== 점수 내림차순 (람다) ===");
        Collections.sort(list, (a, b) -> b.score - a.score);
        print(list);

        System.out.println("\n=== 점수 내림차순, 같으면 이름순 ===");
        Collections.sort(list, (a, b) -> {
            if (b.score != a.score) {
                return b.score - a.score;
            }
            return a.name.compareTo(b.name);
        });
        print(list);
    }

    static void print(ArrayList<Student> list) {
        for (Student s : list) {
            System.out.println(s);
        }
    }
}
```

---

## 실행 결과

```
=== 이름 오름차순 ===
김철수(나이:22, 점수:92)
박민수(나이:19, 점수:92)
이영희(나이:21, 점수:78)
홍길동(나이:20, 점수:85)

=== 나이 오름차순 (람다) ===
박민수(나이:19, 점수:92)
홍길동(나이:20, 점수:85)
이영희(나이:21, 점수:78)
김철수(나이:22, 점수:92)

=== 점수 내림차순 (람다) ===
박민수(나이:19, 점수:92)
김철수(나이:22, 점수:92)
홍길동(나이:20, 점수:85)
이영희(나이:21, 점수:78)

=== 점수 내림차순, 같으면 이름순 ===
김철수(나이:22, 점수:92)
박민수(나이:19, 점수:92)
홍길동(나이:20, 점수:85)
이영희(나이:21, 점수:78)
```
