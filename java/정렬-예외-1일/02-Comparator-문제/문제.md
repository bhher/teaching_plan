# Comparator 정렬 문제

`Student` 리스트를 **여러 기준**으로 정렬하세요.  
`Comparable`은 **구현하지 마세요.**  
정렬은 전부 `Collections.sort(list, …)` + **Comparator / 람다**로 합니다.

정답: [정답.md](./정답.md)

---

## 할 일

아래 데이터를 넣고, **4가지** 정렬을 순서대로 구현한 뒤 매번 목록을 출력하세요.

```java
list.add(new Student("홍길동", 20, 85));
list.add(new Student("김철수", 22, 92));
list.add(new Student("이영희", 21, 78));
list.add(new Student("박민수", 19, 92));
```

| 번호 | 정렬 기준 | 작성 방식 |
|------|-----------|-----------|
| ① | 이름 오름차순 | `Comparator` **익명 클래스** |
| ② | 나이 오름차순 | **람다** |
| ③ | 점수 내림차순 | **람다** |
| ④ | 점수 내림차순, 같으면 이름 오름차순 | **람다** |

---

## 힌트

```text
음수 → a가 앞
0    → 같음
양수 → a가 뒤

오름차순: a - b  /  a.name.compareTo(b.name)
내림차순: b - a
```

---

## 출력 예시

```
=== 이름 오름차순 ===
김철수(나이:22, 점수:92)
박민수(나이:19, 점수:92)
이영희(나이:21, 점수:78)
홍길동(나이:20, 점수:85)

=== 나이 오름차순 (람다) ===
박민수(나이:19, 점수:92)
홍길동(나이:20, 점수:85)
이영희(나이:21, 점수:78)
김철수(나이:22, 점수:92)

=== 점수 내림차순 (람다) ===
박민수(나이:19, 점수:92)
김철수(나이:22, 점수:92)
홍길동(나이:20, 점수:85)
이영희(나이:21, 점수:78)

=== 점수 내림차순, 같으면 이름순 ===
김철수(나이:22, 점수:92)
박민수(나이:19, 점수:92)
홍길동(나이:20, 점수:85)
이영희(나이:21, 점수:78)
```

---

## Student.java (그대로 사용)

```java
public class Student {
    String name;
    int age;
    int score;

    public Student(String name, int age, int score) {
        this.name = name;
        this.age = age;
        this.score = score;
    }

    @Override
    public String toString() {
        return name + "(나이:" + age + ", 점수:" + score + ")";
    }
}
```

---

## ComparatorPractice.java (TODO 채우기)

```java
import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;

public class ComparatorPractice {
    public static void main(String[] args) {
        ArrayList<Student> list = new ArrayList<>();
        list.add(new Student("홍길동", 20, 85));
        list.add(new Student("김철수", 22, 92));
        list.add(new Student("이영희", 21, 78));
        list.add(new Student("박민수", 19, 92));

        // ① 이름 오름차순 (익명 클래스 Comparator)
        System.out.println("=== 이름 오름차순 ===");
        Collections.sort(list, new Comparator<Student>() {
            @Override
            public int compare(Student a, Student b) {
                // TODO: 이름 오름차순
                return 0;
            }
        });
        print(list);

        // ② 나이 오름차순 (람다)
        System.out.println("\n=== 나이 오름차순 (람다) ===");
        // TODO: Collections.sort(list, (a, b) -> …);
        print(list);

        // ③ 점수 내림차순 (람다)
        System.out.println("\n=== 점수 내림차순 (람다) ===");
        // TODO: Collections.sort(list, (a, b) -> …);
        print(list);

        // ④ 점수 내림차순, 같으면 이름 오름차순
        System.out.println("\n=== 점수 내림차순, 같으면 이름순 ===");
        // TODO: Collections.sort(list, (a, b) -> { … });
        print(list);
    }

    static void print(ArrayList<Student> list) {
        for (Student s : list) {
            System.out.println(s);
        }
    }
}
```

---

## 체크

- [ ] 익명 클래스 Comparator 1번
- [ ] 람다로 나이·점수 정렬
- [ ] 복합 조건(점수 → 이름) 정렬
- [ ] `Comparable`을 Student에 쓰지 않음
