# 자바 작업형 평가 문제

**총점: 80점 (각 문제 20점)**

**시험 시간: 90분**

**주의사항:**
- 각 문제는 독립적으로 작성하세요
- 클래스명과 메서드명은 정확히 지켜주세요
- 컴파일 오류가 발생하면 해당 문제는 0점 처리됩니다
- 주석은 선택사항이지만, 코드 가독성을 위해 작성하는 것을 권장합니다

---

## 문제 1: Student 클래스 작성 (20점)

### 📚 문제 설명

학생 정보를 저장하는 `Student` 클래스를 작성하세요. 학생의 이름, 나이, 점수를 관리하고, 점수에 따라 등급을 계산하는 기능을 구현하세요.

---

### 📋 요구사항

#### 1. Student 클래스 작성

**필드**
- `name` (String): 학생 이름
- `age` (int): 학생 나이
- `score` (int): 학생 점수

**생성자**
- 매개변수가 없는 기본 생성자
- `name`, `age`, `score`를 받는 매개변수 생성자

**메서드**
- `toString()`: 학생 정보를 다음 형식으로 반환
  ```
  "이름: [name], 나이: [age], 점수: [score]"
  ```
- `getGrade()`: 점수에 따라 등급을 반환하는 메서드
  - 90점 이상: "A"
  - 80점 이상: "B"
  - 70점 이상: "C"
  - 60점 이상: "D"
  - 60점 미만: "F"

#### 2. 실행 코드 (제공됨)

```java
public class Problem1 {
    public static void main(String[] args) {
        Student s1 = new Student("홍길동", 20, 85);
        Student s2 = new Student("김철수", 22, 95);
        
        System.out.println(s1);
        System.out.println("등급: " + s1.getGrade());
        System.out.println(s2);
        System.out.println("등급: " + s2.getGrade());
    }
}
```

---

### 🎯 실행 결과 예시

```
이름: 홍길동, 나이: 20, 점수: 85
등급: B
이름: 김철수, 나이: 22, 점수: 95
등급: A
```

---

### 💡 힌트

1. **필드 선언**: 클래스 내부에 변수 선언
2. **생성자**: `this` 키워드를 사용하여 필드 초기화
3. **toString()**: `@Override` 어노테이션 사용, 문자열 연결
4. **getGrade()**: `if-else if` 구조 사용, 조건 순서 중요 (높은 점수부터 확인)

---

### ✅ 체크리스트

- [ ] Student 클래스에 필드 3개 선언 (name, age, score)
- [ ] 기본 생성자 작성
- [ ] 매개변수 생성자 작성 (this 키워드 사용)
- [ ] toString() 메서드 구현 (@Override 사용)
- [ ] getGrade() 메서드 구현 (if-else if 구조)
- [ ] 실행 코드와 함께 컴파일 및 실행 확인

---

### 📝 채점 기준

- 필드 선언: 5점
- 생성자 작성: 5점
- toString() 메서드: 5점
- getGrade() 메서드: 5점

---

## 문제 2: ArrayList를 이용한 학생 관리 (20점)

### 📚 문제 설명

ArrayList를 사용하여 학생 정보를 관리하는 프로그램을 작성하세요. 학생을 추가, 검색, 평균 점수 계산, 전체 출력하는 기능을 구현하세요.

---

### 📋 요구사항

#### 1. StudentManager 클래스 작성

다음 static 메서드들을 `StudentManager` 클래스에 작성하세요:

**1. addStudent()**
```java
public static void addStudent(ArrayList<Student> list, String name, int age, int score)
```
- 학생 정보를 리스트에 추가하는 메서드
- 반환값: 없음 (void)

**2. findStudent()**
```java
public static Student findStudent(ArrayList<Student> list, String name)
```
- 이름으로 학생을 찾는 메서드
- 찾으면 Student 객체 반환, 못 찾으면 null 반환

**3. getAverageScore()**
```java
public static double getAverageScore(ArrayList<Student> list)
```
- 리스트에 있는 모든 학생의 평균 점수를 계산하는 메서드
- 반환값: double (평균 점수)
- 빈 리스트일 경우 0.0 반환

**4. printAll()**
```java
public static void printAll(ArrayList<Student> list)
```
- 리스트에 있는 모든 학생 정보를 출력하는 메서드
- 반환값: 없음 (void)

#### 2. 실행 코드 (제공됨)

```java
import java.util.ArrayList;

public class Problem2 {
    public static void main(String[] args) {
        ArrayList<Student> list = new ArrayList<>();
        
        StudentManager.addStudent(list, "홍길동", 20, 85);
        StudentManager.addStudent(list, "김철수", 22, 95);
        StudentManager.addStudent(list, "이영희", 21, 75);
        
        System.out.println("=== 전체 학생 정보 ===");
        StudentManager.printAll(list);
        
        System.out.println("\n=== 학생 검색 ===");
        Student s = StudentManager.findStudent(list, "김철수");
        if (s != null) {
            System.out.println("찾은 학생: " + s);
        }
        
        System.out.println("\n=== 평균 점수 ===");
        double avg = StudentManager.getAverageScore(list);
        System.out.println("평균: " + avg);
    }
}
```

---

### 🎯 실행 결과 예시

```
=== 전체 학생 정보 ===
이름: 홍길동, 나이: 20, 점수: 85
이름: 김철수, 나이: 22, 점수: 95
이름: 이영희, 나이: 21, 점수: 75

=== 학생 검색 ===
찾은 학생: 이름: 김철수, 나이: 22, 점수: 95

=== 평균 점수 ===
평균: 85.0
```

---

### 💡 힌트

1. **ArrayList 사용**: `import java.util.ArrayList;` 필요
2. **addStudent()**: `list.add(new Student(...))` 사용
3. **findStudent()**: 향상된 for문 사용, `equals()` 메서드로 이름 비교
4. **getAverageScore()**: 빈 리스트 체크 (`isEmpty()`), 형변환 필요 `(double) sum / list.size()`
5. **printAll()**: 향상된 for문으로 순회하며 출력

---

### ✅ 체크리스트

- [ ] StudentManager 클래스 작성
- [ ] addStudent() 메서드 구현 (list.add() 사용)
- [ ] findStudent() 메서드 구현 (향상된 for문, equals() 사용)
- [ ] getAverageScore() 메서드 구현 (빈 리스트 체크, 형변환)
- [ ] printAll() 메서드 구현 (향상된 for문)
- [ ] 모든 메서드가 static으로 선언되었는지 확인
- [ ] 실행 코드와 함께 컴파일 및 실행 확인

---

### 📝 채점 기준

- addStudent() 메서드: 5점
- findStudent() 메서드: 5점
- getAverageScore() 메서드: 5점
- printAll() 메서드: 5점

---

## 문제 3: 문자열 처리 및 배열 활용 (20점)

### 📚 문제 설명

문자열을 처리하고 배열을 활용하는 유틸리티 메서드들을 작성하세요. 단어 개수 세기, 문자열 뒤집기, 최댓값 찾기, 값 포함 여부 확인 기능을 구현하세요.

---

### 📋 요구사항

#### 1. StringUtil 클래스 작성

다음 static 메서드들을 `StringUtil` 클래스에 작성하세요:

**1. countWords()**
```java
public static int countWords(String text)
```
- 문자열에서 단어의 개수를 세는 메서드
- 단어는 공백으로 구분됨
- null이거나 빈 문자열이면 0 반환
- 예: "안녕하세요 자바입니다" → 2개

**2. reverseString()**
```java
public static String reverseString(String text)
```
- 문자열을 뒤집는 메서드
- null이면 null 반환
- 예: "자바" → "바자"

**3. findMax()**
```java
public static int findMax(int[] arr)
```
- 정수 배열에서 최댓값을 찾는 메서드
- 배열이 null이거나 비어있으면 -1 반환

**4. contains()**
```java
public static boolean contains(int[] arr, int value)
```
- 배열에 특정 값이 있는지 확인하는 메서드
- 있으면 true, 없으면 false 반환
- 배열이 null이면 false 반환

#### 2. 실행 코드 (제공됨)

```java
public class Problem3 {
    public static void main(String[] args) {
        // 1. 단어 개수 세기
        String text1 = "안녕하세요 자바입니다";
        System.out.println("단어 개수: " + StringUtil.countWords(text1));
        
        // 2. 문자열 뒤집기
        String text2 = "자바";
        System.out.println("뒤집기: " + StringUtil.reverseString(text2));
        
        // 3. 최댓값 찾기
        int[] arr = {10, 5, 20, 8, 15};
        System.out.println("최댓값: " + StringUtil.findMax(arr));
        
        // 4. 값 포함 여부 확인
        System.out.println("15 포함: " + StringUtil.contains(arr, 15));
        System.out.println("100 포함: " + StringUtil.contains(arr, 100));
    }
}
```

---

### 🎯 실행 결과 예시

```
단어 개수: 2
뒤집기: 바자
최댓값: 20
15 포함: true
100 포함: false
```

---

### 💡 힌트

1. **countWords()**: `trim()`, `split("\\s+")` 사용, null 체크 필요
2. **reverseString()**: StringBuilder 사용, `charAt()` 메서드로 문자 추출
3. **findMax()**: 첫 번째 값을 초기값으로 설정, 순회하며 비교
4. **contains()**: 향상된 for문 사용 가능, 찾으면 즉시 반환

---

### ✅ 체크리스트

- [ ] StringUtil 클래스 작성
- [ ] countWords() 메서드 구현 (trim(), split() 사용, null 체크)
- [ ] reverseString() 메서드 구현 (StringBuilder 사용)
- [ ] findMax() 메서드 구현 (null 체크, 빈 배열 체크)
- [ ] contains() 메서드 구현 (null 체크)
- [ ] 모든 메서드가 static으로 선언되었는지 확인
- [ ] 실행 코드와 함께 컴파일 및 실행 확인

---

### 📝 채점 기준

- countWords() 메서드: 5점
- reverseString() 메서드: 5점
- findMax() 메서드: 5점
- contains() 메서드: 5점

---

## 문제 4: 종합 문제 - 도서 관리 시스템 (20점)

### 📚 문제 설명

도서 정보를 관리하는 간단한 시스템을 구현하세요. Book 클래스를 작성하고, BookManager 클래스에 도서 추가, 검색, 가격 계산, 삭제 기능을 구현하세요.

---

### 📋 요구사항

#### 1. Book 클래스 작성

**필드**
- `title` (String): 도서 제목
- `author` (String): 저자
- `price` (int): 가격

**생성자**
- `title`, `author`, `price`를 받는 생성자

**메서드**
- `toString()`: 도서 정보를 다음 형식으로 반환
  ```
  "제목: [title], 저자: [author], 가격: [price]원"
  ```

#### 2. BookManager 클래스 작성

다음 static 메서드들을 `BookManager` 클래스에 작성하세요:

**1. addBook()**
```java
public static void addBook(ArrayList<Book> list, String title, String author, int price)
```
- 도서를 리스트에 추가하는 메서드
- 반환값: 없음 (void)

**2. findBooksByAuthor()**
```java
public static ArrayList<Book> findBooksByAuthor(ArrayList<Book> list, String author)
```
- 특정 저자의 도서를 모두 찾아서 리스트로 반환
- 없으면 빈 리스트 반환
- 새로운 ArrayList를 생성하여 결과를 저장

**3. getTotalPrice()**
```java
public static int getTotalPrice(ArrayList<Book> list)
```
- 모든 도서의 총 가격을 계산하여 반환
- 반환값: int (총 가격)

**4. removeBook()**
```java
public static boolean removeBook(ArrayList<Book> list, String title)
```
- 제목으로 도서를 찾아 삭제
- 성공하면 true, 실패하면 false 반환
- 인덱스를 사용한 삭제 (일반 for문 필요)

#### 3. 실행 코드 (제공됨)

```java
import java.util.ArrayList;

public class Problem4 {
    public static void main(String[] args) {
        ArrayList<Book> list = new ArrayList<>();
        
        BookManager.addBook(list, "자바의 정석", "남궁성", 30000);
        BookManager.addBook(list, "혼자 공부하는 자바", "신용권", 28000);
        BookManager.addBook(list, "이것이 자바다", "신용권", 32000);
        
        System.out.println("=== 전체 도서 ===");
        for (Book b : list) {
            System.out.println(b);
        }
        
        System.out.println("\n=== 신용권 저자 도서 ===");
        ArrayList<Book> authorBooks = BookManager.findBooksByAuthor(list, "신용권");
        for (Book b : authorBooks) {
            System.out.println(b);
        }
        
        System.out.println("\n=== 총 가격 ===");
        int total = BookManager.getTotalPrice(list);
        System.out.println("총 가격: " + total + "원");
        
        System.out.println("\n=== 도서 삭제 ===");
        boolean removed = BookManager.removeBook(list, "자바의 정석");
        System.out.println("삭제 결과: " + removed);
        
        System.out.println("\n=== 삭제 후 전체 도서 ===");
        for (Book b : list) {
            System.out.println(b);
        }
    }
}
```

---

### 🎯 실행 결과 예시

```
=== 전체 도서 ===
제목: 자바의 정석, 저자: 남궁성, 가격: 30000원
제목: 혼자 공부하는 자바, 저자: 신용권, 가격: 28000원
제목: 이것이 자바다, 저자: 신용권, 가격: 32000원

=== 신용권 저자 도서 ===
제목: 혼자 공부하는 자바, 저자: 신용권, 가격: 28000원
제목: 이것이 자바다, 저자: 신용권, 가격: 32000원

=== 총 가격 ===
총 가격: 90000원

=== 도서 삭제 ===
삭제 결과: true

=== 삭제 후 전체 도서 ===
제목: 혼자 공부하는 자바, 저자: 신용권, 가격: 28000원
제목: 이것이 자바다, 저자: 신용권, 가격: 32000원
```

---

### 💡 힌트

1. **Book 클래스**: 필드, 생성자, toString() 메서드 작성
2. **addBook()**: `list.add(new Book(...))` 사용
3. **findBooksByAuthor()**: 새로운 ArrayList 생성, 향상된 for문으로 순회하며 조건 확인
4. **getTotalPrice()**: 모든 도서의 가격을 합산
5. **removeBook()**: 일반 for문 사용 (인덱스 필요), `list.remove(i)` 사용

---

### ✅ 체크리스트

- [ ] Book 클래스 작성 (필드 3개, 생성자, toString())
- [ ] BookManager 클래스 작성
- [ ] addBook() 메서드 구현
- [ ] findBooksByAuthor() 메서드 구현 (새 리스트 생성)
- [ ] getTotalPrice() 메서드 구현
- [ ] removeBook() 메서드 구현 (일반 for문 사용)
- [ ] 모든 메서드가 static으로 선언되었는지 확인
- [ ] 실행 코드와 함께 컴파일 및 실행 확인

---

### 📝 채점 기준

- Book 클래스 작성: 5점
- addBook() 메서드: 3점
- findBooksByAuthor() 메서드: 5점
- getTotalPrice() 메서드: 3점
- removeBook() 메서드: 4점

---

## 제출 방법

1. 각 문제별로 별도의 파일로 작성하세요
   - Problem1.java (Student 클래스 포함)
   - Problem2.java (StudentManager 클래스 포함)
   - Problem3.java (StringUtil 클래스 포함)
   - Problem4.java (Book, BookManager 클래스 포함)

2. 모든 파일을 컴파일하여 오류가 없는지 확인하세요

3. 실행 결과가 예시와 일치하는지 확인하세요

---

## 참고사항

- 문제 1의 Student 클래스는 문제 2에서도 사용됩니다
- 문제 4는 문제 1~3의 개념을 종합하여 해결하는 문제입니다
- 각 메서드는 요구사항에 정확히 맞게 작성해야 합니다
- 예외 처리는 선택사항입니다 (기본적인 입력값 검증만 필요)

---

## 📝 추가 도전 과제 (선택사항)

### 문제 1 확장
- Student 클래스에 `setScore(int score)` 메서드 추가
- 점수 변경 시 등급도 자동으로 업데이트되는 기능

### 문제 2 확장
- `findStudentsByGrade(ArrayList<Student> list, String grade)` 메서드 추가
- 특정 등급의 학생들을 모두 찾는 기능

### 문제 3 확장
- `findMin(int[] arr)` 메서드 추가 (최솟값 찾기)
- `reverseArray(int[] arr)` 메서드 추가 (배열 뒤집기)

### 문제 4 확장
- `findBooksByPriceRange(ArrayList<Book> list, int min, int max)` 메서드 추가
- 특정 가격 범위의 도서를 찾는 기능
- `sortBooksByPrice(ArrayList<Book> list)` 메서드 추가
- 가격 순으로 정렬하는 기능
