# 자바 작업형 평가 문제 정답

---

## 문제 1: 도서 관리 시스템 - 메뉴 기반 정답

### 📁 Book.java

```java
public class Book {
    String title;   // 도서 제목
    String author;  // 저자
    
    // 생성자
    public Book(String title, String author) {
        this.title = title;
        this.author = author;
    }
    
    // toString() 메서드: 객체를 문자열로 변환
    @Override
    public String toString() {
        return "Book{title=" + title + ", author=" + author + "}";
    }
}
```

---

### 📁 BookApp.java

```java
import java.util.ArrayList;
import java.util.Scanner;

public class BookApp {
    public static void main(String[] args) {

        ArrayList<Book> list = new ArrayList<>();
        Scanner sc = new Scanner(System.in);
        boolean run = true;

        while (run) {
            System.out.println("------------------------");
            System.out.println("1. 도서 등록");
            System.out.println("2. 도서 검색");
            System.out.println("3. 도서 수정");
            System.out.println("4. 도서 삭제");
            System.out.println("5. 전체 출력");
            System.out.println("6. 종료");
            System.out.print("선택 > ");

            int menu;
            try {
                menu = Integer.parseInt(sc.nextLine());
            } catch (Exception e) {
                System.out.println("숫자만 입력하세요");
                continue;
            }

            switch (menu) {
                case 1: // 등록
                    System.out.print("제목 입력 : ");
                    String title = sc.nextLine();

                    System.out.print("저자 입력 : ");
                    String author = sc.nextLine();

                    list.add(new Book(title, author));
                    System.out.println("도서 등록 완료");
                    break;

                case 2: // 검색
                    System.out.print("검색할 제목 : ");
                    title = sc.nextLine();

                    Book b = findBook(list, title);
                    if (b != null) {
                        System.out.println(b);
                    } else {
                        System.out.println("도서가 없습니다");
                    }
                    break;

                case 3: // 수정
                    System.out.print("수정할 제목 : ");
                    title = sc.nextLine();

                    System.out.print("새 저자 입력 : ");
                    author = sc.nextLine();

                    if (updateBook(list, title, author)) {
                        System.out.println("수정 완료");
                    } else {
                        System.out.println("해당 도서가 없습니다");
                    }
                    break;

                case 4: // 삭제
                    System.out.print("삭제할 제목 : ");
                    title = sc.nextLine();

                    if (deleteBook(list, title)) {
                        System.out.println("삭제 완료");
                    } else {
                        System.out.println("삭제할 도서가 없습니다");
                    }
                    break;

                case 5: // 전체 출력
                    if (list.isEmpty()) {
                        System.out.println("등록된 도서가 없습니다");
                    } else {
                        for (Book book : list) {
                            System.out.println(book);
                        }
                    }
                    break;

                case 6: // 종료
                    run = false;
                    System.out.println("프로그램 종료");
                    break;

                default:
                    System.out.println("1~6번 중에 선택하세요");
            }
        }
        sc.close();
    }

    // 도서 검색
    static Book findBook(ArrayList<Book> list, String title) {
        for (Book b : list) {
            if (b.title.equals(title)) {
                return b;
            }
        }
        return null;
    }

    // 도서 수정
    static boolean updateBook(ArrayList<Book> list, String title, String newAuthor) {
        for (Book b : list) {
            if (b.title.equals(title)) {
                b.author = newAuthor;
                return true;
            }
        }
        return false;
    }

    // 도서 삭제
    static boolean deleteBook(ArrayList<Book> list, String title) {
        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).title.equals(title)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }
}
```

---

## 문제 2: Car 클래스 작성 정답

### 📁 Car.java

```java
public class Car {
    String brand;   // 자동차 브랜드
    String model;   // 자동차 모델
    int year;       // 제조년도
    
    // 기본 생성자
    public Car() {
    }
    
    // 매개변수 생성자
    public Car(String brand, String model, int year) {
        this.brand = brand;
        this.model = model;
        this.year = year;
    }
    
    // toString() 메서드: 자동차 정보를 문자열로 반환
    @Override
    public String toString() {
        return "브랜드: " + brand + ", 모델: " + model + ", 제조년도: " + year + "년";
    }
    
    // 자동차 나이 계산 메서드
    public int getAge(int currentYear) {
        return currentYear - year;
    }
}
```

---

### 📁 Problem2.java (실행 파일)

```java
public class Problem2 {
    public static void main(String[] args) {
        Car c1 = new Car("현대", "소나타", 2020);
        Car c2 = new Car("기아", "K5", 2022);
        
        System.out.println(c1);
        System.out.println("나이: " + c1.getAge(2024) + "년");
        System.out.println(c2);
        System.out.println("나이: " + c2.getAge(2024) + "년");
    }
}
```

---

### 🔍 코드 설명

#### Car 클래스 구조

**필드:**
- `brand` (String): 자동차 브랜드 저장
- `model` (String): 자동차 모델 저장
- `year` (int): 제조년도 저장

**생성자:**
- 기본 생성자: 매개변수 없이 객체 생성 가능
- 매개변수 생성자: 브랜드, 모델, 제조년도를 받아 객체 초기화

**메서드:**
- `toString()`: 객체를 문자열로 표현 (자동 호출)
- `getAge()`: 현재 연도와 제조년도의 차이를 계산하여 자동차 나이 반환

---

### 📐 메서드별 상세 설명

#### 📝 toString() 메서드

```java
@Override
public String toString() {
    return "브랜드: " + brand + ", 모델: " + model + ", 제조년도: " + year + "년";
}
```

**동작 원리:**
1. `@Override` 어노테이션: 부모 클래스(Object)의 toString() 메서드를 재정의
2. 문자열 연결: `+` 연산자로 문자열과 변수값을 연결
3. 자동 호출: `System.out.println(c1)` 실행 시 자동으로 toString() 호출

---

#### 🚗 getAge() 메서드

```java
public int getAge(int currentYear) {
    return currentYear - year;
}
```

**동작 원리:**
1. 현재 연도에서 제조년도를 빼서 자동차 나이 계산
2. int 타입으로 반환

**계산 예시:**
```
현대 소나타: 2024년 - 2020년 = 4년
기아 K5: 2024년 - 2022년 = 2년
```

---

## 문제 3: ArrayList를 이용한 점수 관리 정답

### 📁 ScoreManager.java

```java
import java.util.ArrayList;

public class ScoreManager {
    
    // 점수 추가
    public static void addScore(ArrayList<Integer> list, int score) {
        list.add(score);
    }
    
    // 평균 계산
    public static double getAverage(ArrayList<Integer> list) {
        if (list.isEmpty()) {
            return 0.0;
        }
        
        int sum = 0;
        for (int score : list) {
            sum += score;
        }
        
        return (double) sum / list.size();
    }
    
    // 합격자 수 계산
    public static int countPass(ArrayList<Integer> list, int passScore) {
        int count = 0;
        for (int score : list) {
            if (score >= passScore) {
                count++;
            }
        }
        return count;
    }
    
    // 최고 점수 찾기
    public static int getMaxScore(ArrayList<Integer> list) {
        if (list.isEmpty()) {
            return -1;
        }
        
        int max = list.get(0);
        for (int i = 1; i < list.size(); i++) {
            if (list.get(i) > max) {
                max = list.get(i);
            }
        }
        return max;
    }
}
```

---

### 📁 Problem3.java (실행 파일)

```java
import java.util.ArrayList;

public class Problem3 {
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();
        
        ScoreManager.addScore(list, 85);
        ScoreManager.addScore(list, 92);
        ScoreManager.addScore(list, 78);
        ScoreManager.addScore(list, 65);
        ScoreManager.addScore(list, 88);
        
        System.out.println("평균: " + ScoreManager.getAverage(list));
        System.out.println("합격자 수 (60점 이상): " + ScoreManager.countPass(list, 60));
        System.out.println("최고 점수: " + ScoreManager.getMaxScore(list));
    }
}
```

---

### 🔍 코드 설명

#### ScoreManager 클래스 구조

**메서드:**
- `addScore()`: 점수를 리스트에 추가
- `getAverage()`: 평균 점수 계산
- `countPass()`: 합격자 수 계산
- `getMaxScore()`: 최고 점수 찾기

**특징:**
- 모든 메서드가 `static`으로 선언됨 (객체 생성 없이 호출 가능)
- ArrayList<Integer>를 매개변수로 받아 처리

---

### 📐 메서드별 상세 설명

#### ➕ addScore() - 점수 추가

```java
public static void addScore(ArrayList<Integer> list, int score) {
    list.add(score);
}
```

**동작 순서:**
1. `list.add(score)`로 ArrayList에 점수 추가
2. 리스트 크기가 자동으로 증가

---

#### 📊 getAverage() - 평균 계산

```java
public static double getAverage(ArrayList<Integer> list) {
    if (list.isEmpty()) {              // 리스트가 비어있으면
        return 0.0;                     // 0.0 반환
    }
    
    int sum = 0;
    for (int score : list) {           // 모든 점수 합산
        sum += score;
    }
    
    return (double) sum / list.size();  // 평균 계산 (형변환 필요)
}
```

**동작 순서:**
1. 빈 리스트 체크 (`isEmpty()`)
2. 모든 점수를 합산
3. 합계를 개수로 나누어 평균 계산
4. 정수 나눗셈을 피하기 위해 `(double)` 캐스팅

**계산 예시:**
```
점수: [85, 92, 78, 65, 88]
합계: 85 + 92 + 78 + 65 + 88 = 408
평균: 408 / 5 = 81.6
```

---

#### ✅ countPass() - 합격자 수 계산

```java
public static int countPass(ArrayList<Integer> list, int passScore) {
    int count = 0;                     // 합격자 수 카운터
    for (int score : list) {           // 모든 점수 순회
        if (score >= passScore) {       // 합격 점수 이상이면
            count++;                    // 카운터 증가
        }
    }
    return count;                       // 합격자 수 반환
}
```

**동작 순서:**
1. 합격자 수를 저장할 변수 초기화 (0)
2. 모든 점수를 순회하며 합격 점수 이상인지 확인
3. 조건을 만족하면 카운터 증가
4. 합격자 수 반환

**예시:**
```
점수: [85, 92, 78, 65, 88], 합격 점수: 60
85 >= 60 ✓, 92 >= 60 ✓, 78 >= 60 ✓, 65 >= 60 ✓, 88 >= 60 ✓
합격자 수: 5명
```

---

#### 📈 getMaxScore() - 최고 점수 찾기

```java
public static int getMaxScore(ArrayList<Integer> list) {
    if (list.isEmpty()) {               // 리스트가 비어있으면
        return -1;                      // -1 반환
    }
    
    int max = list.get(0);              // 첫 번째 값을 최고 점수로 초기화
    for (int i = 1; i < list.size(); i++) {  // 두 번째부터 순회
        if (list.get(i) > max) {        // 현재 값이 최고 점수보다 크면
            max = list.get(i);          // 최고 점수 갱신
        }
    }
    return max;                         // 최고 점수 반환
}
```

**동작 순서:**
1. 빈 리스트 체크
2. 첫 번째 값을 최고 점수로 초기화
3. 두 번째 값부터 마지막까지 순회하며 비교
4. 더 큰 값을 찾으면 최고 점수 갱신
5. 최고 점수 반환

---

## 문제 4: 문자열 처리 유틸리티 정답

### 📁 StringUtil.java

```java
public class StringUtil {
    
    // 모음 개수 세기
    public static int countVowels(String text) {
        if (text == null) {
            return 0;
        }
        
        int count = 0;
        String lowerText = text.toLowerCase();  // 소문자로 변환하여 대소문자 구분 없이 처리
        for (int i = 0; i < lowerText.length(); i++) {
            char ch = lowerText.charAt(i);
            if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
                count++;
            }
        }
        return count;
    }
    
    // 대문자 변환
    public static String toUpperCase(String text) {
        if (text == null) {
            return null;
        }
        
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < text.length(); i++) {
            char ch = text.charAt(i);
            if (ch >= 'a' && ch <= 'z') {
                sb.append((char)(ch - 32));  // 소문자를 대문자로 변환
            } else {
                sb.append(ch);
            }
        }
        return sb.toString();
    }
    
    // 단어 포함 확인
    public static boolean containsWord(String text, String word) {
        if (text == null || word == null) {
            return false;
        }
        
        return text.contains(word);
    }
    
    // 문자 교체
    public static String replaceChar(String text, char oldChar, char newChar) {
        if (text == null) {
            return null;
        }
        
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < text.length(); i++) {
            char ch = text.charAt(i);
            if (ch == oldChar) {
                sb.append(newChar);
            } else {
                sb.append(ch);
            }
        }
        return sb.toString();
    }
}
```

---

### 📁 Problem4.java (실행 파일)

```java
public class Problem4 {
    public static void main(String[] args) {
        // 1. 모음 개수 세기
        System.out.println("모음 개수: " + StringUtil.countVowels("Hello"));
        
        // 2. 대문자 변환
        System.out.println("대문자: " + StringUtil.toUpperCase("hello world"));
        
        // 3. 단어 포함 확인
        System.out.println("World 포함: " + StringUtil.containsWord("Hello World", "World"));
        System.out.println("Java 포함: " + StringUtil.containsWord("Hello World", "Java"));
        
        // 4. 문자 교체
        System.out.println("문자 교체: " + StringUtil.replaceChar("Hello", 'l', 'L'));
    }
}
```

---

### 🔍 코드 설명

#### StringUtil 클래스 구조

**메서드:**
- `countVowels()`: 문자열에서 모음 개수 세기
- `toUpperCase()`: 문자열을 대문자로 변환
- `containsWord()`: 단어 포함 여부 확인
- `replaceChar()`: 문자 교체

**특징:**
- 모든 메서드가 `static`으로 선언됨
- null 체크 포함

---

### 📐 메서드별 상세 설명

#### 🔤 countVowels() - 모음 개수 세기

```java
public static int countVowels(String text) {
    if (text == null) {
        return 0;
    }
    
    int count = 0;
    String lowerText = text.toLowerCase();  // 소문자로 변환
    for (int i = 0; i < lowerText.length(); i++) {
        char ch = lowerText.charAt(i);
        if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
            count++;
        }
    }
    return count;
}
```

**동작 순서:**
1. null 체크
2. 문자열을 소문자로 변환 (`toLowerCase()`)
3. 모든 문자를 순회하며 모음인지 확인
4. 모음이면 카운터 증가
5. 카운터 반환

**예시:**
```java
countVowels("Hello")  // → 2 ('e', 'o')
countVowels("Java")   // → 2 ('a', 'a')
```

---

#### 🔤 toUpperCase() - 대문자 변환

```java
public static String toUpperCase(String text) {
    if (text == null) {
        return null;
    }
    
    StringBuilder sb = new StringBuilder();
    for (int i = 0; i < text.length(); i++) {
        char ch = text.charAt(i);
        if (ch >= 'a' && ch <= 'z') {        // 소문자이면
            sb.append((char)(ch - 32));      // 대문자로 변환 (ASCII 코드 차이: 32)
        } else {
            sb.append(ch);                   // 그 외는 그대로 추가
        }
    }
    return sb.toString();
}
```

**동작 순서:**
1. null 체크
2. StringBuilder 객체 생성
3. 문자열의 모든 문자를 순회
4. 소문자('a'~'z')이면 대문자로 변환 (ASCII 코드: 소문자 - 32 = 대문자)
5. StringBuilder를 String으로 변환하여 반환

**예시:**
```java
toUpperCase("hello")      // → "HELLO"
toUpperCase("hello world") // → "HELLO WORLD"
```

---

#### 🔍 containsWord() - 단어 포함 확인

```java
public static boolean containsWord(String text, String word) {
    if (text == null || word == null) {
        return false;
    }
    
    return text.contains(word);
}
```

**동작 순서:**
1. null 체크 (text와 word 모두)
2. `contains()` 메서드를 사용하여 단어 포함 여부 확인
3. 포함되면 true, 아니면 false 반환

**예시:**
```java
containsWord("Hello World", "World")  // → true
containsWord("Hello World", "Java")   // → false
```

---

#### 🔄 replaceChar() - 문자 교체

```java
public static String replaceChar(String text, char oldChar, char newChar) {
    if (text == null) {
        return null;
    }
    
    StringBuilder sb = new StringBuilder();
    for (int i = 0; i < text.length(); i++) {
        char ch = text.charAt(i);
        if (ch == oldChar) {           // 교체할 문자와 같으면
            sb.append(newChar);        // 새 문자 추가
        } else {
            sb.append(ch);            // 그 외는 원래 문자 추가
        }
    }
    return sb.toString();
}
```

**동작 순서:**
1. null 체크
2. StringBuilder 객체 생성
3. 문자열의 모든 문자를 순회
4. 교체할 문자와 같으면 새 문자로 교체
5. 그 외는 원래 문자 그대로 추가
6. StringBuilder를 String으로 변환하여 반환

**예시:**
```java
replaceChar("Hello", 'l', 'L')  // → "HeLLo"
replaceChar("Java", 'a', 'A')   // → "JAvA"
```

---

## 전체 실행 파일 (통합 테스트용)

### Problem1.java
```java
import java.util.ArrayList;
import java.util.Scanner;

public class Problem1 {
    // Book 클래스와 BookApp 클래스는 위의 정답 코드 참조
    // (Book.java와 BookApp.java의 내용을 그대로 사용)
}
```

### Problem2.java
```java
public class Problem2 {
    public static void main(String[] args) {
        Car c1 = new Car("현대", "소나타", 2020);
        Car c2 = new Car("기아", "K5", 2022);
        
        System.out.println(c1);
        System.out.println("나이: " + c1.getAge(2024) + "년");
        System.out.println(c2);
        System.out.println("나이: " + c2.getAge(2024) + "년");
    }
}
```

### Problem3.java
```java
import java.util.ArrayList;

public class Problem3 {
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>();
        
        ScoreManager.addScore(list, 85);
        ScoreManager.addScore(list, 92);
        ScoreManager.addScore(list, 78);
        ScoreManager.addScore(list, 65);
        ScoreManager.addScore(list, 88);
        
        System.out.println("평균: " + ScoreManager.getAverage(list));
        System.out.println("합격자 수 (60점 이상): " + ScoreManager.countPass(list, 60));
        System.out.println("최고 점수: " + ScoreManager.getMaxScore(list));
    }
}
```

### Problem4.java
```java
public class Problem4 {
    public static void main(String[] args) {
        // 1. 모음 개수 세기
        System.out.println("모음 개수: " + StringUtil.countVowels("Hello"));
        
        // 2. 대문자 변환
        System.out.println("대문자: " + StringUtil.toUpperCase("hello world"));
        
        // 3. 단어 포함 확인
        System.out.println("World 포함: " + StringUtil.containsWord("Hello World", "World"));
        System.out.println("Java 포함: " + StringUtil.containsWord("Hello World", "Java"));
        
        // 4. 문자 교체
        System.out.println("문자 교체: " + StringUtil.replaceChar("Hello", 'l', 'L'));
    }
}
```

---

## 채점 기준 상세

### 문제 1 (20점)
- Book 클래스 작성: 5점
- 메뉴 시스템 구현: 5점
- findBook() 메서드: 3점
- updateBook() 메서드: 3점
- deleteBook() 메서드: 2점
- 예외 처리: 2점

### 문제 2 (20점)
- 필드 선언 (brand, model, year): 5점
- 기본 생성자: 2점
- 매개변수 생성자: 3점
- toString() 메서드: 5점
- getAge() 메서드: 5점

### 문제 3 (20점)
- addScore() 메서드: 5점
- getAverage() 메서드 (빈 리스트 체크, 평균 계산): 5점
- countPass() 메서드 (조건 확인): 5점
- getMaxScore() 메서드 (빈 리스트 체크, 최고 점수 찾기): 5점

### 문제 4 (20점)
- countVowels() 메서드 (null 체크, 모음 개수 세기): 5점
- toUpperCase() 메서드 (null 체크, 대문자 변환): 5점
- containsWord() 메서드 (null 체크, 단어 포함 확인): 5점
- replaceChar() 메서드 (null 체크, 문자 교체): 5점

---

## 추가 설명

### 문제 1 - 예외 처리
- 메뉴 입력: `try-catch`로 숫자 입력 예외 처리
- `continue`: 예외 발생 시 다시 메뉴로 돌아가기
- `default`: switch문에서 잘못된 메뉴 번호 처리

### 문제 2 - getAge() 메서드
- 간단한 뺄셈 연산: `currentYear - year`
- 반환 타입: int

### 문제 3 - getAverage() 메서드
- 빈 리스트 체크 필수 (`isEmpty()`)
- 정수 나눗셈을 피하기 위해 `(double)` 캐스팅 필요
- 형변환: `(double) sum / list.size()`

### 문제 3 - countPass() 메서드
- 조건문 사용: `score >= passScore`
- 합격 점수 이상인 점수만 카운트

### 문제 4 - countVowels() 메서드
- `toLowerCase()` 사용하여 대소문자 구분 없이 처리
- 모음: a, e, i, o, u (5개)

### 문제 4 - toUpperCase() 메서드
- ASCII 코드 활용: 소문자 - 32 = 대문자
- 'a' = 97, 'A' = 65 (차이: 32)
- 직접 구현하는 방법 (String의 toUpperCase() 메서드 사용 가능하지만 직접 구현)

### 문제 4 - containsWord() 메서드
- String의 `contains()` 메서드 사용
- null 체크 필수

### 문제 4 - replaceChar() 메서드
- `charAt()`으로 각 문자 확인
- 조건문으로 문자 비교 후 교체
- String의 `replace()` 메서드 사용 가능하지만 직접 구현하는 것이 요구사항
