# String 비교: == vs equals() 시각화

## 📌 핵심 개념

- **`==` 연산자**: 참조(주소) 비교 → 같은 객체를 가리키는지 확인
- **`equals()` 메서드**: 내용 비교 → 문자열의 값이 같은지 확인

---

## 🎨 메모리 구조 시각화

### 코드

```java
String str1 = "Hello";
String str2 = "Hello";
String str3 = new String("Hello");
```

### 메모리 구조 그림

```
┌─────────────────────────────────────────────────────────┐
│                    힙(Heap) 메모리                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────┐                                     │
│  │ String Pool     │                                     │
│  │ (문자열 상수 풀) │                                     │
│  ├─────────────────┤                                     │
│  │ "Hello" ────────┼──┐                                  │
│  └─────────────────┘  │                                  │
│                       │                                  │
│  ┌─────────────────┐  │                                  │
│  │ 일반 힙 영역     │  │                                  │
│  ├─────────────────┤  │                                  │
│  │ new String()   │  │                                  │
│  │ "Hello" ────────┼──┼──┐                               │
│  └─────────────────┘  │  │                               │
│                       │  │                               │
└───────────────────────┼──┼───────────────────────────────┘
                        │  │
┌───────────────────────┼──┼───────────────────────────────┐
│              스택(Stack) 메모리                           │
├───────────────────────┼──┼───────────────────────────────┤
│                       │  │                               │
│  str1 ────────────────┘  │                               │
│  (참조 변수)              │                               │
│                          │                               │
│  str2 ───────────────────┘                               │
│  (참조 변수)                                              │
│                                                           │
│  str3 ────────────────────────────────────────────────────┘
│  (참조 변수)                                              │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## 🔍 상세 설명

### 1. String 리터럴 방식 (`str1`, `str2`)

```java
String str1 = "Hello";
String str2 = "Hello";
```

**메모리 구조:**

```
┌─────────────────────────────────────┐
│         String Pool (힙)            │
│                                     │
│  ┌──────────┐                       │
│  │ "Hello"  │ ← str1, str2 모두 여기 참조
│  └──────────┘                       │
│                                     │
└─────────────────────────────────────┘
         ▲           ▲
         │           │
    ┌────┘           └────┐
    │                    │
┌───┴───┐          ┌────┴───┐
│ str1  │          │  str2  │
└───────┘          └────────┘
(스택)              (스택)
```

**특징:**
- `"Hello"`는 **String Pool**에 저장됨
- 같은 문자열 리터럴은 **같은 객체를 참조**
- `str1`과 `str2`는 **같은 주소**를 가리킴

### 2. new String() 방식 (`str3`)

```java
String str3 = new String("Hello");
```

**메모리 구조:**

```
┌─────────────────────────────────────┐
│         String Pool (힙)            │
│                                     │
│  ┌──────────┐                       │
│  │ "Hello"  │ ← str1, str2 참조     │
│  └──────────┘                       │
│                                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│      일반 힙 영역                    │
│                                     │
│  ┌──────────┐                       │
│  │ "Hello"  │ ← str3 참조 (새 객체)  │
│  └──────────┘                       │
│                                     │
└─────────────────────────────────────┘
         ▲
         │
    ┌────┘
    │
┌───┴───┐
│ str3  │
└───────┘
(스택)
```

**특징:**
- `new String()`은 **항상 새로운 객체**를 생성
- String Pool과는 **별도의 메모리 공간**에 저장
- `str3`는 **다른 주소**를 가리킴

---

## ⚖️ 비교 연산 결과

### 전체 메모리 구조 통합

```
                    ┌─────────────────┐
                    │   String Pool   │
                    │                 │
                    │  ┌───────────┐  │
                    │  │ "Hello"   │  │ ← str1, str2 참조
                    │  └───────────┘  │
                    └─────────────────┘
                           ▲ ▲
                           │ │
                    ┌──────┘ └──────┐
                    │               │
              ┌─────┴───┐      ┌─────┴───┐
              │  str1   │      │  str2   │
              └─────────┘      └─────────┘
              주소: 0x100      주소: 0x100
              (같은 주소!)     (같은 주소!)

                    ┌─────────────────┐
                    │   일반 힙 영역  │
                    │                 │
                    │  ┌───────────┐  │
                    │  │ "Hello"   │  │ ← str3 참조
                    │  └───────────┘  │
                    └─────────────────┘
                           ▲
                           │
                    ┌──────┘
                    │
              ┌─────┴───┐
              │  str3   │
              └─────────┘
              주소: 0x200
              (다른 주소!)
```

### 비교 결과 분석

```java
// == 연산자: 참조 비교 (주소 비교)
System.out.println(str1 == str2);        // true
System.out.println(str1 == str3);       // false

// equals() 메서드: 내용 비교 (값 비교)
System.out.println(str1.equals(str2));  // true
System.out.println(str1.equals(str3));  // true
```

**비교 과정:**

#### 1️⃣ `str1 == str2` → `true`

```
str1 ──→ [주소: 0x100] ──→ "Hello"
                              ▲
str2 ──→ [주소: 0x100] ───────┘

비교: 0x100 == 0x100 → true ✅
(같은 주소를 가리키므로 true)
```

#### 2️⃣ `str1 == str3` → `false`

```
str1 ──→ [주소: 0x100] ──→ "Hello"

str3 ──→ [주소: 0x200] ──→ "Hello"

비교: 0x100 == 0x200 → false ❌
(다른 주소를 가리키므로 false)
```

#### 3️⃣ `str1.equals(str2)` → `true`

```
str1 ──→ "Hello"
str2 ──→ "Hello"

비교: "Hello".equals("Hello") → true ✅
(내용이 같으므로 true)
```

#### 4️⃣ `str1.equals(str3)` → `true`

```
str1 ──→ "Hello"
str3 ──→ "Hello"

비교: "Hello".equals("Hello") → true ✅
(내용이 같으므로 true, 주소는 무관)
```

---

## 📊 비교표

| 비교 방법 | str1 == str2 | str1 == str3 | str1.equals(str2) | str1.equals(str3) |
|----------|-------------|-------------|-------------------|-------------------|
| **비교 대상** | 주소 | 주소 | 내용 | 내용 |
| **결과** | `true` | `false` | `true` | `true` |
| **이유** | 같은 String Pool 객체 | 다른 객체 | 같은 문자열 | 같은 문자열 |

---

## 🎯 핵심 정리

### == 연산자 (참조 비교)

```
== 연산자는 변수가 가리키는 "주소"를 비교합니다.

str1 == str2
  ↓
주소 비교: 0x100 == 0x100 → true

str1 == str3
  ↓
주소 비교: 0x100 == 0x200 → false
```

**시각화:**
```
== 비교는 화살표(참조)를 비교하는 것!

str1 ──→ [같은 객체] ←── str2  → true
str1 ──→ [객체1]              → false
str3 ──→ [객체2]
```

### equals() 메서드 (내용 비교)

```
equals() 메서드는 문자열의 "내용"을 비교합니다.

str1.equals(str3)
  ↓
내용 비교: "Hello" == "Hello" → true
(주소는 무관, 내용만 확인)
```

**시각화:**
```
equals() 비교는 박스 안의 내용을 비교하는 것!

str1 ──→ [ "Hello" ]
str3 ──→ [ "Hello" ]
         ↑
      내용이 같음 → true
```

---

## 💡 실전 예제

### 예제 1: String 리터럴 비교

```java
String a = "Java";
String b = "Java";
System.out.println(a == b);        // true (같은 String Pool)
System.out.println(a.equals(b));  // true
```

**메모리:**
```
String Pool: "Java" ← a, b 모두 참조
```

### 예제 2: new String() 비교

```java
String a = "Java";
String b = new String("Java");
System.out.println(a == b);        // false (다른 객체)
System.out.println(a.equals(b));  // true (내용은 같음)
```

**메모리:**
```
String Pool: "Java" ← a 참조
일반 힙: "Java" ← b 참조 (새 객체)
```

### 예제 3: intern() 메서드

```java
String a = "Java";
String b = new String("Java").intern();
System.out.println(a == b);        // true (intern()으로 Pool에 등록)
```

**메모리:**
```
intern() 호출 후:
String Pool: "Java" ← a, b 모두 참조
```

---

## ⚠️ 주의사항

### ❌ 잘못된 사용

```java
String str = null;
if (str == "Hello") {  // NullPointerException 위험
    // ...
}
```

### ✅ 올바른 사용

```java
String str = null;
if ("Hello".equals(str)) {  // 안전 (null 체크 불필요)
    // ...
}

// 또는
if (str != null && str.equals("Hello")) {
    // ...
}
```

---

## 🔄 equals() 메서드 동작 원리

```java
public boolean equals(Object obj) {
    // 1. 같은 객체인지 확인
    if (this == obj) return true;
    
    // 2. null 체크
    if (obj == null) return false;
    
    // 3. 타입 확인
    if (!(obj instanceof String)) return false;
    
    // 4. 내용 비교
    String other = (String) obj;
    return this.value.equals(other.value);
}
```

**시각화:**
```
equals() 메서드 실행 과정:

str1.equals(str3)
  ↓
1. this == obj? → false (다른 객체)
  ↓
2. obj == null? → false
  ↓
3. obj instanceof String? → true
  ↓
4. this.value == "Hello"
   other.value == "Hello"
   → "Hello" == "Hello" → true ✅
```

---

## 📝 요약

### 메모리 구조 요약

```
┌─────────────────────────────────────────────┐
│              String Pool                     │
│  "Hello" ← str1, str2 (같은 주소)            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│            일반 힙 영역                       │
│  "Hello" ← str3 (다른 주소)                  │
└─────────────────────────────────────────────┘
```

### 비교 결과 요약

| 비교 | 결과 | 이유 |
|------|------|------|
| `str1 == str2` | `true` | 같은 String Pool 객체 |
| `str1 == str3` | `false` | 다른 객체 (주소 다름) |
| `str1.equals(str2)` | `true` | 내용 같음 |
| `str1.equals(str3)` | `true` | 내용 같음 |

### 권장 사항

✅ **문자열 비교는 항상 `equals()` 사용!**

```java
// ✅ 권장
if (str1.equals(str2)) { ... }

// ❌ 비권장 (의도치 않은 버그 가능)
if (str1 == str2) { ... }
```

---

## 🎓 학습 체크리스트

- [ ] `==` 연산자가 주소를 비교한다는 것을 이해했다
- [ ] `equals()` 메서드가 내용을 비교한다는 것을 이해했다
- [ ] String Pool의 개념을 이해했다
- [ ] `new String()`이 새로운 객체를 만든다는 것을 이해했다
- [ ] 문자열 비교 시 `equals()`를 사용해야 하는 이유를 이해했다


