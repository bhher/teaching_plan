# 자바 작업형 평가 문제 정답

---

## 문제 1: Student 클래스 작성

### 📁 Student.java

```java
public class Student {
    String name;   // 학생 이름
    int age;       // 학생 나이
    int score;     // 학생 점수
    
    // 기본 생성자
    public Student() {
    }
    
    // 매개변수 생성자
    public Student(String name, int age, int score) {
        this.name = name;
        this.age = age;
        this.score = score;
    }
    
    // toString() 메서드: 학생 정보를 문자열로 반환
    @Override
    public String toString() {
        return "이름: " + name + ", 나이: " + age + ", 점수: " + score;
    }
    
    // 등급 반환 메서드
    public String getGrade() {
        if (score >= 90) {
            return "A";
        } else if (score >= 80) {
            return "B";
        } else if (score >= 70) {
            return "C";
        } else if (score >= 60) {
            return "D";
        } else {
            return "F";
        }
    }
}
```

---

### 📁 Problem1.java (실행 파일)

```java
public class Problem1 {
    public static void main(String[] args) {
        Student s1 = new Student("홍길동", 20, 85);
        Student s2 = new Student("김철수", 22, 95);
        
        System.out.println(s1);
        System.out.println("등급: " + s1.getGrade());
        System.out.println(s2);
        System.out.println("등급: " + s2.getGrade());
    }
}
```

---

### 🔍 코드 설명

#### 1. Student 클래스 구조

**필드:**
- `name` (String): 학생 이름 저장
- `age` (int): 학생 나이 저장
- `score` (int): 학생 점수 저장

**생성자:**
- 기본 생성자: 매개변수 없이 객체 생성 가능
- 매개변수 생성자: 이름, 나이, 점수를 받아 객체 초기화

**메서드:**
- `toString()`: 객체를 문자열로 표현 (자동 호출)
- `getGrade()`: 점수에 따라 등급 반환

---

### 📐 메서드별 상세 설명

#### 📝 toString() 메서드

```java
@Override
public String toString() {
    return "이름: " + name + ", 나이: " + age + ", 점수: " + score;
}
```

**동작 원리:**
1. `@Override` 어노테이션: 부모 클래스(Object)의 toString() 메서드를 재정의
2. 문자열 연결: `+` 연산자로 문자열과 변수값을 연결
3. 자동 호출: `System.out.println(s1)` 실행 시 자동으로 toString() 호출

**사용 예시:**
```java
Student s = new Student("홍길동", 20, 85);
System.out.println(s);  // toString() 자동 호출
// 출력: 이름: 홍길동, 나이: 20, 점수: 85
```

---

#### 🎯 getGrade() 메서드

```java
public String getGrade() {
    if (score >= 90) {
        return "A";
    } else if (score >= 80) {
        return "B";
    } else if (score >= 70) {
        return "C";
    } else if (score >= 60) {
        return "D";
    } else {
        return "F";
    }
}
```

**동작 순서:**
1. 점수가 90 이상인지 확인 → "A" 반환
2. 아니면 80 이상인지 확인 → "B" 반환
3. 아니면 70 이상인지 확인 → "C" 반환
4. 아니면 60 이상인지 확인 → "D" 반환
5. 모두 아니면 → "F" 반환

**⚠️ 주의사항:**
- 조건문의 순서가 중요 (높은 점수부터 확인)
- `else if`를 사용하여 하나의 조건만 실행되도록 함

**점수 범위별 등급:**
```
90점 이상: A
80점 이상 90점 미만: B
70점 이상 80점 미만: C
60점 이상 70점 미만: D
60점 미만: F
```

---

### 🔄 전체 실행 흐름도

```
프로그램 시작
    ↓
Student s1 = new Student("홍길동", 20, 85)
    ↓
생성자 호출 → name="홍길동", age=20, score=85 저장
    ↓
System.out.println(s1)
    ↓
toString() 자동 호출
    ↓
"이름: 홍길동, 나이: 20, 점수: 85" 출력
    ↓
s1.getGrade() 호출
    ↓
score(85) >= 80 확인 → "B" 반환
    ↓
"등급: B" 출력
```

---

### 🎯 핵심 포인트

1. **클래스와 객체**
   - 클래스: 객체의 설계도
   - 객체: 클래스를 통해 생성된 인스턴스

2. **생성자**
   - 기본 생성자: 매개변수 없음
   - 매개변수 생성자: 초기값을 받아 필드 설정
   - `this` 키워드: 현재 객체를 가리킴

3. **toString() 메서드**
   - 객체를 문자열로 표현
   - `System.out.println()`에서 자동 호출
   - `@Override`로 재정의 표시

4. **조건문**
   - `if-else if-else` 구조
   - 조건 순서가 중요 (높은 값부터 확인)

---

### 🔍 실행 결과 예시

```
이름: 홍길동, 나이: 20, 점수: 85
등급: B
이름: 김철수, 나이: 22, 점수: 95
등급: A
```

---

### 🎓 학습 포인트

#### ✅ 이해해야 할 개념
1. **클래스 선언**
   - 필드 선언 방법
   - 생성자 작성 방법

2. **메서드 작성**
   - 반환 타입 지정
   - 매개변수 사용

3. **조건문 활용**
   - if-else if 구조
   - 조건 순서의 중요성

#### ✅ 주의할 점
1. **this 키워드**
   - 필드와 매개변수 이름이 같을 때 구분
   - `this.name = name` 형태로 사용

2. **toString() 재정의**
   - `@Override` 어노테이션 사용
   - 반환 타입은 String

3. **등급 계산**
   - 조건문 순서 (높은 점수부터)
   - 경계값 처리 (90점은 A, 89점은 B)

---

## 문제 2: ArrayList를 이용한 학생 관리

### 📁 StudentManager.java

```java
import java.util.ArrayList;

public class StudentManager {
    
    // 학생 추가
    public static void addStudent(ArrayList<Student> list, String name, int age, int score) {
        list.add(new Student(name, age, score));
    }
    
    // 학생 검색
    public static Student findStudent(ArrayList<Student> list, String name) {
        for (Student s : list) {
            if (s.name.equals(name)) {
                return s;
            }
        }
        return null;
    }
    
    // 평균 점수 계산
    public static double getAverageScore(ArrayList<Student> list) {
        if (list.isEmpty()) {
            return 0.0;
        }
        
        int sum = 0;
        for (Student s : list) {
            sum += s.score;
        }
        
        return (double) sum / list.size();
    }
    
    // 전체 출력
    public static void printAll(ArrayList<Student> list) {
        for (Student s : list) {
            System.out.println(s);
        }
    }
}
```

---

### 📁 Problem2.java (실행 파일)

```java
import java.util.ArrayList;

public class Problem2 {
    public static void main(String[] args) {
        ArrayList<Student> list = new ArrayList<>();
        
        StudentManager.addStudent(list, "홍길동", 20, 85);
        StudentManager.addStudent(list, "김철수", 22, 95);
        StudentManager.addStudent(list, "이영희", 21, 75);
        
        System.out.println("=== 전체 학생 정보 ===");
        StudentManager.printAll(list);
        
        System.out.println("\n=== 학생 검색 ===");
        Student s = StudentManager.findStudent(list, "김철수");
        if (s != null) {
            System.out.println("찾은 학생: " + s);
        }
        
        System.out.println("\n=== 평균 점수 ===");
        double avg = StudentManager.getAverageScore(list);
        System.out.println("평균: " + avg);
    }
}
```

---

### 🔍 코드 설명

#### 1. StudentManager 클래스 구조

**메서드:**
- `addStudent()`: 학생을 리스트에 추가
- `findStudent()`: 이름으로 학생 검색
- `getAverageScore()`: 평균 점수 계산
- `printAll()`: 전체 학생 정보 출력

**특징:**
- 모든 메서드가 `static`으로 선언됨 (객체 생성 없이 호출 가능)
- ArrayList를 매개변수로 받아 처리

---

### 📐 메서드별 상세 설명

#### ➕ addStudent() - 학생 추가

```java
public static void addStudent(ArrayList<Student> list, String name, int age, int score) {
    list.add(new Student(name, age, score));
}
```

**동작 순서:**
1. Student 객체 생성 (name, age, score로 초기화)
2. `list.add()`로 ArrayList에 추가
3. 리스트 크기가 자동으로 증가

**사용 예시:**
```java
ArrayList<Student> list = new ArrayList<>();
addStudent(list, "홍길동", 20, 85);
// list에 Student("홍길동", 20, 85) 객체 추가됨
```

---

#### 🔎 findStudent() - 학생 검색

```java
public static Student findStudent(ArrayList<Student> list, String name) {
    for (Student s : list) {              // 향상된 for문으로 리스트 순회
        if (s.name.equals(name)) {         // 이름이 일치하면
            return s;                       // 해당 Student 객체 반환
        }
    }
    return null;                           // 찾지 못하면 null 반환
}
```

**동작 순서:**
1. 리스트의 첫 번째 요소부터 마지막까지 순회
2. 각 Student 객체의 name 필드와 검색할 name 비교
3. 일치하는 객체를 찾으면 즉시 반환 (선형 검색)
4. 찾지 못하면 null 반환

**⚠️ 주의사항:**
- 문자열 비교는 `equals()` 사용 (== 사용 금지)
- null 체크 필요 (호출하는 쪽에서 처리)

**사용 예시:**
```java
Student s = findStudent(list, "김철수");
if (s != null) {
    System.out.println("찾은 학생: " + s);
} else {
    System.out.println("학생을 찾을 수 없습니다");
}
```

---

#### 📊 getAverageScore() - 평균 점수 계산

```java
public static double getAverageScore(ArrayList<Student> list) {
    if (list.isEmpty()) {                  // 리스트가 비어있으면
        return 0.0;                        // 0.0 반환
    }
    
    int sum = 0;
    for (Student s : list) {               // 모든 학생의 점수 합산
        sum += s.score;
    }
    
    return (double) sum / list.size();     // 평균 계산 (형변환 필요)
}
```

**동작 순서:**
1. 리스트가 비어있는지 확인 (`isEmpty()`)
2. 비어있으면 0.0 반환
3. 모든 학생의 점수를 합산
4. 합계를 학생 수로 나누어 평균 계산
5. 정수 나눗셈을 피하기 위해 `(double)` 캐스팅

**⚠️ 주의사항:**
- 빈 리스트 체크 필수 (0으로 나누기 방지)
- 정수 나눗셈을 피하기 위해 형변환 필요
- `(double) sum` 또는 `sum * 1.0` 사용

**계산 예시:**
```
학생 3명: 85점, 95점, 75점
합계: 85 + 95 + 75 = 255
평균: 255 / 3 = 85.0
```

---

#### 📋 printAll() - 전체 출력

```java
public static void printAll(ArrayList<Student> list) {
    for (Student s : list) {              // 향상된 for문으로 순회
        System.out.println(s);             // toString() 자동 호출
    }
}
```

**동작 순서:**
1. 리스트의 모든 요소를 순회
2. 각 Student 객체를 출력 (`toString()` 자동 호출)
3. 한 줄에 하나씩 출력

**사용 예시:**
```java
printAll(list);
// 출력:
// 이름: 홍길동, 나이: 20, 점수: 85
// 이름: 김철수, 나이: 22, 점수: 95
// 이름: 이영희, 나이: 21, 점수: 75
```

---

### 🔄 전체 실행 흐름도

```
프로그램 시작
    ↓
ArrayList<Student> list 생성
    ↓
addStudent(list, "홍길동", 20, 85)
    ↓
new Student("홍길동", 20, 85) 생성
    ↓
list.add()로 추가
    ↓
(김철수, 이영희도 동일하게 추가)
    ↓
printAll(list) 호출
    ↓
리스트 순회하며 각 학생 정보 출력
    ↓
findStudent(list, "김철수") 호출
    ↓
리스트 순회하며 이름 비교
    ↓
일치하는 Student 객체 반환
    ↓
getAverageScore(list) 호출
    ↓
모든 점수 합산 후 평균 계산
    ↓
결과 출력
```

---

### 🎯 핵심 포인트

1. **ArrayList 활용**
   - 동적 크기: 필요에 따라 크기가 자동으로 변함
   - 객체 저장: Student 객체들을 저장
   - `add()`: 요소 추가
   - `isEmpty()`: 비어있는지 확인
   - `size()`: 요소 개수 반환

2. **향상된 for문**
   - `for (Student s : list)`: 리스트의 모든 요소 순회
   - 인덱스 없이 직접 요소에 접근

3. **문자열 비교**
   - `==` 연산자 사용 금지
   - `equals()` 메서드 사용 필수

4. **형변환**
   - 정수 나눗셈을 실수로 변환
   - `(double) sum` 또는 `sum * 1.0`

---

### 🔍 실행 결과 예시

```
=== 전체 학생 정보 ===
이름: 홍길동, 나이: 20, 점수: 85
이름: 김철수, 나이: 22, 점수: 95
이름: 이영희, 나이: 21, 점수: 75

=== 학생 검색 ===
찾은 학생: 이름: 김철수, 나이: 22, 점수: 95

=== 평균 점수 ===
평균: 85.0
```

---

### 🎓 학습 포인트

#### ✅ 이해해야 할 개념
1. **ArrayList 사용법**
   - 생성: `new ArrayList<>()`
   - 추가: `add()`
   - 크기: `size()`
   - 비어있음 확인: `isEmpty()`

2. **향상된 for문**
   - `for (타입 변수명 : 컬렉션)`
   - 인덱스 없이 요소에 직접 접근

3. **static 메서드**
   - 객체 생성 없이 호출 가능
   - 클래스명으로 호출: `StudentManager.addStudent()`

#### ✅ 주의할 점
1. **문자열 비교**
   - `==` 사용 금지 (참조 비교)
   - `equals()` 사용 필수 (값 비교)

2. **null 체크**
   - findStudent() 결과가 null일 수 있음
   - 사용 전 null 체크 필요

3. **형변환**
   - 정수 나눗셈 → 실수 결과를 원할 때
   - `(double)` 캐스팅 또는 `* 1.0` 사용

---

## 문제 3: 문자열 처리 및 배열 활용

### 📁 StringUtil.java

```java
public class StringUtil {
    
    // 단어 개수 세기
    public static int countWords(String text) {
        if (text == null || text.trim().isEmpty()) {
            return 0;
        }
        
        String[] words = text.trim().split("\\s+");
        return words.length;
    }
    
    // 문자열 뒤집기
    public static String reverseString(String text) {
        if (text == null) {
            return null;
        }
        
        StringBuilder sb = new StringBuilder();
        for (int i = text.length() - 1; i >= 0; i--) {
            sb.append(text.charAt(i));
        }
        return sb.toString();
    }
    
    // 최댓값 찾기
    public static int findMax(int[] arr) {
        if (arr == null || arr.length == 0) {
            return -1;
        }
        
        int max = arr[0];
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] > max) {
                max = arr[i];
            }
        }
        return max;
    }
    
    // 값 포함 여부 확인
    public static boolean contains(int[] arr, int value) {
        if (arr == null) {
            return false;
        }
        
        for (int num : arr) {
            if (num == value) {
                return true;
            }
        }
        return false;
    }
}
```

---

### 📁 Problem3.java (실행 파일)

```java
public class Problem3 {
    public static void main(String[] args) {
        // 1. 단어 개수 세기
        String text1 = "안녕하세요 자바입니다";
        System.out.println("단어 개수: " + StringUtil.countWords(text1));
        
        // 2. 문자열 뒤집기
        String text2 = "자바";
        System.out.println("뒤집기: " + StringUtil.reverseString(text2));
        
        // 3. 최댓값 찾기
        int[] arr = {10, 5, 20, 8, 15};
        System.out.println("최댓값: " + StringUtil.findMax(arr));
        
        // 4. 값 포함 여부 확인
        System.out.println("15 포함: " + StringUtil.contains(arr, 15));
        System.out.println("100 포함: " + StringUtil.contains(arr, 100));
    }
}
```

---

### 🔍 코드 설명

#### 1. StringUtil 클래스 구조

**메서드:**
- `countWords()`: 문자열에서 단어 개수 세기
- `reverseString()`: 문자열 뒤집기
- `findMax()`: 배열에서 최댓값 찾기
- `contains()`: 배열에 값이 있는지 확인

**특징:**
- 모든 메서드가 `static`으로 선언됨
- null 체크 및 예외 처리 포함

---

### 📐 메서드별 상세 설명

#### 📝 countWords() - 단어 개수 세기

```java
public static int countWords(String text) {
    if (text == null || text.trim().isEmpty()) {
        return 0;
    }
    
    String[] words = text.trim().split("\\s+");
    return words.length;
}
```

**동작 순서:**
1. null 체크 및 빈 문자열 체크
2. `trim()`: 앞뒤 공백 제거
3. `split("\\s+")`: 하나 이상의 공백으로 분리
4. 분리된 배열의 길이 반환

**⚠️ 주의사항:**
- `\\s+`: 하나 이상의 공백 문자 (정규표현식)
- `trim()`: 앞뒤 공백만 제거 (중간 공백은 유지)

**예시:**
```java
countWords("안녕하세요 자바입니다")  // → 2
countWords("  안녕  하세요  ")      // → 2 (trim 후 split)
countWords("")                     // → 0
```

---

#### 🔄 reverseString() - 문자열 뒤집기

```java
public static String reverseString(String text) {
    if (text == null) {
        return null;
    }
    
    StringBuilder sb = new StringBuilder();
    for (int i = text.length() - 1; i >= 0; i--) {
        sb.append(text.charAt(i));
    }
    return sb.toString();
}
```

**동작 순서:**
1. null 체크
2. StringBuilder 객체 생성
3. 문자열의 마지막 문자부터 첫 번째 문자까지 역순으로 추가
4. StringBuilder를 String으로 변환하여 반환

**⚠️ 주의사항:**
- `charAt(i)`: i번째 문자 추출
- `length() - 1`: 마지막 인덱스
- StringBuilder 사용: 문자열 연결 시 효율적

**예시:**
```java
reverseString("자바")        // → "바자"
reverseString("Hello")       // → "olleH"
reverseString("안녕하세요")   // → "요세하녕안"
```

---

#### 📊 findMax() - 최댓값 찾기

```java
public static int findMax(int[] arr) {
    if (arr == null || arr.length == 0) {
        return -1;
    }
    
    int max = arr[0];
    for (int i = 1; i < arr.length; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    return max;
}
```

**동작 순서:**
1. null 체크 및 빈 배열 체크
2. 첫 번째 요소를 최댓값으로 초기화
3. 두 번째 요소부터 마지막까지 순회
4. 현재 요소가 최댓값보다 크면 최댓값 갱신
5. 최댓값 반환

**⚠️ 주의사항:**
- 빈 배열일 때 -1 반환
- 첫 번째 값을 초기값으로 설정

**예시:**
```java
findMax({10, 5, 20, 8, 15})  // → 20
findMax({5})                 // → 5
findMax({})                  // → -1
```

---

#### 🔍 contains() - 값 포함 여부 확인

```java
public static boolean contains(int[] arr, int value) {
    if (arr == null) {
        return false;
    }
    
    for (int num : arr) {
        if (num == value) {
            return true;
        }
    }
    return false;
}
```

**동작 순서:**
1. null 체크
2. 배열의 모든 요소를 순회
3. 요소가 찾는 값과 같으면 true 반환
4. 모두 확인했는데 없으면 false 반환

**⚠️ 주의사항:**
- 향상된 for문 사용 가능
- 찾으면 즉시 반환 (효율적)

**예시:**
```java
contains({10, 5, 20, 8, 15}, 15)  // → true
contains({10, 5, 20, 8, 15}, 100) // → false
```

---

### 🔄 전체 실행 흐름도

```
프로그램 시작
    ↓
countWords("안녕하세요 자바입니다")
    ↓
trim() → "안녕하세요 자바입니다"
    ↓
split("\\s+") → ["안녕하세요", "자바입니다"]
    ↓
words.length → 2 반환
    ↓
reverseString("자바")
    ↓
StringBuilder 생성
    ↓
역순으로 문자 추가: "바" + "자"
    ↓
"바자" 반환
    ↓
findMax({10, 5, 20, 8, 15})
    ↓
max = 10 (초기값)
    ↓
순회하며 비교: 5 < 10, 20 > 10 → max = 20
    ↓
20 반환
```

---

### 🎯 핵심 포인트

1. **문자열 처리**
   - `trim()`: 앞뒤 공백 제거
   - `split()`: 문자열 분리
   - `charAt()`: 특정 위치의 문자 추출
   - `length()`: 문자열 길이

2. **StringBuilder**
   - 문자열 연결 시 효율적
   - `append()`: 문자열 추가
   - `toString()`: String으로 변환

3. **배열 처리**
   - `arr.length`: 배열 길이
   - `arr[i]`: 인덱스로 접근
   - 향상된 for문 사용 가능

4. **예외 처리**
   - null 체크 필수
   - 빈 배열 체크 필요

---

### 🔍 실행 결과 예시

```
단어 개수: 2
뒤집기: 바자
최댓값: 20
15 포함: true
100 포함: false
```

---

### 🎓 학습 포인트

#### ✅ 이해해야 할 개념
1. **문자열 메서드**
   - `trim()`, `split()`, `charAt()`, `length()`
   - 정규표현식 사용 (`\\s+`)

2. **StringBuilder**
   - 문자열 연결 시 사용
   - `append()`, `toString()` 메서드

3. **배열 순회**
   - 일반 for문: 인덱스 사용
   - 향상된 for문: 요소 직접 접근

#### ✅ 주의할 점
1. **null 체크**
   - 모든 메서드에서 null 체크 필요
   - NullPointerException 방지

2. **정규표현식**
   - `\\s+`: 하나 이상의 공백
   - `split()`에서 사용

3. **배열 인덱스**
   - 0부터 시작
   - `length - 1`이 마지막 인덱스

---

## 문제 4: 종합 문제 - 도서 관리 시스템

### 📁 Book.java

```java
public class Book {
    String title;   // 도서 제목
    String author; // 저자
    int price;     // 가격
    
    // 생성자
    public Book(String title, String author, int price) {
        this.title = title;
        this.author = author;
        this.price = price;
    }
    
    // toString() 메서드: 도서 정보를 문자열로 반환
    @Override
    public String toString() {
        return "제목: " + title + ", 저자: " + author + ", 가격: " + price + "원";
    }
}
```

---

### 📁 BookManager.java

```java
import java.util.ArrayList;

public class BookManager {
    
    // 도서 추가
    public static void addBook(ArrayList<Book> list, String title, String author, int price) {
        list.add(new Book(title, author, price));
    }
    
    // 저자로 도서 찾기
    public static ArrayList<Book> findBooksByAuthor(ArrayList<Book> list, String author) {
        ArrayList<Book> result = new ArrayList<>();
        for (Book b : list) {
            if (b.author.equals(author)) {
                result.add(b);
            }
        }
        return result;
    }
    
    // 총 가격 계산
    public static int getTotalPrice(ArrayList<Book> list) {
        int total = 0;
        for (Book b : list) {
            total += b.price;
        }
        return total;
    }
    
    // 도서 삭제
    public static boolean removeBook(ArrayList<Book> list, String title) {
        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).title.equals(title)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }
}
```

---

### 📁 Problem4.java (실행 파일)

```java
import java.util.ArrayList;

public class Problem4 {
    public static void main(String[] args) {
        ArrayList<Book> list = new ArrayList<>();
        
        BookManager.addBook(list, "자바의 정석", "남궁성", 30000);
        BookManager.addBook(list, "혼자 공부하는 자바", "신용권", 28000);
        BookManager.addBook(list, "이것이 자바다", "신용권", 32000);
        
        System.out.println("=== 전체 도서 ===");
        for (Book b : list) {
            System.out.println(b);
        }
        
        System.out.println("\n=== 신용권 저자 도서 ===");
        ArrayList<Book> authorBooks = BookManager.findBooksByAuthor(list, "신용권");
        for (Book b : authorBooks) {
            System.out.println(b);
        }
        
        System.out.println("\n=== 총 가격 ===");
        int total = BookManager.getTotalPrice(list);
        System.out.println("총 가격: " + total + "원");
        
        System.out.println("\n=== 도서 삭제 ===");
        boolean removed = BookManager.removeBook(list, "자바의 정석");
        System.out.println("삭제 결과: " + removed);
        
        System.out.println("\n=== 삭제 후 전체 도서 ===");
        for (Book b : list) {
            System.out.println(b);
        }
    }
}
```

---

### 🔍 코드 설명

#### 1. Book 클래스

**필드:**
- `title` (String): 도서 제목
- `author` (String): 저자
- `price` (int): 가격

**생성자:**
- 제목, 저자, 가격을 받아 객체 초기화

**메서드:**
- `toString()`: 도서 정보를 문자열로 표현

---

#### 2. BookManager 클래스

**메서드:**
- `addBook()`: 도서 추가
- `findBooksByAuthor()`: 저자로 도서 찾기
- `getTotalPrice()`: 총 가격 계산
- `removeBook()`: 도서 삭제

---

### 📐 메서드별 상세 설명

#### ➕ addBook() - 도서 추가

```java
public static void addBook(ArrayList<Book> list, String title, String author, int price) {
    list.add(new Book(title, author, price));
}
```

**동작 순서:**
1. Book 객체 생성 (title, author, price로 초기화)
2. `list.add()`로 ArrayList에 추가

**사용 예시:**
```java
addBook(list, "자바의 정석", "남궁성", 30000);
// list에 Book("자바의 정석", "남궁성", 30000) 객체 추가됨
```

---

#### 🔎 findBooksByAuthor() - 저자로 도서 찾기

```java
public static ArrayList<Book> findBooksByAuthor(ArrayList<Book> list, String author) {
    ArrayList<Book> result = new ArrayList<>();  // 결과를 저장할 새 리스트 생성
    for (Book b : list) {                       // 리스트 순회
        if (b.author.equals(author)) {           // 저자가 일치하면
            result.add(b);                      // 결과 리스트에 추가
        }
    }
    return result;                              // 결과 리스트 반환
}
```

**동작 순서:**
1. 결과를 저장할 새로운 ArrayList 생성
2. 원본 리스트의 모든 도서를 순회
3. 저자가 일치하는 도서를 찾으면 결과 리스트에 추가
4. 결과 리스트 반환 (여러 개일 수 있음)

**⚠️ 주의사항:**
- 새로운 리스트를 생성하여 반환 (원본 수정 안 함)
- 여러 개의 도서를 반환할 수 있음

**사용 예시:**
```java
ArrayList<Book> books = findBooksByAuthor(list, "신용권");
// 신용권 저자의 모든 도서가 books 리스트에 저장됨
```

---

#### 💰 getTotalPrice() - 총 가격 계산

```java
public static int getTotalPrice(ArrayList<Book> list) {
    int total = 0;
    for (Book b : list) {      // 모든 도서 순회
        total += b.price;       // 가격 누적
    }
    return total;              // 총 가격 반환
}
```

**동작 순서:**
1. 총 가격을 저장할 변수 초기화 (0)
2. 모든 도서를 순회하며 가격 누적
3. 총 가격 반환

**계산 예시:**
```
도서 3권: 30000원, 28000원, 32000원
총 가격: 30000 + 28000 + 32000 = 90000원
```

---

#### 🗑️ removeBook() - 도서 삭제

```java
public static boolean removeBook(ArrayList<Book> list, String title) {
    for (int i = 0; i < list.size(); i++) {        // 인덱스로 순회
        if (list.get(i).title.equals(title)) {     // 제목이 일치하면
            list.remove(i);                         // 해당 인덱스의 요소 제거
            return true;                           // 성공
        }
    }
    return false;                                  // 실패 (해당 제목의 도서 없음)
}
```

**동작 순서:**
1. 인덱스 0부터 시작하여 순회 (일반 for문 필요)
2. 해당 인덱스의 도서 제목이 일치하면
3. `list.remove(i)`로 해당 인덱스의 요소 제거
4. 제거 후 뒤의 요소들이 자동으로 앞으로 이동
5. 성공/실패 여부를 boolean으로 반환

**⚠️ 왜 일반 for문을 사용하나요?**
- 삭제 시 인덱스가 필요하기 때문
- 향상된 for문에서는 현재 인덱스를 알 수 없음
- 향상된 for문 사용 시 ConcurrentModificationException 발생 가능

**사용 예시:**
```java
boolean removed = removeBook(list, "자바의 정석");
if (removed) {
    System.out.println("삭제 완료");
} else {
    System.out.println("해당 도서가 없습니다");
}
```

---

### 🔄 전체 실행 흐름도

```
프로그램 시작
    ↓
ArrayList<Book> list 생성
    ↓
addBook()로 도서 3권 추가
    ↓
전체 도서 출력
    ↓
findBooksByAuthor(list, "신용권") 호출
    ↓
새 리스트 생성 → 저자 일치하는 도서 추가
    ↓
결과 리스트 반환 및 출력
    ↓
getTotalPrice(list) 호출
    ↓
모든 도서의 가격 합산
    ↓
총 가격 반환 및 출력
    ↓
removeBook(list, "자바의 정석") 호출
    ↓
인덱스로 순회하며 제목 비교
    ↓
일치하는 도서 찾아 삭제
    ↓
삭제 후 전체 도서 출력
```

---

### 🎯 핵심 포인트

1. **클래스 설계**
   - Book 클래스: 도서 정보 표현
   - BookManager 클래스: 도서 관리 기능

2. **ArrayList 활용**
   - 동적 크기 관리
   - CRUD 작업 구현

3. **메서드 분리**
   - 각 기능을 별도 메서드로 분리
   - 재사용성 향상

4. **인덱스 관리**
   - 삭제 시 일반 for문 사용
   - `get()`, `remove()` 메서드 활용

---

### 🔍 실행 결과 예시

```
=== 전체 도서 ===
제목: 자바의 정석, 저자: 남궁성, 가격: 30000원
제목: 혼자 공부하는 자바, 저자: 신용권, 가격: 28000원
제목: 이것이 자바다, 저자: 신용권, 가격: 32000원

=== 신용권 저자 도서 ===
제목: 혼자 공부하는 자바, 저자: 신용권, 가격: 28000원
제목: 이것이 자바다, 저자: 신용권, 가격: 32000원

=== 총 가격 ===
총 가격: 90000원

=== 도서 삭제 ===
삭제 결과: true

=== 삭제 후 전체 도서 ===
제목: 혼자 공부하는 자바, 저자: 신용권, 가격: 28000원
제목: 이것이 자바다, 저자: 신용권, 가격: 32000원
```

---

### 🎓 학습 포인트

#### ✅ 이해해야 할 개념
1. **클래스 설계**
   - 데이터 클래스 (Book)
   - 관리 클래스 (BookManager)

2. **ArrayList 활용**
   - 동적 리스트 관리
   - CRUD 작업

3. **메서드 작성**
   - 반환 타입 지정
   - 매개변수 활용

#### ✅ 주의할 점
1. **인덱스 관리**
   - 삭제 시 일반 for문 사용
   - 향상된 for문 사용 시 예외 발생 가능

2. **새 리스트 생성**
   - findBooksByAuthor()에서 새 리스트 생성
   - 원본 리스트 수정 안 함

3. **문자열 비교**
   - `equals()` 사용 필수
   - `==` 사용 금지

---

## 채점 기준 상세

### 문제 1 (20점)
- 필드 선언 (name, age, score): 5점
- 기본 생성자: 2점
- 매개변수 생성자: 3점
- toString() 메서드: 5점
- getGrade() 메서드 (if-else 로직): 5점

### 문제 2 (20점)
- addStudent() 메서드: 5점
- findStudent() 메서드 (반복문, equals 사용): 5점
- getAverageScore() 메서드 (합계 계산, 평균 계산): 5점
- printAll() 메서드: 5점

### 문제 3 (20점)
- countWords() 메서드 (split 사용): 5점
- reverseString() 메서드 (문자열 뒤집기 로직): 5점
- findMax() 메서드 (최댓값 찾기): 5점
- contains() 메서드 (배열 순회): 5점

### 문제 4 (20점)
- Book 클래스 필드 및 생성자: 3점
- Book 클래스 toString() 메서드: 2점
- addBook() 메서드: 3점
- findBooksByAuthor() 메서드 (새 리스트 생성, 조건 검사): 5점
- getTotalPrice() 메서드: 3점
- removeBook() 메서드 (인덱스 사용, 삭제): 4점

---

## 추가 설명

### 문제 1 - getGrade() 메서드
- if-else if 구조를 사용하여 점수 범위에 따라 등급 반환
- 조건문의 순서가 중요 (높은 점수부터 확인)

### 문제 2 - getAverageScore() 메서드
- 빈 리스트 체크 필요
- 정수 나눗셈을 피하기 위해 `(double)` 캐스팅 필요

### 문제 3 - countWords() 메서드
- `split("\\s+")` 사용: 하나 이상의 공백으로 분리
- null 체크 및 빈 문자열 체크 필요

### 문제 4 - findBooksByAuthor() 메서드
- 새로운 ArrayList를 생성하여 결과를 저장
- 원본 리스트를 수정하지 않음

### 문제 4 - removeBook() 메서드
- 인덱스를 사용한 삭제 (일반 for문 필요)
- 향상된 for문 사용 시 ConcurrentModificationException 발생 가능
