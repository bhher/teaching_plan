# ArrayList에 Student 객체 추가하기 - 도식화

## 코드
```java
public static void addStudent(ArrayList<Student> list, String name, int age, int score) {
    list.add(new Student(name, age, score));
}
```

---

## 📐 전체 과정 도식화

### 1단계: 메서드 호출 전

```
┌─────────────────────────────────────────────────────────┐
│  main() 메서드                                           │
│                                                          │
│  ArrayList<Student> list = new ArrayList<>();           │
│  ┌──────────────────────────────────────┐              │
│  │  list (참조 변수)                     │              │
│  │  ┌─────────┐                         │              │
│  │  │ 주소값  │─────────────┐           │              │
│  │  └─────────┘             │           │              │
│  └──────────────────────────┼───────────┘              │
│                             │                          │
│                             ▼                          │
│                  ┌──────────────────────┐              │
│                  │  ArrayList 객체      │              │
│                  │  ┌────────────────┐ │              │
│                  │  │ elementData    │ │              │
│                  │  │ (Object[])     │ │              │
│                  │  │                │ │              │
│                  │  │  [null]        │ │              │
│                  │  │  [null]        │ │              │
│                  │  │  [null]        │ │              │
│                  │  │  ...           │ │              │
│                  │  └────────────────┘ │              │
│                  │  size = 0           │              │
│                  └──────────────────────┘              │
└─────────────────────────────────────────────────────────┘
```

---

### 2단계: 메서드 호출 및 매개변수 전달

```
┌─────────────────────────────────────────────────────────┐
│  addStudent(list, "홍길동", 20, 85) 호출                │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  매개변수 스택                                     │ │
│  │  ┌─────────────┬──────────┬─────────┬──────────┐ │ │
│  │  │ list        │ name     │ age     │ score    │ │ │
│  │  │ (참조)      │ "홍길동" │ 20      │ 85       │ │ │
│  │  └─────────────┴──────────┴─────────┴──────────┘ │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  list 참조는 main()의 list와 같은 객체를 가리킴         │
│                                                          │
│  ┌─────────────┐                                        │
│  │ list (main) │────────┐                               │
│  └─────────────┘        │                               │
│                         │ 같은 객체 참조                │
│  ┌─────────────┐        │                               │
│  │ list (매개변수) │────┘                               │
│  └─────────────┘                                        │
└─────────────────────────────────────────────────────────┘
```

---

### 3단계: new Student() 객체 생성

```
┌─────────────────────────────────────────────────────────┐
│  new Student("홍길동", 20, 85) 실행                     │
│                                                          │
│  1. 힙(Heap) 메모리에 Student 객체 생성                 │
│  2. 생성자 호출하여 필드 초기화                          │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  힙(Heap) 메모리                                   │ │
│  │                                                    │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │  Student 객체 (0x1000 주소)          │         │ │
│  │  │  ┌──────────┬─────────┬───────────┐ │         │ │
│  │  │  │ name     │ age     │ score     │ │         │ │
│  │  │  │ "홍길동" │ 20      │ 85        │ │         │ │
│  │  │  └──────────┴─────────┴───────────┘ │         │ │
│  │  │  (메서드 영역)                       │         │ │
│  │  │  - toString()                       │         │ │
│  │  │  - getGrade()                       │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  │                                                    │ │
│  │  ※ 참고: String 객체도 별도로 힙에 저장           │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │  String 객체 (0x2000 주소)           │         │ │
│  │  │  "홍길동"                             │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

**상세 설명:**
- `new Student()`가 실행되면 힙 메모리에 Student 객체가 생성됨
- 생성자가 호출되어 `this.name = "홍길동"`, `this.age = 20`, `this.score = 85` 설정
- 생성된 객체의 주소값(예: 0x1000)이 반환됨

---

### 4단계: list.add() 실행

```
┌─────────────────────────────────────────────────────────┐
│  list.add(new Student("홍길동", 20, 85))               │
│                                                          │
│  ArrayList 내부 동작:                                    │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  ArrayList 객체                                    │ │
│  │  ┌──────────────────────────────────────────────┐ │ │
│  │  │  elementData (Object[] 배열)                 │ │ │
│  │  │                                              │ │ │
│  │  │  인덱스  │  저장값                            │ │ │
│  │  │  ──────┼──────────────────────────────────  │ │ │
│  │  │    0   │  [0x1000] ───┐                     │ │ │
│  │  │    1   │  [null]      │                     │ │ │
│  │  │    2   │  [null]      │                     │ │ │
│  │  │    3   │  [null]      │                     │ │ │
│  │  │   ... │  ...          │                     │ │ │
│  │  └──────────────────────────────────────────────┘ │ │
│  │  size = 1  (요소 개수 1개)                        │ │
│  └────────────────────────────────────────────────────┘ │
│                         │                               │
│                         ▼                               │
│  ┌────────────────────────────────────────────────────┐ │
│  │  힙(Heap) 메모리                                   │ │
│  │                                                    │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │  Student 객체 (0x1000)               │         │ │
│  │  │  ┌──────────┬─────────┬───────────┐ │         │ │
│  │  │  │ name     │ age     │ score     │ │         │ │
│  │  │  │ "홍길동" │ 20      │ 85        │ │         │ │
│  │  │  └──────────┴─────────┴───────────┘ │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

**핵심 개념:**
- ArrayList에 실제 Student 객체가 저장되는 것이 아니라, **객체의 참조(주소값)**가 저장됨
- `elementData[0] = 0x1000` (Student 객체의 주소)
- `size` 값이 1 증가

---

### 5단계: 여러 객체 추가 후

```
┌─────────────────────────────────────────────────────────┐
│  addStudent(list, "홍길동", 20, 85);                    │
│  addStudent(list, "김철수", 22, 95);                    │
│  addStudent(list, "이영희", 21, 75);                    │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  ArrayList 객체                                    │ │
│  │  ┌──────────────────────────────────────────────┐ │ │
│  │  │  elementData (Object[] 배열)                 │ │ │
│  │  │                                              │ │ │
│  │  │  [0] → [0x1000] ──────┐                     │ │ │
│  │  │  [1] → [0x2000] ──────┼───┐                 │ │ │
│  │  │  [2] → [0x3000] ──────┼───┼───┐             │ │ │
│  │  │  [3] → [null]         │   │   │             │ │ │
│  │  │  ... │  ...           │   │   │             │ │ │
│  │  └───────────────────────┼───┼───┼────────────┘ │ │
│  │  size = 3                │   │   │              │ │
│  └──────────────────────────┼───┼───┼──────────────┘ │
│                             │   │   │                │
│                             ▼   ▼   ▼                │
│  ┌────────────────────────────────────────────────────┐ │
│  │  힙(Heap) 메모리                                   │ │
│  │                                                    │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │  Student (0x1000)                    │         │ │
│  │  │  name: "홍길동"                      │         │ │
│  │  │  age: 20                            │         │ │
│  │  │  score: 85                          │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  │                                                    │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │  Student (0x2000)                    │         │ │
│  │  │  name: "김철수"                      │         │ │
│  │  │  age: 22                            │         │ │
│  │  │  score: 95                          │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  │                                                    │ │
│  │  ┌──────────────────────────────────────┐         │ │
│  │  │  Student (0x3000)                    │         │ │
│  │  │  name: "이영희"                      │         │ │
│  │  │  age: 21                            │         │ │
│  │  │  score: 75                          │         │ │
│  │  └──────────────────────────────────────┘         │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 상세 실행 흐름

### 단계별 메모리 변화

```
[1] 메서드 호출 전
┌──────────┐
│ list     │──────┐
└──────────┘      │
                  ▼
         ┌─────────────────┐
         │ ArrayList       │
         │ elementData: [] │
         │ size: 0         │
         └─────────────────┘

[2] new Student() 실행
┌──────────┐
│ list     │──────┐
└──────────┘      │
                  ▼
         ┌─────────────────┐     ┌─────────────────┐
         │ ArrayList       │     │ Student (0x1000)│
         │ elementData: [] │     │ name: "홍길동"  │
         │ size: 0         │     │ age: 20         │
         └─────────────────┘     │ score: 85       │
                                 └─────────────────┘

[3] list.add() 실행
┌──────────┐
│ list     │──────┐
└──────────┘      │
                  ▼
         ┌─────────────────┐     ┌─────────────────┐
         │ ArrayList       │     │ Student (0x1000)│
         │ elementData:    │────>│ name: "홍길동"  │
         │   [0x1000]      │     │ age: 20         │
         │ size: 1         │     │ score: 85       │
         └─────────────────┘     └─────────────────┘
```

---

## 💡 핵심 개념 설명

### 1. 객체 vs 참조

```java
// 잘못된 이해
ArrayList에 Student 객체가 직접 저장됨  ❌

// 올바른 이해
ArrayList에 Student 객체의 참조(주소)가 저장됨  ✅
```

**비유:**
- ArrayList는 **주소록**
- Student 객체는 **실제 집**
- ArrayList에는 집의 **주소**가 저장됨

### 2. 참조 저장 과정

```
┌─────────────────────────────────────────────┐
│  new Student("홍길동", 20, 85)              │
│              ↓                               │
│  힙 메모리에 Student 객체 생성               │
│  주소: 0x1000                               │
│              ↓                               │
│  list.add(객체의 주소값)                     │
│              ↓                               │
│  elementData[0] = 0x1000 (참조 저장)        │
└─────────────────────────────────────────────┘
```

### 3. 메모리 구조

```
┌─────────────────────────────────────────────┐
│  스택(Stack) 메모리                          │
│  - 메서드 호출 정보                          │
│  - 지역 변수 (list 참조)                     │
│  - 매개변수                                  │
└─────────────────────────────────────────────┘
              │
              │ 참조
              ▼
┌─────────────────────────────────────────────┐
│  힙(Heap) 메모리                             │
│  - Student 객체                              │
│  - ArrayList 객체                            │
│  - String 객체                               │
└─────────────────────────────────────────────┘
```

---

## 🎯 예제로 이해하기

### 코드
```java
ArrayList<Student> list = new ArrayList<>();

addStudent(list, "홍길동", 20, 85);
addStudent(list, "김철수", 22, 95);

Student s = list.get(0);  // 첫 번째 학생 가져오기
```

### 메모리 구조

```
┌──────────────────────────────────────────────────────┐
│  스택                                                │
│  ┌─────────────┐                                     │
│  │ list        │─────┐                               │
│  │ s           │     │                               │
│  └─────────────┘     │                               │
└──────────────────────┼───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│  힙                                                   │
│  ┌────────────────────────────────────────────────┐ │
│  │  ArrayList                                     │ │
│  │  elementData[0] = 0x1000                      │ │
│  │  elementData[1] = 0x2000                      │ │
│  │  size = 2                                      │ │
│  └────────────────────────────────────────────────┘ │
│                       │         │                    │
│                       ▼         ▼                    │
│  ┌──────────────┐  ┌──────────────┐                │
│  │ Student      │  │ Student      │                │
│  │ (0x1000)     │  │ (0x2000)     │                │
│  │ name: 홍길동 │  │ name: 김철수 │                │
│  │ age: 20      │  │ age: 22      │                │
│  │ score: 85    │  │ score: 95    │                │
│  └──────────────┘  └──────────────┘                │
│         ▲                                           │
│         │                                           │
│  s가 가리키는 객체 (list.get(0) 결과)              │
└──────────────────────────────────────────────────────┘
```

**설명:**
- `list.get(0)`은 `elementData[0]`의 값을 반환 (0x1000)
- 변수 `s`는 해당 주소의 Student 객체를 참조

---

## 🔍 주의사항

### 1. 같은 객체를 여러 리스트에 저장 가능

```java
ArrayList<Student> list1 = new ArrayList<>();
ArrayList<Student> list2 = new ArrayList<>();

Student s = new Student("홍길동", 20, 85);
list1.add(s);
list2.add(s);  // 같은 객체 참조
```

**메모리 구조:**
```
list1 → [0x1000]
         ▲
         │ 같은 객체
list2 → [0x1000]
         │
         ▼
   Student 객체
```

### 2. 참조를 통한 수정

```java
Student s = list.get(0);
s.score = 100;  // 원본 객체 수정

// list의 해당 Student도 수정됨!
```

**이유:** 참조를 통해 실제 객체를 수정하기 때문

---

## 📊 요약

| 단계 | 동작 | 메모리 변화 |
|------|------|------------|
| 1 | `new Student()` | 힙에 Student 객체 생성 (주소: 0x1000) |
| 2 | `list.add()` | ArrayList의 elementData 배열에 참조 저장 |
| 3 | 결과 | size 증가, elementData[0] = 0x1000 |

**핵심:**
- ArrayList에는 **객체가 아닌 참조(주소)**가 저장됨
- 실제 객체는 **힙 메모리**에 저장됨
- 여러 참조가 같은 객체를 가리킬 수 있음
