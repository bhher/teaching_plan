# 도서 관리 시스템 - 정답

## 📁 Book.java

```java
public class Book {
    String title;   // 제목
    String author;  // 저자
    
    // 생성자
    public Book(String title, String author) {
        this.title = title;
        this.author = author;
    }
    
    // toString() 메서드: 객체를 문자열로 변환
    @Override
    public String toString() {
        return "Book{title='" + title + "', author='" + author + "'}";
    }
}
```

---

## 📁 BookApp.java

```java
import java.util.ArrayList;

public class BookApp {
    public static void main(String[] args) {

        ArrayList<Book> list = new ArrayList<>();
        list.add(new Book("자바의 정석", "남궁성"));
        list.add(new Book("혼자 공부하는 자바", "신용권"));
        list.add(new Book("이것이 자바다", "신용권"));

        printAll(list);

        System.out.println("=== 수정 ===");
        updateBook(list, "자바의 정석", "남궁성(개정판)");
        printAll(list);

        System.out.println("=== 삭제 ===");
        deleteBook(list, "혼자 공부하는 자바");
        printAll(list);

        System.out.println("=== 검색 ===");
        Book b = findBook(list, "이것이 자바다");
        System.out.println(b);
    }

    // 메서드들
    static Book findBook(ArrayList<Book> list, String title) {
        for (Book b : list) {
            if (b.title.equals(title)) return b;
        }
        return null;
    }

    static boolean updateBook(ArrayList<Book> list, String title, String newAuthor) {
        for (Book b : list) {
            if (b.title.equals(title)) {
                b.author = newAuthor;
                return true;
            }
        }
        return false;
    }

    static boolean deleteBook(ArrayList<Book> list, String title) {
        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).title.equals(title)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }

    static void printAll(ArrayList<Book> list) {
        for (Book b : list) {
            System.out.println(b);
        }
    }
}
```

---

## 🔍 코드 설명

### 1. Book 클래스
- **필드**: 도서의 제목과 저자 정보를 저장
- **생성자**: 제목과 저자를 받아 객체 초기화
- **`toString()` 메서드**: 객체를 문자열로 표현하여 출력에 사용

### 2. BookApp 클래스
- **`list`**: Book 객체들을 저장하는 ArrayList
- **CRUD 메서드**: 도서의 생성, 읽기, 수정, 삭제 기능 제공

---

## 📐 시스템 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                    BookApp (메인 클래스)                      │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  ArrayList<Book> list                                 │  │
│  │  ┌────────┐  ┌────────┐  ┌────────┐                 │  │
│  │  │ Book   │→ │ Book   │→ │ Book   │→ ...            │  │
│  │  │객체 참조│  │객체 참조│  │객체 참조│                 │  │
│  │  └────────┘  └────────┘  └────────┘                 │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  메서드들:                                                     │
│  • printAll()    - 전체 출력                                 │
│  • findBook()    - 검색                                      │
│  • updateBook()  - 수정                                      │
│  • deleteBook()  - 삭제                                      │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ 사용
                              │
                  ┌───────────┴───────────┐
                  │                       │
        ┌─────────▼──────────┐  ┌────────▼──────────┐
        │   Book 클래스       │  │  ArrayList 클래스  │
        │                     │  │  (Java 라이브러리) │
        │  - title: String    │  │                   │
        │  - author: String   │  │  - add()          │
        │                     │  │  - remove()       │
        │  + toString()       │  │  - get()          │
        └─────────────────────┘  │  - size()         │
                                 └───────────────────┘
```

---

## 🔄 실행 흐름 도식화

### 1️⃣ 초기화 단계
```
main() 메서드 시작
    ↓
ArrayList<Book> list 생성 (빈 리스트)
    ↓
list.add(new Book("자바의 정석", "남궁성"))
list.add(new Book("혼자 공부하는 자바", "신용권"))
list.add(new Book("이것이 자바다", "신용권"))
    ↓
┌─────────────────────────────────────────────┐
│ ArrayList 내부 상태:                         │
│                                             │
│ [0] → Book{title="자바의 정석",            │
│            author="남궁성"}                 │
│ [1] → Book{title="혼자 공부하는 자바",      │
│            author="신용권"}                 │
│ [2] → Book{title="이것이 자바다",          │
│            author="신용권"}                 │
└─────────────────────────────────────────────┘
```

### 2️⃣ 전체 출력 (printAll)
```
printAll(list) 호출
    ↓
for (Book b : list) 반복
    ↓
b = list[0] → Book{title="자바의 정석", author="남궁성"}
    ↓
System.out.println(b) → b.toString() 호출
    ↓
출력: Book{title='자바의 정석', author='남궁성'}
    ↓
(나머지 도서들도 동일하게 출력)
```

### 3️⃣ 수정 (updateBook)
```
updateBook(list, "자바의 정석", "남궁성(개정판)") 호출
    ↓
for (Book b : list) 반복
    ↓
b = list[0] → Book{title="자바의 정석", author="남궁성"}
    ↓
b.title.equals("자바의 정석")? → true
    ↓
b.author = "남궁성(개정판)" (수정!)
    ↓
return true
    ↓
┌─────────────────────────────────────────────┐
│ ArrayList 내부 상태 (수정 후):              │
│                                             │
│ [0] → Book{title="자바의 정석",            │
│            author="남궁성(개정판)"} ← 수정됨│
│ [1] → Book{title="혼자 공부하는 자바",    │
│            author="신용권"}                 │
│ [2] → Book{title="이것이 자바다",         │
│            author="신용권"}                 │
└─────────────────────────────────────────────┘
```

### 4️⃣ 삭제 (deleteBook)
```
deleteBook(list, "혼자 공부하는 자바") 호출
    ↓
for (int i = 0; i < list.size(); i++) 반복
    ↓
i = 0: list.get(0).title = "자바의 정석" ≠ "혼자 공부하는 자바"
    ↓
i = 1: list.get(1).title = "혼자 공부하는 자바" = "혼자 공부하는 자바" ✓
    ↓
list.remove(1) 실행
    ↓
┌─────────────────────────────────────────────┐
│ ArrayList 내부 상태 (삭제 후):              │
│                                             │
│ [0] → Book{title="자바의 정석",            │
│            author="남궁성(개정판)"}         │
│ [1] → Book{title="이것이 자바다",         │
│            author="신용권"}                 │
│ (인덱스 자동 재정렬됨)                       │
└─────────────────────────────────────────────┘
```

### 5️⃣ 검색 (findBook)
```
findBook(list, "이것이 자바다") 호출
    ↓
for (Book b : list) 반복
    ↓
b = list[0] → Book{title="자바의 정석", ...}
    ↓
b.title.equals("이것이 자바다")? → false
    ↓
b = list[1] → Book{title="이것이 자바다", author="신용권"}
    ↓
b.title.equals("이것이 자바다")? → true
    ↓
return b (Book 객체 반환)
    ↓
main()에서 Book b = (반환된 객체)
    ↓
System.out.println(b) → Book{title='이것이 자바다', author='신용권'} 출력
```

---

## 📊 메서드별 동작 원리

### 🔎 findBook() - 검색
```java
static Book findBook(ArrayList<Book> list, String title) {
    for (Book b : list) {              // 향상된 for문: 리스트의 각 요소를 순회
        if (b.title.equals(title))      // 제목이 일치하면
            return b;                   // 해당 Book 객체 반환
    }
    return null;                        // 찾지 못하면 null 반환
}
```

**동작 순서:**
1. 리스트의 첫 번째 요소부터 마지막까지 순회
2. 각 Book 객체의 title 필드와 검색할 title 비교
3. 일치하는 객체를 찾으면 즉시 반환 (선형 검색)
4. 찾지 못하면 null 반환

---

### ✏️ updateBook() - 수정
```java
static boolean updateBook(ArrayList<Book> list, String title, String newAuthor) {
    for (Book b : list) {              // 리스트 순회
        if (b.title.equals(title)) {    // 제목이 일치하면
            b.author = newAuthor;       // author 필드 직접 수정
            return true;                // 성공
        }
    }
    return false;                       // 실패 (해당 제목의 도서 없음)
}
```

**동작 순서:**
1. 리스트를 순회하며 제목이 일치하는 Book 찾기
2. 찾은 객체의 author 필드를 newAuthor로 변경
3. 성공/실패 여부를 boolean으로 반환

**⚠️ 주의사항:**
- ArrayList에 저장된 것은 객체의 **참조(주소)**입니다
- 참조를 통해 필드를 수정하면 원본 객체가 수정됩니다

---

### 🗑️ deleteBook() - 삭제
```java
static boolean deleteBook(ArrayList<Book> list, String title) {
    for (int i = 0; i < list.size(); i++) {      // 인덱스로 순회
        if (list.get(i).title.equals(title)) {     // 제목 일치 확인
            list.remove(i);                       // 인덱스 i의 요소 제거
            return true;                          // 성공
        }
    }
    return false;                                 // 실패
}
```

**동작 순서:**
1. 인덱스 0부터 시작하여 순회 (일반 for문 필요)
2. 해당 인덱스의 Book 제목이 일치하면
3. `list.remove(i)`로 해당 인덱스의 요소 제거
4. 제거 후 뒤의 요소들이 자동으로 앞으로 이동

**⚠️ 왜 일반 for문을 사용하나요?**
- 삭제 시 인덱스가 필요하기 때문
- 향상된 for문에서는 현재 인덱스를 알 수 없음

---

### 📝 printAll() - 전체 출력
```java
static void printAll(ArrayList<Book> list) {
    for (Book b : list) {          // 리스트의 모든 요소 순회
        System.out.println(b);      // toString() 자동 호출
    }
}
```

**동작 순서:**
1. 리스트의 모든 Book 객체를 순회
2. `System.out.println(b)` 호출 시 자동으로 `b.toString()` 호출
3. Book 클래스의 toString() 메서드가 정의한 형식으로 출력

---

## 🎯 핵심 개념 도식화

### 1. 객체와 참조
```
┌─────────────────────────────────────────────────────┐
│ ArrayList<Book> list                                │
│                                                     │
│  인덱스  │  참조(주소)  │  실제 객체                   │
│ ────────┼─────────────┼──────────────────────────│
│   0     │  0x1000    │  Book{title="자바의 정석",│
│         │            │            author="남궁성"}│
│   1     │  0x2000    │  Book{title="혼자 공부하는│
│         │            │            자바", ...}     │
└─────────────────────────────────────────────────────┘
         │              │
         └──────┬───────┘
                │
        list.get(0)는 참조를 반환
        = 실제 Book 객체를 가리키는 주소
```

### 2. 필드 직접 접근 vs Getter/Setter
```java
// 현재 코드: 필드에 직접 접근
b.title           // public 필드 직접 접근
b.author = "..."   // public 필드 직접 수정

// 캡슐화된 코드 (권장):
b.getTitle()      // getter 메서드로 접근
b.setAuthor("...") // setter 메서드로 수정
```

### 3. ArrayList 동작 원리
```
추가 (add):
list.add(new Book("자바의 정석", "남궁성"))
    ↓
ArrayList 내부 배열에 추가
[null, null, null, ...] → [Book객체, null, null, ...]

삭제 (remove):
list.remove(0)
    ↓
해당 인덱스 요소 제거 후 뒤 요소들 앞으로 이동
[객체1, 객체2, 객체3] → [객체2, 객체3, null]
    ↑                    ↑
    제거됨                자동으로 앞당겨짐

검색 (get):
list.get(0)
    ↓
인덱스 0의 요소 반환 (참조 반환)
```

---

## 🔄 전체 실행 결과

```
Book{title='자바의 정석', author='남궁성'}
Book{title='혼자 공부하는 자바', author='신용권'}
Book{title='이것이 자바다', author='신용권'}
=== 수정 ===
Book{title='자바의 정석', author='남궁성(개정판)'}    ← 저자가 수정됨
Book{title='혼자 공부하는 자바', author='신용권'}
Book{title='이것이 자바다', author='신용권'}
=== 삭제 ===
Book{title='자바의 정석', author='남궁성(개정판)'}    ← "혼자 공부하는 자바" 삭제됨
Book{title='이것이 자바다', author='신용권'}
=== 검색 ===
Book{title='이것이 자바다', author='신용권'}
```

---

## 💡 핵심 포인트

1. **ArrayList 활용**
   - 동적으로 크기가 변하는 배열
   - 객체 참조를 저장하는 리스트

2. **객체 참조**
   - ArrayList에 저장되는 것은 객체 자체가 아닌 **참조(주소)**
   - 같은 객체를 여러 리스트에서 참조 가능

3. **향상된 for문 vs 일반 for문**
   - 향상된 for문: 요소만 필요할 때 (읽기, 수정)
   - 일반 for문: 인덱스가 필요할 때 (삭제)

4. **toString() 메서드**
   - 객체를 문자열로 표현
   - `System.out.println()`에서 자동 호출

5. **문자열 비교**
   - `==` 연산자 사용 금지
   - `equals()` 메서드 사용 필수

---

## 🎓 학습 포인트

### ✅ 이해해야 할 개념
- 클래스와 객체의 관계
- ArrayList와 배열의 차이
- 객체 참조와 복사
- 메서드의 매개변수 전달 방식

### ✅ 주의할 점
1. 삭제 시 인덱스 순회 방식 사용
2. 문자열 비교는 `equals()` 사용
3. null 체크 필요 (findBook 결과)
4. 필드 접근 제어자 (현재는 public, 실제로는 private 권장)

### ✅ 개선 가능한 부분
1. Book 클래스의 필드를 private로 변경
2. Getter/Setter 메서드 추가
3. 생성자 추가
4. 예외 처리 (null 체크 등)

---

## 📝 추가 도전 과제 정답

### 1. 같은 저자의 도서를 모두 찾는 메서드
```java
static ArrayList<Book> findBooksByAuthor(ArrayList<Book> list, String author) {
    ArrayList<Book> result = new ArrayList<>();
    for (Book b : list) {
        if (b.author.equals(author)) {
            result.add(b);
        }
    }
    return result;
}
```

### 2. 도서 개수를 반환하는 메서드
```java
static int getBookCount(ArrayList<Book> list) {
    return list.size();
}
```

### 3. 도서 제목으로 정렬하는 메서드 (선택 정렬)
```java
static void sortBooksByTitle(ArrayList<Book> list) {
    for (int i = 0; i < list.size() - 1; i++) {
        for (int j = i + 1; j < list.size(); j++) {
            if (list.get(i).title.compareTo(list.get(j).title) > 0) {
                Book temp = list.get(i);
                list.set(i, list.get(j));
                list.set(j, temp);
            }
        }
    }
}
```
