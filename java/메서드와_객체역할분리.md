# 메서드와 객체 역할 분리

## 📌 학습 목표

- 필드 vs 지역변수를 구분할 수 있다
- 메서드가 객체의 행동임을 이해한다
- getter/setter의 개념을 이해한다
- 객체가 자신의 일을 스스로 하도록 설계할 수 있다

---

## 1. 필드 vs 지역변수 복습

### 필드 (Field)

**클래스 내부에 선언된 변수**로, 객체의 **속성(상태)**을 나타냅니다.

```java
public class Student {
    // 필드 (객체의 속성)
    String name;      // 학생 이름
    int korean;       // 국어 점수
    int english;      // 영어 점수
    int math;         // 수학 점수
}
```

**특징:**
- 클래스 내부, 메서드 외부에 선언
- 객체가 생성될 때 함께 생성됨
- 객체의 생명주기 동안 존재
- **객체의 상태를 저장**

### 지역변수 (Local Variable)

**메서드 내부에 선언된 변수**로, 메서드 실행 중에만 사용됩니다.

```java
public void calculateTotal() {
    // 지역변수 (메서드 내부에 선언)
    int total = korean + english + math;  // 계산용 임시 변수
    System.out.println("총점: " + total);
}  // total은 여기서 사라짐
```

**특징:**
- 메서드 내부에 선언
- 메서드가 호출될 때 생성
- 메서드가 끝나면 사라짐
- **일시적인 값 저장**

### 비교표

| 구분 | 필드 | 지역변수 |
|------|------|----------|
| **선언 위치** | 클래스 내부, 메서드 외부 | 메서드 내부 |
| **생명주기** | 객체와 함께 생성/소멸 | 메서드 실행 중에만 존재 |
| **초기화** | 자동 초기화 (기본값) | 수동 초기화 필요 |
| **접근 범위** | 클래스 전체 | 메서드 내부만 |
| **용도** | 객체의 속성 저장 | 임시 계산값 저장 |

### 예제로 이해하기

```java
public class Student {
    // 필드 (객체의 속성)
    String name;
    int korean;
    int english;
    int math;
    
    // 메서드
    public void calculateAndPrint() {
        // 지역변수 (임시 계산값)
        int total = korean + english + math;
        double average = total / 3.0;
        
        System.out.println(name + "의 총점: " + total);
        System.out.println(name + "의 평균: " + average);
        
        // total, average는 여기서 사라짐
        // 하지만 name, korean, english, math는 계속 존재
    }
}
```

---

## 2. 메서드는 객체의 행동

### 핵심 개념

**메서드는 객체가 할 수 있는 행동(동작)을 정의합니다.**

### 객체 = 속성(필드) + 행동(메서드)

```java
public class Student {
    // 속성 (필드)
    String name;
    int korean;
    int english;
    int math;
    
    // 행동 (메서드)
    public void printInfo() {
        // 학생이 자신의 정보를 출력하는 행동
        System.out.println("이름: " + name);
    }
    
    public int calculateTotal() {
        // 학생이 자신의 총점을 계산하는 행동
        return korean + english + math;
    }
}
```

### "객체가 일을 한다"의 의미

```java
// ❌ 잘못된 방법 (객체 외부에서 계산)
Student student = new Student();
student.name = "홍길동";
student.korean = 90;
student.english = 80;
student.math = 70;

// 객체 밖에서 계산
int total = student.korean + student.english + student.math;
System.out.println("총점: " + total);
```

```java
// ✅ 올바른 방법 (객체가 자신의 일을 스스로 함)
Student student = new Student();
student.name = "홍길동";
student.korean = 90;
student.english = 80;
student.math = 70;

// 객체가 자신의 총점을 계산
int total = student.calculateTotal();
System.out.println("총점: " + total);
```

### 객체 중심 사고

| 객체 외부에서 처리 ❌ | 객체 내부에서 처리 ✅ |
|---------------------|---------------------|
| `int total = s.korean + s.english + s.math;` | `int total = s.calculateTotal();` |
| `System.out.println(s.name + "의 점수는...");` | `s.printInfo();` |
| `double avg = total / 3.0;` | `double avg = s.calculateAverage();` |

**왜 중요한가?**
- 코드 재사용성 증가
- 유지보수 용이
- 객체의 책임 명확화
- 캡슐화 원칙 준수

---

## 3. Getter / Setter 개념

### Getter (접근자)

**필드의 값을 읽어오는 메서드**입니다.

```java
public class Student {
    private String name;  // private으로 선언
    
    // Getter: name 값을 가져오는 메서드
    public String getName() {
        return name;
    }
}
```

**Getter 네이밍 규칙:**
- `get` + 필드명(첫 글자 대문자)
- boolean의 경우 `is` + 필드명 사용 가능

### Setter (설정자)

**필드의 값을 설정하는 메서드**입니다.

```java
public class Student {
    private String name;
    
    // Setter: name 값을 설정하는 메서드
    public void setName(String name) {
        this.name = name;  // this.name은 필드, name은 매개변수
    }
}
```

**Setter 네이밍 규칙:**
- `set` + 필드명(첫 글자 대문자)

### Getter/Setter 사용 이유

#### 1. 캡슐화 (Encapsulation)

```java
// ❌ 필드를 직접 접근 (위험)
public class Student {
    public String name;  // public - 누구나 직접 접근 가능
}

Student s = new Student();
s.name = "";  // 빈 문자열도 설정 가능 (유효성 검사 없음)
s.name = null;  // null도 설정 가능 (문제 발생 가능)
```

```java
// ✅ Getter/Setter 사용 (안전)
public class Student {
    private String name;  // private - 외부에서 직접 접근 불가
    
    // Setter에 유효성 검사 추가
    public void setName(String name) {
        if (name == null || name.trim().isEmpty()) {
            System.out.println("이름은 필수입니다!");
            return;
        }
        this.name = name;
    }
    
    // Getter로 안전하게 접근
    public String getName() {
        return name;
    }
}
```

#### 2. 데이터 보호

```java
public class Student {
    private int score;  // private
    
    // Setter로 유효 범위 검사
    public void setScore(int score) {
        if (score < 0 || score > 100) {
            System.out.println("점수는 0~100 사이여야 합니다!");
            return;
        }
        this.score = score;
    }
    
    public int getScore() {
        return score;
    }
}
```

### 완전한 Getter/Setter 예제

```java
public class Student {
    // 필드 (private)
    private String name;
    private int korean;
    private int english;
    private int math;
    
    // Getter 메서드들
    public String getName() {
        return name;
    }
    
    public int getKorean() {
        return korean;
    }
    
    public int getEnglish() {
        return english;
    }
    
    public int getMath() {
        return math;
    }
    
    // Setter 메서드들 (유효성 검사 포함)
    public void setName(String name) {
        if (name == null || name.trim().isEmpty()) {
            System.out.println("이름은 필수입니다!");
            return;
        }
        this.name = name;
    }
    
    public void setKorean(int korean) {
        if (korean < 0 || korean > 100) {
            System.out.println("국어 점수는 0~100 사이여야 합니다!");
            return;
        }
        this.korean = korean;
    }
    
    public void setEnglish(int english) {
        if (english < 0 || english > 100) {
            System.out.println("영어 점수는 0~100 사이여야 합니다!");
            return;
        }
        this.english = english;
    }
    
    public void setMath(int math) {
        if (math < 0 || math > 100) {
            System.out.println("수학 점수는 0~100 사이여야 합니다!");
            return;
        }
        this.math = math;
    }
}
```

---

## 4. 메서드로 계산/출력 맡기기

### 원칙: 객체가 자신의 일을 스스로 하도록

객체의 속성(필드)에 대한 **계산, 출력, 검증** 등은 객체 내부의 메서드로 처리합니다.

### 계산 메서드 예제

```java
public class Student {
    private String name;
    private int korean;
    private int english;
    private int math;
    
    // 계산 메서드: 총점 계산
    public int calculateTotal() {
        return korean + english + math;
    }
    
    // 계산 메서드: 평균 계산
    public double calculateAverage() {
        return calculateTotal() / 3.0;
    }
    
    // 계산 메서드: 등급 계산
    public String calculateGrade() {
        double avg = calculateAverage();
        if (avg >= 90) return "A";
        else if (avg >= 80) return "B";
        else if (avg >= 70) return "C";
        else if (avg >= 60) return "D";
        else return "F";
    }
}
```

### 출력 메서드 예제

```java
public class Student {
    private String name;
    private int korean;
    private int english;
    private int math;
    
    // 출력 메서드: 학생 정보 출력
    public void printInfo() {
        System.out.println("=== 학생 정보 ===");
        System.out.println("이름: " + name);
        System.out.println("국어: " + korean + "점");
        System.out.println("영어: " + english + "점");
        System.out.println("수학: " + math + "점");
    }
    
    // 출력 메서드: 성적표 출력
    public void printReport() {
        printInfo();  // 정보 출력
        System.out.println("총점: " + calculateTotal() + "점");
        System.out.println("평균: " + calculateAverage() + "점");
        System.out.println("등급: " + calculateGrade());
    }
}
```

### 검증 메서드 예제

```java
public class Student {
    private String name;
    private int korean;
    private int english;
    private int math;
    
    // 검증 메서드: 점수가 유효한지 확인
    public boolean isValid() {
        return korean >= 0 && korean <= 100 &&
               english >= 0 && english <= 100 &&
               math >= 0 && math <= 100;
    }
    
    // 검증 메서드: 합격 여부 확인
    public boolean isPass() {
        return calculateAverage() >= 60;
    }
}
```

---

## 5. 실전 예제

### 예제 1: 학생 점수 계산 메서드

**요구사항:**
- 학생의 이름과 3과목 점수를 저장
- 총점, 평균, 등급을 계산하는 메서드
- 학생 정보를 출력하는 메서드

**코드:**

```java
public class Student {
    // 필드 (속성)
    private String name;
    private int korean;
    private int english;
    private int math;
    
    // 생성자
    public Student(String name, int korean, int english, int math) {
        this.name = name;
        this.korean = korean;
        this.english = english;
        this.math = math;
    }
    
    // Getter
    public String getName() {
        return name;
    }
    
    public int getKorean() {
        return korean;
    }
    
    public int getEnglish() {
        return english;
    }
    
    public int getMath() {
        return math;
    }
    
    // 계산 메서드: 총점
    public int calculateTotal() {
        return korean + english + math;
    }
    
    // 계산 메서드: 평균
    public double calculateAverage() {
        return calculateTotal() / 3.0;
    }
    
    // 계산 메서드: 등급
    public String calculateGrade() {
        double avg = calculateAverage();
        if (avg >= 90) return "A";
        else if (avg >= 80) return "B";
        else if (avg >= 70) return "C";
        else if (avg >= 60) return "D";
        else return "F";
    }
    
    // 출력 메서드: 학생 정보 출력
    public void printInfo() {
        System.out.println("=== " + name + " 학생 정보 ===");
        System.out.println("국어: " + korean + "점");
        System.out.println("영어: " + english + "점");
        System.out.println("수학: " + math + "점");
        System.out.println("총점: " + calculateTotal() + "점");
        System.out.println("평균: " + String.format("%.2f", calculateAverage()) + "점");
        System.out.println("등급: " + calculateGrade());
    }
}

// 사용 예제
public class StudentTest {
    public static void main(String[] args) {
        Student student = new Student("홍길동", 90, 85, 88);
        student.printInfo();  // 객체가 자신의 정보를 출력
    }
}
```

---

### 예제 2: 회원 정보 출력 메서드

**요구사항:**
- 회원의 이름, 이메일, 가입일을 저장
- 회원 정보를 출력하는 메서드
- 회원 등급을 계산하는 메서드 (가입 기간 기준)

**코드:**

```java
import java.time.LocalDate;
import java.time.temporal.ChronoUnit;

public class Member {
    // 필드 (속성)
    private String name;
    private String email;
    private LocalDate joinDate;
    
    // 생성자
    public Member(String name, String email, LocalDate joinDate) {
        this.name = name;
        this.email = email;
        this.joinDate = joinDate;
    }
    
    // Getter
    public String getName() {
        return name;
    }
    
    public String getEmail() {
        return email;
    }
    
    public LocalDate getJoinDate() {
        return joinDate;
    }
    
    // 계산 메서드: 가입 기간 (일수)
    public long getJoinDays() {
        return ChronoUnit.DAYS.between(joinDate, LocalDate.now());
    }
    
    // 계산 메서드: 회원 등급
    public String getMemberGrade() {
        long days = getJoinDays();
        if (days >= 365) return "VIP";
        else if (days >= 180) return "Gold";
        else if (days >= 90) return "Silver";
        else return "Bronze";
    }
    
    // 출력 메서드: 회원 정보 출력
    public void printMemberInfo() {
        System.out.println("=== 회원 정보 ===");
        System.out.println("이름: " + name);
        System.out.println("이메일: " + email);
        System.out.println("가입일: " + joinDate);
        System.out.println("가입 기간: " + getJoinDays() + "일");
        System.out.println("회원 등급: " + getMemberGrade());
    }
    
    // 출력 메서드: 간단한 정보 출력
    public void printSimpleInfo() {
        System.out.println(name + " (" + email + ") - " + getMemberGrade());
    }
}

// 사용 예제
public class MemberTest {
    public static void main(String[] args) {
        Member member = new Member("김철수", "kim@example.com", 
                                  LocalDate.of(2023, 1, 1));
        member.printMemberInfo();  // 객체가 자신의 정보를 출력
    }
}
```

---

### 예제 3: 상품 총금액 계산 메서드

**요구사항:**
- 상품의 이름, 단가, 수량을 저장
- 총 금액을 계산하는 메서드
- 할인율을 적용한 최종 금액을 계산하는 메서드
- 상품 정보를 출력하는 메서드

**코드:**

```java
public class Product {
    // 필드 (속성)
    private String name;
    private int price;      // 단가
    private int quantity;   // 수량
    private double discountRate;  // 할인율 (0.0 ~ 1.0)
    
    // 생성자
    public Product(String name, int price, int quantity) {
        this.name = name;
        this.price = price;
        this.quantity = quantity;
        this.discountRate = 0.0;  // 기본 할인율 0%
    }
    
    // Getter
    public String getName() {
        return name;
    }
    
    public int getPrice() {
        return price;
    }
    
    public int getQuantity() {
        return quantity;
    }
    
    public double getDiscountRate() {
        return discountRate;
    }
    
    // Setter: 할인율 설정
    public void setDiscountRate(double discountRate) {
        if (discountRate < 0 || discountRate > 1.0) {
            System.out.println("할인율은 0.0 ~ 1.0 사이여야 합니다!");
            return;
        }
        this.discountRate = discountRate;
    }
    
    // 계산 메서드: 총 금액 (할인 전)
    public int calculateTotalPrice() {
        return price * quantity;
    }
    
    // 계산 메서드: 할인 금액
    public int calculateDiscountAmount() {
        return (int)(calculateTotalPrice() * discountRate);
    }
    
    // 계산 메서드: 최종 금액 (할인 후)
    public int calculateFinalPrice() {
        return calculateTotalPrice() - calculateDiscountAmount();
    }
    
    // 출력 메서드: 상품 정보 출력
    public void printProductInfo() {
        System.out.println("=== 상품 정보 ===");
        System.out.println("상품명: " + name);
        System.out.println("단가: " + price + "원");
        System.out.println("수량: " + quantity + "개");
        System.out.println("총 금액: " + calculateTotalPrice() + "원");
        
        if (discountRate > 0) {
            System.out.println("할인율: " + (discountRate * 100) + "%");
            System.out.println("할인 금액: " + calculateDiscountAmount() + "원");
            System.out.println("최종 금액: " + calculateFinalPrice() + "원");
        } else {
            System.out.println("최종 금액: " + calculateFinalPrice() + "원");
        }
    }
    
    // 출력 메서드: 영수증 출력
    public void printReceipt() {
        System.out.println("========== 영수증 ==========");
        printProductInfo();
        System.out.println("===========================");
    }
}

// 사용 예제
public class ProductTest {
    public static void main(String[] args) {
        Product product = new Product("노트북", 1000000, 2);
        product.setDiscountRate(0.1);  // 10% 할인
        product.printReceipt();  // 객체가 자신의 정보를 출력
    }
}
```

---

## 6. 핵심 정리

### 객체 중심 설계 원칙

1. **필드는 객체의 속성(상태)**
   - 클래스 내부에 선언
   - 객체의 데이터 저장

2. **메서드는 객체의 행동(동작)**
   - 객체가 할 수 있는 일
   - 자신의 속성을 활용한 계산, 출력, 검증

3. **객체가 자신의 일을 스스로 함**
   - 외부에서 계산/출력하지 않음
   - 객체 내부 메서드로 처리

4. **Getter/Setter로 안전한 접근**
   - private 필드 보호
   - 유효성 검사 가능

### 비교표

| 구분 | 필드 직접 접근 ❌ | 메서드 사용 ✅ |
|------|-----------------|--------------|
| **총점 계산** | `total = s.korean + s.english + s.math` | `total = s.calculateTotal()` |
| **정보 출력** | `System.out.println(s.name + "...")` | `s.printInfo()` |
| **필드 설정** | `s.score = -10;` | `s.setScore(-10);` (검증) |
| **필드 읽기** | `int k = s.korean;` | `int k = s.getKorean();` |

### 학습 체크리스트

- [ ] 필드와 지역변수의 차이를 설명할 수 있다
- [ ] 메서드가 객체의 행동임을 이해했다
- [ ] Getter/Setter의 개념과 사용 이유를 설명할 수 있다
- [ ] 객체가 자신의 일을 스스로 하도록 코드를 작성할 수 있다
- [ ] 계산 메서드, 출력 메서드를 작성할 수 있다
- [ ] 실전 예제를 이해하고 응용할 수 있다

---

## 7. 다음 단계

메서드와 객체 역할 분리를 마스터한 후:

1. **메서드 오버로딩**: 같은 이름, 다른 매개변수
2. **접근 제어자**: public, private 이해
3. **생성자 오버로딩**: 다양한 초기화 방법
4. **static 메서드**: 객체 없이 사용하는 메서드
5. **다른 객체와의 협력**: 객체 간 메시지 전달

---

## 📚 관련 자료

- [Java 6장: 메서드](./6장_메서드.md)
- [Java 7장: 객체지향 프로그래밍 기초](./7장_객체지향_프로그래밍_기초.md)
- [예제 코드: MethodBasics.java](./example6/MethodBasics.java)


