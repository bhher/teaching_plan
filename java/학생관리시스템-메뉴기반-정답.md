# 학생 관리 시스템 - 메뉴 기반 정답

## 📁 Student.java

```java
public class Student {
    String name;   // 학생 이름
    int age;       // 학생 나이
    
    // 생성자
    public Student(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // toString() 메서드: 객체를 문자열로 변환
    @Override
    public String toString() {
        return "Student{name='" + name + "', age=" + age + "}";
    }
}
```

---

## 📁 StudentApp.java

```java
import java.util.ArrayList;
import java.util.Scanner;

public class StudentApp {
    public static void main(String[] args) {

        ArrayList<Student> list = new ArrayList<>();
        Scanner sc = new Scanner(System.in);
        boolean run = true;

        while (run) {
            System.out.println("------------------------");
            System.out.println("1. 학생 등록");
            System.out.println("2. 학생 검색");
            System.out.println("3. 학생 수정");
            System.out.println("4. 학생 삭제");
            System.out.println("5. 전체 출력");
            System.out.println("6. 종료");
            System.out.print("선택 > ");

            int menu;
            try {
                menu = Integer.parseInt(sc.nextLine());
            } catch (Exception e) {
                System.out.println("숫자만 입력하세요");
                continue;
            }

            switch (menu) {
                case 1: // 등록
                    System.out.print("이름 입력 : ");
                    String name = sc.nextLine();

                    int age;
                    while (true) {
                        try {
                            System.out.print("나이 입력 : ");
                            age = Integer.parseInt(sc.nextLine());
                            break;
                        } catch (Exception e) {
                            System.out.println("나이는 숫자로 입력하세요");
                        }
                    }

                    list.add(new Student(name, age));
                    System.out.println("학생 등록 완료");
                    break;

                case 2: // 검색
                    System.out.print("검색할 이름 : ");
                    name = sc.nextLine();

                    Student s = findStudent(list, name);
                    if (s != null) {
                        System.out.println(s);
                    } else {
                        System.out.println("학생이 없습니다");
                    }
                    break;

                case 3: // 수정
                    System.out.print("수정할 이름 : ");
                    name = sc.nextLine();

                    int newAge;
                    while (true) {
                        try {
                            System.out.print("새 나이 입력 : ");
                            newAge = Integer.parseInt(sc.nextLine());
                            break;
                        } catch (Exception e) {
                            System.out.println("나이는 숫자로 입력하세요");
                        }
                    }

                    if (updateStudent(list, name, newAge)) {
                        System.out.println("수정 완료");
                    } else {
                        System.out.println("해당 학생이 없습니다");
                    }
                    break;

                case 4: // 삭제
                    System.out.print("삭제할 이름 : ");
                    name = sc.nextLine();

                    if (deleteStudent(list, name)) {
                        System.out.println("삭제 완료");
                    } else {
                        System.out.println("삭제할 학생이 없습니다");
                    }
                    break;

                case 5: // 전체 출력
                    if (list.isEmpty()) {
                        System.out.println("등록된 학생이 없습니다");
                    } else {
                        for (Student student : list) {
                            System.out.println(student);
                        }
                    }
                    break;

                case 6: // 종료
                    run = false;
                    System.out.println("프로그램 종료");
                    break;

                default:
                    System.out.println("1~6번 중에 선택하세요");
            }
        }
        sc.close();
    }

    // 학생 검색
    static Student findStudent(ArrayList<Student> list, String name) {
        for (Student s : list) {
            if (s.name.equals(name)) {
                return s;
            }
        }
        return null;
    }

    // 학생 수정
    static boolean updateStudent(ArrayList<Student> list, String name, int newAge) {
        for (Student s : list) {
            if (s.name.equals(name)) {
                s.age = newAge;
                return true;
            }
        }
        return false;
    }

    // 학생 삭제
    static boolean deleteStudent(ArrayList<Student> list, String name) {
        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).name.equals(name)) {
                list.remove(i);
                return true;
            }
        }
        return false;
    }
}
```

---

## 🔍 코드 설명

### 1. Student 클래스
- **필드**: 학생의 이름과 나이 정보를 저장
- **생성자**: 이름과 나이를 받아 객체 초기화
- **`toString()` 메서드**: 객체를 문자열로 표현하여 출력에 사용

### 2. StudentApp 클래스 - 메인 구조

#### 2.1 변수 선언
```java
ArrayList<Student> list = new ArrayList<>();  // 학생 목록 저장
Scanner sc = new Scanner(System.in);         // 사용자 입력 받기
boolean run = true;                          // 프로그램 실행 제어
```

#### 2.2 메뉴 반복 구조
```java
while (run) {
    // 메뉴 출력
    // 사용자 입력 받기
    // 메뉴에 따라 기능 실행
}
```

#### 2.3 예외 처리
```java
try {
    menu = Integer.parseInt(sc.nextLine());
} catch (Exception e) {
    System.out.println("숫자만 입력하세요");
    continue;  // 다시 메뉴로 돌아감
}
```

#### 2.4 나이 입력 예외 처리
```java
int age;
while (true) {
    try {
        System.out.print("나이 입력 : ");
        age = Integer.parseInt(sc.nextLine());
        break;  // 정상 입력 시 루프 종료
    } catch (Exception e) {
        System.out.println("나이는 숫자로 입력하세요");
        // 루프 계속 진행하여 다시 입력받기
    }
}
```

---

## 📊 메뉴별 동작 원리

### 1️⃣ 학생 등록 (case 1)
```
사용자 입력:
  - 이름: "홍길동"
  - 나이: 20
    ↓
나이 입력 예외 처리 (while 루프)
    ↓
list.add(new Student("홍길동", 20))
    ↓
ArrayList에 Student 객체 추가
    ↓
"학생 등록 완료" 출력
```

### 2️⃣ 학생 검색 (case 2)
```
사용자 입력:
  - 검색할 이름: "홍길동"
    ↓
findStudent(list, "홍길동") 호출
    ↓
리스트를 순회하며 이름이 일치하는 Student 찾기
    ↓
찾으면: Student 객체 반환 → 출력
못 찾으면: null 반환 → "학생이 없습니다" 출력
```

### 3️⃣ 학생 수정 (case 3)
```
사용자 입력:
  - 수정할 이름: "홍길동"
  - 새 나이: 21
    ↓
나이 입력 예외 처리 (while 루프)
    ↓
updateStudent(list, "홍길동", 21) 호출
    ↓
이름으로 학생 찾기
    ↓
찾으면: age 필드 수정 → true 반환 → "수정 완료"
못 찾으면: false 반환 → "해당 학생이 없습니다"
```

### 4️⃣ 학생 삭제 (case 4)
```
사용자 입력:
  - 삭제할 이름: "김철수"
    ↓
deleteStudent(list, "김철수") 호출
    ↓
인덱스로 순회하며 이름이 일치하는 Student 찾기
    ↓
찾으면: list.remove(i) 실행 → true 반환 → "삭제 완료"
못 찾으면: false 반환 → "삭제할 학생이 없습니다"
```

### 5️⃣ 전체 출력 (case 5)
```
list.isEmpty() 체크
    ↓
비어있으면: "등록된 학생이 없습니다"
    ↓
비어있지 않으면:
  for (Student student : list) {
      System.out.println(student);  // toString() 자동 호출
  }
```

### 6️⃣ 종료 (case 6)
```
run = false 설정
    ↓
while (run) 조건이 false가 됨
    ↓
루프 종료
    ↓
"프로그램 종료" 출력
    ↓
sc.close()로 Scanner 닫기
```

---

## 🔄 전체 실행 흐름도

```
프로그램 시작
    ↓
ArrayList<Student> list 생성
Scanner sc 생성
boolean run = true
    ↓
┌─────────────────────────┐
│   while (run) 루프      │
│                         │
│   1. 메뉴 출력           │
│   2. 사용자 입력 받기    │
│   3. 예외 처리           │
│   4. switch문으로 분기   │
│      - case 1: 등록     │
│        (나이 입력 예외처리)│
│      - case 2: 검색     │
│      - case 3: 수정     │
│        (나이 입력 예외처리)│
│      - case 4: 삭제     │
│      - case 5: 전체출력  │
│      - case 6: 종료     │
│                         │
└─────────────────────────┘
         ↑              │
         │              │
         └──────────────┘
      (run이 false가 될 때까지 반복)
    ↓
Scanner 닫기
프로그램 종료
```

---

## 📐 메서드별 상세 설명

### 🔎 findStudent() - 검색
```java
static Student findStudent(ArrayList<Student> list, String name) {
    for (Student s : list) {              // 향상된 for문으로 리스트 순회
        if (s.name.equals(name)) {         // 이름이 일치하면
            return s;                       // 해당 Student 객체 반환
        }
    }
    return null;                           // 찾지 못하면 null 반환
}
```

**동작 순서:**
1. 리스트의 첫 번째 요소부터 마지막까지 순회
2. 각 Student 객체의 name 필드와 검색할 name 비교
3. 일치하는 객체를 찾으면 즉시 반환 (선형 검색)
4. 찾지 못하면 null 반환

**⚠️ 주의사항:**
- 문자열 비교는 `equals()` 사용 (== 사용 금지)
- null 체크 필요 (호출하는 쪽에서 처리)

---

### ✏️ updateStudent() - 수정
```java
static boolean updateStudent(ArrayList<Student> list, String name, int newAge) {
    for (Student s : list) {              // 리스트 순회
        if (s.name.equals(name)) {         // 이름이 일치하면
            s.age = newAge;                // age 필드 직접 수정
            return true;                   // 성공
        }
    }
    return false;                          // 실패 (해당 이름의 학생 없음)
}
```

**동작 순서:**
1. 리스트를 순회하며 이름이 일치하는 Student 찾기
2. 찾은 객체의 age 필드를 newAge로 변경
3. 성공/실패 여부를 boolean으로 반환

**⚠️ 주의사항:**
- ArrayList에 저장된 것은 객체의 **참조(주소)**
- 참조를 통해 필드를 수정하면 원본 객체가 수정됨

---

### 🗑️ deleteStudent() - 삭제
```java
static boolean deleteStudent(ArrayList<Student> list, String name) {
    for (int i = 0; i < list.size(); i++) {      // 인덱스로 순회
        if (list.get(i).name.equals(name)) {      // 이름 일치 확인
            list.remove(i);                       // 인덱스 i의 요소 제거
            return true;                          // 성공
        }
    }
    return false;                                 // 실패
}
```

**동작 순서:**
1. 인덱스 0부터 시작하여 순회 (일반 for문 필요)
2. 해당 인덱스의 Student 이름이 일치하면
3. `list.remove(i)`로 해당 인덱스의 요소 제거
4. 제거 후 뒤의 요소들이 자동으로 앞으로 이동

**⚠️ 왜 일반 for문을 사용하나요?**
- 삭제 시 인덱스가 필요하기 때문
- 향상된 for문에서는 현재 인덱스를 알 수 없음

---

## 🎯 핵심 포인트

### 1. 메뉴 기반 프로그램 구조
- **while 루프**: 프로그램이 종료될 때까지 반복
- **boolean 변수**: `run`으로 프로그램 실행 제어
- **switch문**: 메뉴 선택에 따른 기능 분기

### 2. 사용자 입력 처리
- **Scanner**: `Scanner sc = new Scanner(System.in);`
- **nextLine()**: 한 줄 전체를 문자열로 읽기
- **Integer.parseInt()**: 문자열을 정수로 변환

### 3. 예외 처리
- **try-catch**: 숫자 입력 시 예외 처리
- **continue**: 예외 발생 시 다시 메뉴로 돌아가기
- **default**: switch문에서 잘못된 메뉴 번호 처리
- **while(true) 루프**: 나이 입력 시 잘못된 입력 반복 처리

### 4. ArrayList 활용
- **동적 크기**: 필요에 따라 크기가 자동으로 변함
- **객체 참조 저장**: 실제 객체가 아닌 참조(주소)를 저장
- **isEmpty()**: 리스트가 비어있는지 확인

### 5. 객체 지향 개념
- **클래스와 객체**: Student 클래스로 학생 정보 표현
- **캡슐화**: 필드와 메서드를 하나의 클래스로 묶음
- **toString()**: 객체를 문자열로 표현

---

## 🔍 실행 결과 예시

```
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 1
이름 입력 : 홍길동
나이 입력 : 20
학생 등록 완료
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 1
이름 입력 : 김철수
나이 입력 : 22
학생 등록 완료
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 5
Student{name='홍길동', age=20}
Student{name='김철수', age=22}
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 2
검색할 이름 : 홍길동
Student{name='홍길동', age=20}
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 2
검색할 이름 : 이영희
학생이 없습니다
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 3
수정할 이름 : 홍길동
새 나이 입력 : 21
수정 완료
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 3
수정할 이름 : 이영희
새 나이 입력 : 19
해당 학생이 없습니다
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 4
삭제할 이름 : 김철수
삭제 완료
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 4
삭제할 이름 : 이영희
삭제할 학생이 없습니다
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 5
Student{name='홍길동', age=21}
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 7
1~6번 중에 선택하세요
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > abc
숫자만 입력하세요
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 1
이름 입력 : 이영희
나이 입력 : abc
나이는 숫자로 입력하세요
나이 입력 : 19
학생 등록 완료
------------------------
1. 학생 등록
2. 학생 검색
3. 학생 수정
4. 학생 삭제
5. 전체 출력
6. 종료
선택 > 6
프로그램 종료
```

---

## 🎓 학습 포인트

### ✅ 이해해야 할 개념
1. **메뉴 기반 프로그램 구조**
   - while 루프로 반복 실행
   - switch문으로 기능 분기

2. **사용자 입력 처리**
   - Scanner 클래스 사용법
   - nextLine() vs next() 차이

3. **예외 처리**
   - try-catch 문법
   - NumberFormatException 처리
   - while(true) 루프를 이용한 반복 입력 처리

4. **ArrayList 활용**
   - 동적 리스트 관리
   - CRUD 작업 구현

5. **객체 지향 프로그래밍**
   - 클래스와 객체
   - 메서드 분리와 재사용

### ✅ 주의할 점
1. **Scanner 사용**
   - `sc.nextLine()` 사용 시 주의 (버퍼 문제)
   - 프로그램 종료 시 `sc.close()` 호출

2. **문자열 비교**
   - `==` 연산자 사용 금지
   - `equals()` 메서드 사용 필수

3. **null 체크**
   - findStudent() 결과가 null일 수 있음
   - null 체크 후 사용

4. **인덱스 관리**
   - 삭제 시 일반 for문 사용
   - 향상된 for문은 인덱스 접근 불가

5. **나이 입력 예외 처리**
   - while(true) 루프로 잘못된 입력 반복 처리
   - 정상 입력 시 break로 루프 탈출

### ✅ 개선 가능한 부분
1. **캡슐화**
   - Student 클래스의 필드를 private로 변경
   - Getter/Setter 메서드 추가

2. **예외 처리 강화**
   - 빈 문자열 입력 체크
   - 나이 범위 검증 (예: 0 이상 150 이하)
   - 더 구체적인 예외 처리

3. **기능 확장**
   - 같은 나이의 학생 검색
   - 학생 정렬 기능
   - 파일 저장/불러오기

4. **코드 구조화**
   - 메서드 분리
   - 상수 정의 (메뉴 번호 등)
