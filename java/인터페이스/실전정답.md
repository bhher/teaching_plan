# 인터페이스 실전 정답 — 온라인 쇼핑몰 주문 시스템

문제: [실전문제.md](./실전문제.md)

패키지: `a0825.shop`

## Java 소스 위치

```text
인터페이스/a0825/shop/
├── Discount.java / Payable.java / Notifier.java
├── NoDiscount.java / MemberDiscount.java / CouponDiscount.java
├── CardPay.java / KakaoPay.java / BankTransfer.java
├── SmsNotifier.java / EmailNotifier.java
├── OrderService.java
└── ShopMain.java
```

```bash
cd java/인터페이스
javac a0825/shop/*.java
java a0825.shop.ShopMain
```

---

## Discount.java

```java
package a0825.shop;

public interface Discount {
    double apply(int price);
    String getName();
}
```

---

## Payable.java

```java
package a0825.shop;

public interface Payable {
    boolean pay(int amount);
    String getMethodName();
}
```

---

## Notifier.java

```java
package a0825.shop;

public interface Notifier {
    void send(String message);
}
```

---

## 할인 구현

```java
package a0825.shop;

public class NoDiscount implements Discount {
    @Override
    public double apply(int price) {
        return price;
    }

    @Override
    public String getName() {
        return "할인없음";
    }
}
```

```java
package a0825.shop;

public class MemberDiscount implements Discount {
    @Override
    public double apply(int price) {
        return (int) (price * 0.9);
    }

    @Override
    public String getName() {
        return "회원할인";
    }
}
```

```java
package a0825.shop;

public class CouponDiscount implements Discount {
    private int couponAmount;

    public CouponDiscount(int couponAmount) {
        this.couponAmount = couponAmount;
    }

    @Override
    public double apply(int price) {
        int result = price - couponAmount;
        return result < 0 ? 0 : result;
    }

    @Override
    public String getName() {
        return "쿠폰할인(" + couponAmount + "원)";
    }
}
```

---

## 결제 구현

```java
package a0825.shop;

public class CardPay implements Payable {
    private int limit;

    public CardPay(int limit) {
        this.limit = limit;
    }

    @Override
    public boolean pay(int amount) {
        if (amount > limit) {
            System.out.println("잔액(한도) 부족");
            return false;
        }
        limit -= amount;
        System.out.println("카드 결제 완료: " + amount + "원");
        return true;
    }

    @Override
    public String getMethodName() {
        return "카드";
    }
}
```

```java
package a0825.shop;

public class KakaoPay implements Payable {
    private int balance;

    public KakaoPay(int balance) {
        this.balance = balance;
    }

    @Override
    public boolean pay(int amount) {
        if (amount > balance) {
            System.out.println("잔액(한도) 부족");
            return false;
        }
        balance -= amount;
        System.out.println("카카오페이 결제 완료: " + amount + "원");
        return true;
    }

    @Override
    public String getMethodName() {
        return "카카오페이";
    }
}
```

```java
package a0825.shop;

public class BankTransfer implements Payable {
    @Override
    public boolean pay(int amount) {
        System.out.println("계좌이체 완료: " + amount + "원");
        return true;
    }

    @Override
    public String getMethodName() {
        return "계좌이체";
    }
}
```

---

## 알림 구현

```java
package a0825.shop;

public class SmsNotifier implements Notifier {
    @Override
    public void send(String message) {
        System.out.println("[SMS] " + message);
    }
}
```

```java
package a0825.shop;

public class EmailNotifier implements Notifier {
    @Override
    public void send(String message) {
        System.out.println("[EMAIL] " + message);
    }
}
```

---

## OrderService.java

```java
package a0825.shop;

public class OrderService {
    private Discount discount;
    private Payable payable;
    private Notifier notifier;

    public OrderService(Discount discount, Payable payable, Notifier notifier) {
        this.discount = discount;
        this.payable = payable;
        this.notifier = notifier;
    }

    public boolean checkout(String productName, int price) {
        System.out.println("상품: " + productName + " | 원가: " + price + "원");
        System.out.println("적용 할인: " + discount.getName());

        int payAmount = (int) discount.apply(price);
        System.out.println("결제 금액: " + payAmount + "원");

        boolean ok = payable.pay(payAmount);
        if (ok) {
            notifier.send(productName + " 결제 완료 (" + payAmount + "원)");
            System.out.println("주문 성공!");
        } else {
            System.out.println("주문 실패");
        }
        return ok;
    }
}
```

---

## ShopMain.java

```java
package a0825.shop;

public class ShopMain {
    public static void main(String[] args) {
        System.out.println("===== 주문 1 =====");
        OrderService order1 = new OrderService(
                new MemberDiscount(),
                new CardPay(200000),
                new SmsNotifier()
        );
        order1.checkout("무선이어폰", 100000);

        System.out.println("\n===== 주문 2 =====");
        OrderService order2 = new OrderService(
                new CouponDiscount(5000),
                new KakaoPay(30000),  // 45000 결제 시도 → 실패
                new EmailNotifier()
        );
        order2.checkout("키보드", 50000);

        System.out.println("\n===== 주문 3 =====");
        OrderService order3 = new OrderService(
                new NoDiscount(),
                new BankTransfer(),
                new SmsNotifier()
        );
        order3.checkout("마우스", 20000);
    }
}
```

---

## 실행 결과

```text
===== 주문 1 =====
상품: 무선이어폰 | 원가: 100000원
적용 할인: 회원할인
결제 금액: 90000원
카드 결제 완료: 90000원
[SMS] 무선이어폰 결제 완료 (90000원)
주문 성공!

===== 주문 2 =====
상품: 키보드 | 원가: 50000원
적용 할인: 쿠폰할인(5000원)
결제 금액: 45000원
잔액(한도) 부족
주문 실패

===== 주문 3 =====
상품: 마우스 | 원가: 20000원
적용 할인: 할인없음
결제 금액: 20000원
계좌이체 완료: 20000원
[SMS] 마우스 결제 완료 (20000원)
주문 성공!
```

---

## (선택) 여러 알림 동시 발송

```java
import java.util.ArrayList;
import java.util.List;

public class MultiNotifier implements Notifier {
    private List<Notifier> list = new ArrayList<>();

    public void add(Notifier n) {
        list.add(n);
    }

    @Override
    public void send(String message) {
        for (Notifier n : list) {
            n.send(message);
        }
    }
}

// 사용
MultiNotifier multi = new MultiNotifier();
multi.add(new SmsNotifier());
multi.add(new EmailNotifier());
OrderService order = new OrderService(new NoDiscount(), new BankTransfer(), multi);
```

---

## 설계 포인트 (수업용)

| 원칙 | 이 코드에서 |
|------|-------------|
| 인터페이스로 역할 분리 | 할인 / 결제 / 알림 |
| 의존성 주입 | 생성자로 구현체 전달 |
| 다형성 | `discount.apply`, `payable.pay` — 구현 몰라도 동작 |
| 교체 용이 | 카드 → 계좌이체만 바꿔도 OrderService 수정 없음 |

```text
OrderService는 "카드"를 모른다.
오직 Payable 만 안다.  →  실무 서비스 계층과 같은 패턴
```

---

## 관련 자료

- [문제.md](./문제.md) — 기초
- [실전문제.md](./실전문제.md)
- [인터페이스-완전정리.md](../인터페이스-완전정리.md)
