# 도서 대여 시스템 해설 (응용 버전)

## 프로젝트 개요

커피 주문 시스템을 참고하여 도서 대여 시스템을 구현한 응용 버전입니다.

**프로젝트명:** 더조은도서관 대여 시스템  
**패키지:** `a0331.book`  
**주요 특징:** 싱글톤 패턴, LinkedHashMap, 재대여 기능, 결제 시스템

---

## 전체 소스 코드

### 1. Book.java - 싱글톤 패턴으로 도서 목록 관리

```java
package a0331.book;

import java.text.DecimalFormat;
import java.util.ArrayList;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Map.Entry;

public class Book {
    private static Book instance;  // 싱글톤 인스턴스
    private Book(){};  // private 생성자
    
    public static Book getInstance(){
        if(instance == null){
            instance = new Book();
        }
        return instance;
    }
    
    ArrayList<String> bookList;      // 도서 제목
    ArrayList<Integer> bookPrice;   // 대여 가격 (1일 기준)
    Map<String, Integer> menu;       // 도서 목록 맵
    
    public void getMenu(){
        menu = new LinkedHashMap<String, Integer>();
        bookList = new ArrayList<>();
        bookPrice = new ArrayList<>();
        
        // 도서 제목 추가
        bookList.add("자바의 정석");
        bookList.add("이것이 자바다");
        bookList.add("Do it! 자바 프로그래밍");
        bookList.add("Head First Java");
        bookList.add("Effective Java");
        bookList.add("Clean Code");
        
        // 대여 가격 추가 (1일 기준)
        bookPrice.add(1000);
        bookPrice.add(1200);
        bookPrice.add(1000);
        bookPrice.add(1500);
        bookPrice.add(1800);
        bookPrice.add(1300);
        
        // ArrayList를 Map으로 변환
        for(int i = 0; i < bookList.size(); i++){
            menu.put(bookList.get(i), bookPrice.get(i));
        }
        
        // 메뉴 출력
        DecimalFormat f = new DecimalFormat("0,000원");
        StringBuffer st = new StringBuffer();
        st.append("\n\n")
            .append("+---------------------------------------------------+\n")
            .append("+------------------------도서목록----------------------+\n")
            .append("|            Book                   price           |\n");
        System.out.println(st.toString());
        
        int s = 1;
        for(Entry<String,Integer> get : menu.entrySet()){
            System.out.printf(": [%d] %-25s\t %s          :\n"
                ,s, get.getKey(), f.format(get.getValue()));
            s++;
        }
        System.out.println("+----------------------------------------------------+\n");
    }
}
```

**해설:**
- **싱글톤 패턴**: 커피 시스템과 동일한 구조
- **LinkedHashMap**: 도서 번호 순서 보장
- **DecimalFormat**: 가격을 "1,000원" 형식으로 출력
- **StringBuffer**: 여러 줄의 문자열을 효율적으로 연결

---

### 2. Member.java - 회원 정보 관리

```java
package a0331.book;

import java.util.Map;

public class Member {
    private int memberNum;              // 회원 번호
    private int money;                 // 잔액 (초기값 30000원)
    private Map<String, Integer> bookRental;  // 대여 내역 (도서명, 대여일수)
    
    public Member(int memberNum) {
        this.money = 30000;  // 초기 잔액 설정
        this.memberNum = memberNum;
    }
    
    public Map<String, Integer> getBookRental() {
        return bookRental;
    }
    
    public void setBookRental(Map<String, Integer> bookRental) {
        this.bookRental = bookRental;
    }
    
    public int getMemberNum() {
        return memberNum;
    }
    
    public void setMemberNum(int memberNum) {
        this.memberNum = memberNum;
    }
    
    public int getMoney() {
        return money;
    }
    
    public void setMoney(int money) {
        this.money = money;
    }
    
    public String getMemberName() {
        return "회원 " + memberNum;
    }
}
```

**해설:**
- **Customer와 유사한 구조**: 커피 시스템의 Customer 클래스와 동일한 패턴
- **초기 잔액**: 30000원으로 설정 (도서 대여는 커피보다 비쌀 수 있음)
- **대여 내역**: Map으로 도서명과 대여일수 저장

---

### 3. BookService.java - 대여 서비스 로직

```java
package a0331.book;

import java.text.DecimalFormat;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Scanner;

public class BookService {
    private boolean reRental = false;      // 재대여 여부
    private int memberNum = 1;             // 회원 번호
    Map<String, Integer> rentalList;        // 대여 리스트
    
    public BookService() {
        rentalList = new LinkedHashMap<>();
    }
    
    Book book = Book.getInstance();         // 싱글톤 객체
    Member member;                          // 회원 객체
    Thread t = new Thread();                // 스레드
    Scanner sc = new Scanner(System.in);
    
    public void start() {
        System.out.println("\n 어서오세요 더조은도서관입니다.");
        member = new Member(memberNum);
        book.getMenu();
        rental();
        
        totalRental(member);
        try {
            System.out.println("기다려 주시면 대여하신 도서를 준비하겠습니다.");
            t.sleep(2000);
            end();
        } catch(Exception e) {
            e.printStackTrace();
        }
    }
    
    private void rental() {
        System.out.println("\n 취소를 원하시면 0번을 눌러주세요");
        end:while (true) {
            try {
                System.out.print("\n원하는 도서의 번호를 선택해주세요 :");
                String choice = sc.next();
                int choiceNum = Integer.parseInt(choice.substring(0,1));
                
                if(choiceNum == 0){
                    System.out.println("대여를 취소합니다.");
                    System.exit(0);
                }
                
                sc.nextLine();
                String bookName = book.bookList.get(choiceNum-1);
                System.out.println("선택 하신 도서는 : " + bookName + "입니다. 며칠 대여하시겠습니까?");
                int rentalDays = sc.nextInt();
                sc.nextLine();
                
                // 재대여 처리
                if(reRental){
                    if(rentalList.containsKey(bookName)){
                        // 같은 도서 재대여
                        int addDays = rentalList.get(bookName) + rentalDays;
                        rentalList.replace(bookName, addDays);
                    } else {
                        // 새로운 도서 추가
                        rentalList.put(bookName, rentalDays);
                    }
                } else {
                    rentalList.put(bookName, rentalDays);
                }
                
                member.setBookRental(rentalList);
                addRental();
                break end;
            } catch(Exception e) {
                System.out.println("잘못된 선택입니다");
            }
        }
    }
    
    private void addRental() {
        reRental = false;
        System.out.println("\n대여를 계속 하시겠습니까?");
        System.out.println("예(Y)/아니오(N)");
        String yesOrNo = sc.next();
        if(yesOrNo.equals("예") || yesOrNo.equalsIgnoreCase("y")){
            book.getMenu();
            reRental = true;
            rental();
        } else if(yesOrNo.equals("아니오") || yesOrNo.equalsIgnoreCase("n")){
            return;
        }
    }
    
    private void totalRental(Member member) {
        int s = 1;
        int totalMoney = 0;
        int bookPrice = 0;
        DecimalFormat f = new DecimalFormat("###,000원");
        String name = member.getMemberName() + "번 ";
        StringBuffer message = new StringBuffer();
        
        message.append("\n\n ")
            .append("+----------------------------------------------------+\n ")
            .append("|                                                    | \n ")
            .append("|             " + name + "회원님 의 대여 내역 입니다         | " + "\n");
        
        for(Map.Entry<String, Integer> rental : member.getBookRental().entrySet()){
            String bookName = rental.getKey();
            int rentalDays = rental.getValue();
            int unitPrice = book.menu.get(bookName);
            bookPrice = unitPrice * rentalDays;
            totalMoney = totalMoney + bookPrice;
            String pay = f.format(bookPrice);
            message.append(String.format(" | [%d] %-20s : %2d일  %7s |\n", 
                s, bookName, rentalDays, pay));
            s++;
        }
        
        message.append(" |                                                    |\n ")
            .append("+----------------------------------------------------+ \n")
            .append(" ============ 총 결제 금액은 " + f.format(totalMoney) + "입니다 ========== \n");
        System.out.println(message);
        payment(totalMoney);
    }
    
    private void payment(int totalMoney) {
        System.out.println("\n결제를 도와 드리겠습니다 카드 넣어주세요");
        int payResult = member.getMoney() - totalMoney;
        try {
            System.out.println("결제중입니다.....");
            t.sleep(2500);
        } catch (Exception e) {
            e.printStackTrace();
        }
        if(payResult < 0){
            System.out.println("잔액이 부족합니다. 대여 다시 해주세요");
        } else {
            member.setMoney(payResult);
            System.out.println("결제가 완료 되었습니다");
            System.out.println("이용해주셔서 감사합니다");
            memberNum++;
        }
    }
    
    private void end() {
        int s = 1;
        StringBuffer message = new StringBuffer();
        message.append("\n\n ")
            .append("+----------------------------------------------------+\n ")
            .append("|                                                    | \n ")
            .append("|           " + member.getMemberName() + " 회원님 대여하신 도서 준비되었습니다         | " + "\n");
        System.out.print(message);
        for (Map.Entry<String, Integer> rental : member.getBookRental().entrySet()) {
            System.out.printf(" | [%d] %-20s : %2d일  %7s |\n", 
                s, rental.getKey(), rental.getValue(), "");
            s++;
        }
        System.out.println(" |                                                    |");
        System.out.println(" +----------------------------------------------------+");
    }
}
```

**주요 변경사항:**
- `reOrder` → `reRental` (재대여)
- `order()` → `rental()` (대여)
- `orderCount` → `rentalDays` (대여일수)
- `coffee` → `book` (도서)
- `customer` → `member` (회원)

---

### 4. BookRentalImpl.java - 메인 클래스

```java
package a0331.book;

public class BookRentalImpl {
    public static void main(String[] args) {
        BookService bookService = new BookService();
        bookService.start();
    }
}
```

---

## 커피 시스템과의 차이점

### 1. 용어 변경

| 커피 시스템 | 도서 시스템 |
|------------|------------|
| 커피 | 도서 |
| 잔 | 일 |
| 주문 | 대여 |
| 고객 | 회원 |
| Coffee | Book |
| Customer | Member |

### 2. 계산 방식

**커피:**
```java
coffeePrice = unitPrice * orderCount;  // 가격 × 수량
```

**도서:**
```java
bookPrice = unitPrice * rentalDays;  // 가격 × 대여일수
```

### 3. 초기 잔액

- **커피**: 20000원
- **도서**: 30000원 (도서 대여가 더 비쌀 수 있음)

---

## 핵심 개념 정리

### 1. 싱글톤 패턴 (동일)

```java
private static Book instance;
public static Book getInstance(){
    if(instance == null){
        instance = new Book();
    }
    return instance;
}
```

### 2. LinkedHashMap (동일)

- 순서 보장
- 도서 번호 순서대로 출력

### 3. 재대여 기능

```java
if(reRental){
    if(rentalList.containsKey(bookName)){
        // 같은 도서 재대여 - 기간 누적
        int addDays = rentalList.get(bookName) + rentalDays;
        rentalList.replace(bookName, addDays);
    } else {
        // 새로운 도서 추가
        rentalList.put(bookName, rentalDays);
    }
}
```

**개선된 로직:**
- 커피 시스템의 버그를 수정
- `containsKey()`로 직접 확인
- 더 명확한 로직

### 4. 금액 계산

```java
int unitPrice = book.menu.get(bookName);  // 1일당 가격
bookPrice = unitPrice * rentalDays;        // 총 가격
```

**예시:**
- 자바의 정석: 1000원/일
- 3일 대여: 1000 × 3 = 3000원

---

## 실행 흐름

```
1. BookRentalImpl.main() 실행
   ↓
2. BookService 객체 생성
   ↓
3. start() 메서드 호출
   ├── 인사말 출력
   ├── Member 객체 생성 (회원번호 1, 잔액 30000원)
   ├── Book.getInstance()로 도서 객체 가져오기
   ├── getMenu()로 도서 목록 출력
   ├── rental()로 대여 받기
   │   ├── 도서 번호 선택
   │   ├── 대여일수 입력
   │   ├── 대여 리스트에 추가
   │   └── addRental()로 추가 대여 확인
   │       └── "예" 선택 시 rental() 재호출 (재귀)
   ├── totalRental()로 대여 내역 및 총액 출력
   ├── payment()로 결제 처리
   │   ├── 잔액 확인
   │   ├── 결제 시뮬레이션 (2.5초 대기)
   │   └── 잔액 업데이트
   ├── 2초 대기 (도서 준비 시간)
   └── end()로 완료 메시지 출력
```

---

## 코드 개선 사항

### 1. 재대여 로직 개선

**커피 시스템의 문제점:**
```java
// 버그가 있는 코드
for(String coff : orderList.keySet()){
    if(coff.equals(coffeeName)){
        // ...
    } else {
        orderList.put(coffeeName, orderCount);
        break;  // 문제!
    }
}
```

**도서 시스템의 개선:**
```java
// 개선된 코드
if(reRental){
    if(rentalList.containsKey(bookName)){
        // 같은 도서 재대여
        int addDays = rentalList.get(bookName) + rentalDays;
        rentalList.replace(bookName, addDays);
    } else {
        // 새로운 도서 추가
        rentalList.put(bookName, rentalDays);
    }
}
```

**장점:**
- `containsKey()`로 직접 확인
- 로직이 명확함
- 버그 없음

---

## 추가 기능 구현 예시

### 1. 연체료 계산 추가

```java
private int calculateLateFee(int rentalDays) {
    if(rentalDays > 7) {
        int lateDays = rentalDays - 7;
        return lateDays * 500;  // 1일당 500원
    }
    return 0;
}

private void totalRental(Member member) {
    // ... 기존 코드 ...
    
    int lateFee = 0;
    for(Map.Entry<String, Integer> rental : member.getBookRental().entrySet()){
        int rentalDays = rental.getValue();
        lateFee += calculateLateFee(rentalDays);
        // ... 기존 계산 ...
    }
    
    totalMoney += lateFee;
    if(lateFee > 0) {
        message.append(" | 연체료: " + f.format(lateFee) + " |\n");
    }
    // ...
}
```

### 2. 할인 기능 추가

```java
private int calculateDiscount(int totalMoney) {
    if(totalMoney >= 20000) {
        return (int)(totalMoney * 0.15);  // 15% 할인
    } else if(totalMoney >= 10000) {
        return (int)(totalMoney * 0.10);  // 10% 할인
    }
    return 0;
}

private void totalRental(Member member) {
    // ... 기존 코드 ...
    
    int discount = calculateDiscount(totalMoney);
    int finalTotal = totalMoney - discount;
    
    if(discount > 0) {
        message.append(" | 할인 금액: " + f.format(discount) + " |\n");
    }
    message.append(" ============ 최종 결제 금액은 " + f.format(finalTotal) + "입니다 ========== \n");
    payment(finalTotal);
}
```

---

## 학습 포인트 비교

### 커피 시스템에서 학습한 것

1. 싱글톤 패턴
2. LinkedHashMap
3. DecimalFormat
4. 재귀 호출
5. Thread.sleep()

### 도서 시스템에서 추가로 학습할 것

1. **동일한 패턴의 응용**: 같은 구조를 다른 도메인에 적용
2. **용어 변경**: 비즈니스 로직에 맞는 용어 사용
3. **로직 개선**: 커피 시스템의 버그 수정
4. **확장성**: 연체료, 할인 등 추가 기능 구현

---

## 실전 예시

### 실행 결과

```
 어서오세요 더조은도서관입니다.

+---------------------------------------------------+
+------------------------도서목록----------------------+
|            Book                   price           |
: [1] 자바의 정석              1,000원          :
: [2] 이것이 자바다            1,200원          :
: [3] Do it! 자바 프로그래밍   1,000원          :
: [4] Head First Java         1,500원          :
: [5] Effective Java         1,800원          :
: [6] Clean Code             1,300원          :
+----------------------------------------------------+

 취소를 원하시면 0번을 눌러주세요

원하는 도서의 번호를 선택해주세요 :1
선택 하신 도서는 : 자바의 정석입니다. 며칠 대여하시겠습니까?3

대여를 계속 하시겠습니까?
예(Y)/아니오(N)
y

원하는 도서의 번호를 선택해주세요 :1
선택 하신 도서는 : 자바의 정석입니다. 며칠 대여하시겠습니까?2

대여를 계속 하시겠습니까?
예(Y)/아니오(N)
n

+----------------------------------------------------+
 |                                                    | 
 |             회원 1번 회원님 의 대여 내역 입니다         | 
 | [1] 자바의 정석           :  5일    5,000원 |
 |                                                    |
+----------------------------------------------------+ 
 ============ 총 결제 금액은 5,000원입니다 ========== 

결제를 도와 드리겠습니다 카드 넣어주세요
결제중입니다.....
결제가 완료 되었습니다
이용해주셔서 감사합니다
기다려 주시면 대여하신 도서를 준비하겠습니다.

+----------------------------------------------------+
 |                                                    | 
 |           회원 1 회원님 대여하신 도서 준비되었습니다         | 
 | [1] 자바의 정석           :  5일            |
 |                                                    |
+----------------------------------------------------+
```

**재대여 확인:**
- 첫 번째: 자바의 정석 3일
- 두 번째: 자바의 정석 2일 추가
- 결과: 자바의 정석 5일 (누적됨)

---

## 체크리스트

- [x] 싱글톤 패턴 구현
- [x] LinkedHashMap 사용
- [x] DecimalFormat 포맷팅
- [x] 재대여 기능 (개선된 로직)
- [x] 대여 내역 출력
- [x] 결제 시스템
- [x] Thread.sleep() 지연 효과
- [x] 예외 처리

---

## 다음 단계

1. **연체료 기능 추가**
2. **할인 기능 추가**
3. **대여 취소 기능**
4. **도서 검색 기능**
5. **대여 기간 연장 기능**

---

**이 프로젝트는 커피 주문 시스템의 패턴을 다른 도메인에 응용하는 좋은 예제입니다!**
