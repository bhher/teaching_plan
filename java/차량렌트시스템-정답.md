# 차량 렌트 시스템 - 정답

## 📁 Vehicle.java

```java
package a0324.vehicle;

public class Vehicle {
    private String model;
    private String manufacturer;
    private String location;
    private String plateNumber;
    private boolean available;

    // 기본 생성자
    public Vehicle() {
    }
    
    // 매개변수 생성자
    public Vehicle(String model, String manufacturer, String location, String plateNumber) {
        this.model = model;
        this.manufacturer = manufacturer;
        this.location = location;
        this.plateNumber = plateNumber;
        this.available = true; // 생성 시 대여 가능 상태
    }

    // Getter/Setter
    public String getModel() {
        return model;
    }
    public void setModel(String model) {
        this.model = model;
    }
    public String getManufacturer() {
        return manufacturer;
    }
    public void setManufacturer(String manufacturer) {
        this.manufacturer = manufacturer;
    }
    public String getLocation() {
        return location;
    }
    public void setLocation(String location) {
        this.location = location;
    }
    public String getPlateNumber() {
        return plateNumber;
    }
    public void setPlateNumber(String plateNumber) {
        this.plateNumber = plateNumber;
    }
    public boolean isAvailable() {
        return available;
    }
    public void setAvailable(boolean available) {
        this.available = available;
    }

    // 차량 정보 문자열 반환
    @Override
    public String toString() {
        return "모델 : " + model + ", 제조사 : " + manufacturer + ", 위치 : " + location 
                + ", 번호판 : " + plateNumber + ", 대여여부 : " + (available ? "대여가능" : "대여불가능");
    }

    // 차량 대여 처리 메서드
    public void rent() {
        this.available = false;
    }
}
```

---

## 📁 VehicleManager.java

```java
package a0324.vehicle;

import java.util.ArrayList;
import java.util.Scanner;

public class VehicleManager {
    private ArrayList<Vehicle> vehicles;
    private ArrayList<Vehicle> rentedVehicles;

    public VehicleManager() {
        vehicles = new ArrayList<>();
        rentedVehicles = new ArrayList<>();
        
        // 더미 데이터 추가
        vehicles.add(new Vehicle("소나타", "현대", "A구역", "12가3456"));
        vehicles.add(new Vehicle("캠리", "토요타", "B구역", "34나5678"));
        vehicles.add(new Vehicle("벤츠 E클래스", "메르세데스", "C구역", "56다7890"));
    }

    // 대여 가능한 차량 목록 출력
    public void showAvailableVehicles() {
        System.out.println("대여 가능한 차량 보기");
        for (Vehicle vehicle : vehicles) {
            if (vehicle.isAvailable()) {
                System.out.println(vehicle);
            }
        }
    }

    // 차량 대여 처리
    public boolean rentVehicle(String modelName) {
        for (Vehicle vehicle : vehicles) {
            if (vehicle.getModel().equalsIgnoreCase(modelName) && vehicle.isAvailable()) {
                vehicle.rent(); // 대여 처리
                rentedVehicles.add(vehicle); // 대여된 차량 목록에 추가
                return true;
            }
        }
        return false;
    }

    // 대여 중인 차량 목록 출력
    public void showRentedVehicles() {
        System.out.println("대여 중인 차량 보기");
        for (Vehicle vehicle : rentedVehicles) {
            System.out.println(vehicle);
        }
    }

    // 새로운 차량 추가
    public void addVehicle(String model, String manufacturer, String location, String plateNumber) {
        vehicles.add(new Vehicle(model, manufacturer, location, plateNumber));
    }

    // 차량 삭제
    public void removeVehicle(String modelName) {
        boolean result = false;
        for (Vehicle vehicle : vehicles) {
            if (vehicle.getModel().equalsIgnoreCase(modelName)) {
                if (vehicle.isAvailable()) { // 대여 중이 아닐 때만 삭제 가능
                    vehicles.remove(vehicle);
                    result = true;
                    break;
                } else {
                    result = false;
                    break;
                }
            }
        }
        if (result) {
            System.out.println("삭제됨");
        } else {
            System.out.println("삭제 안됨");
        }
    }

    // 차량 정보 수정
    public void updateVehicle(String modelName) {
        int index = -1;
        int menu = -1;
        boolean flag = true;
        Scanner sc = new Scanner(System.in);
        Vehicle targetVehicle = null;

        // 수정할 차량 찾기
        for (int i = 0; i < vehicles.size(); i++) {
            if (vehicles.get(i).getModel().equalsIgnoreCase(modelName)) {
                index = i;
                targetVehicle = vehicles.get(i);
                break;
            }
        }

        if (index != -1) { // 차량을 찾은 경우
            System.out.print("무엇을 수정할까요?\n 1.모델명 \t 2.제조사 \t 3.위치 \t 4.번호판 \n >> ");
            menu = sc.nextInt();
            sc.nextLine(); // 버퍼 초기화

            while (flag) {
                switch (menu) {
                    case 1:
                        System.out.print("수정할 모델명: ");
                        targetVehicle.setModel(sc.nextLine());
                        vehicles.set(index, targetVehicle);
                        flag = false;
                        break;
                    case 2:
                        System.out.print("수정할 제조사: ");
                        targetVehicle.setManufacturer(sc.nextLine());
                        vehicles.set(index, targetVehicle);
                        flag = false;
                        break;
                    case 3:
                        System.out.print("수정할 위치: ");
                        targetVehicle.setLocation(sc.nextLine());
                        vehicles.set(index, targetVehicle);
                        flag = false;
                        break;
                    case 4:
                        System.out.print("수정할 번호판: ");
                        targetVehicle.setPlateNumber(sc.nextLine());
                        vehicles.set(index, targetVehicle);
                        flag = false;
                        break;
                    default:
                        System.out.println("1~4번 중에 입력하세요");
                        break;
                }
            }
        } else {
            System.out.println("찾는 차량이 없어서 수정할 수 없습니다.");
        }
    }

    // 차량 상세 정보 조회
    public void showVehicleInfo(String modelName) {
        for (Vehicle vehicle : vehicles) {
            if (vehicle.getModel().equalsIgnoreCase(modelName)) {
                System.out.println(vehicle.toString());
            }
        }
    }
}
```

---

## 📁 RentSystem.java

```java
package a0324.vehicle;

import java.util.Scanner;

public class RentSystem {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        VehicleManager manager = new VehicleManager();
        boolean flag = true;

        while (flag) {
            System.out.println("\n차량 렌트 시스템에 오신 것을 환영합니다.");
            System.out.println("1. 대여 가능한 차량 보기");
            System.out.println("2. 차량 대여하기");
            System.out.println("3. 대여 중인 차량 보기");
            System.out.println("4. 차량 추가하기");
            System.out.println("5. 차량 삭제하기");
            System.out.println("6. 차량 정보 수정하기");
            System.out.println("7. 차량 정보 조회하기");
            System.out.println("8. 종료");
            System.out.print("원하는 작업을 선택하세요 >> ");
            
            int choice = sc.nextInt();
            sc.nextLine(); // 버퍼 초기화

            switch (choice) {
                case 1:
                    System.out.println("대여 가능한 차량 보기");
                    manager.showAvailableVehicles();
                    break;

                case 2:
                    System.out.println("차량 대여하기");
                    System.out.print("대여 하려는 차량의 모델명을 입력하세요: ");
                    String modelName = sc.nextLine();
                    if (manager.rentVehicle(modelName)) {
                        System.out.println("차량이 성공적으로 대여되었습니다.");
                    } else {
                        System.out.println("차량이 존재하지 않거나 대여 불가능합니다.");
                    }
                    break;

                case 3:
                    System.out.println("대여 중인 차량 보기");
                    manager.showRentedVehicles();
                    break;

                case 4:
                    System.out.println("차량 추가하기");
                    System.out.print("추가 차량 모델명: ");
                    String newModel = sc.nextLine();
                    System.out.print("추가 차량 제조사: ");
                    String newManufacturer = sc.nextLine();
                    System.out.print("차량 위치: ");
                    String newLocation = sc.nextLine();
                    System.out.print("차량 번호판: ");
                    String newPlateNumber = sc.nextLine();
                    manager.addVehicle(newModel, newManufacturer, newLocation, newPlateNumber);
                    System.out.println("차량 추가 완료되었습니다.");
                    break;

                case 5:
                    System.out.println("삭제 시작");
                    System.out.print("삭제하는 차량 모델명을 입력하세요: ");
                    String dname = sc.nextLine();
                    if (dname.equals("")) {
                        System.out.println("삭제 하려는 차량 모델명을 다시 입력 해주세요");
                        dname = sc.nextLine();
                    }
                    manager.removeVehicle(dname);
                    System.out.println("삭제 완료");
                    break;

                case 6:
                    System.out.println("수정 시작");
                    System.out.print("수정 하려는 차량 모델명을 입력 해주세요: ");
                    String uname = sc.nextLine();
                    if (uname.equals("")) {
                        System.out.println("수정 하려는 차량 모델명을 다시 입력 해주세요");
                        uname = sc.nextLine();
                    }
                    manager.updateVehicle(uname);
                    System.out.println("수정 완료");
                    break;

                case 7:
                    System.out.println("조회 시작");
                    System.out.print("조회할 차량 모델명 입력: ");
                    String sname = sc.nextLine();
                    if (sname.equals("")) {
                        System.out.println("조회 하려는 차량 모델명을 다시 입력 해주세요");
                        sname = sc.nextLine();
                    }
                    manager.showVehicleInfo(sname);
                    System.out.println("조회 완료");
                    break;

                case 8:
                    System.out.println("프로그램을 종료합니다.");
                    flag = false;
                    break;

                default:
                    System.out.println("1~8번 중에 선택하세요.");
                    break;
            }
        }
        sc.close();
        System.exit(0);
    }
}
```

---

## 🔍 코드 설명

### 1. Vehicle 클래스
- **필드**: 차량의 기본 정보와 대여 상태를 저장
- **생성자**: 기본 생성자와 매개변수 생성자 제공
- **`rent()` 메서드**: 차량 대여 처리 (`available = false`)

### 2. VehicleManager 클래스
- **`vehicles`**: 전체 차량 리스트
- **`rentedVehicles`**: 대여 중인 차량 리스트
- **CRUD 메서드**: 차량의 생성, 읽기, 수정, 삭제 기능 제공
- **상태 관리**: 대여 가능/불가능 상태 관리

### 3. RentSystem 클래스
- **메뉴 시스템**: 사용자와 상호작용하는 메인 프로그램
- **입력 처리**: Scanner를 사용한 사용자 입력 처리
- **예외 처리**: 빈 문자열 체크 및 재입력

---

## 🎯 핵심 포인트

1. **ArrayList 활용**: 동적 리스트 관리
2. **캡슐화**: private 필드와 public 메서드
3. **상태 관리**: `available` 필드로 대여 상태 관리
4. **문자열 비교**: `equalsIgnoreCase()`로 대소문자 무시 비교
5. **객체 참조**: 같은 객체를 두 리스트에서 공유

