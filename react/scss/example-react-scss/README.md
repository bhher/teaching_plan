# example-react-scss

`react/scss/SCSS-React-가이드.md` 내용을 반영한 **최소 예제**입니다.

- `sass` 패키지로 `.scss` 컴파일
- `App.scss` — `@use`로 변수, 중첩, `@mixin`
- `DemoCard.scss` — 컴포넌트 옆 일반 SCSS
- `ModuleDemo.module.scss` — CSS Module (클래스 고유화)

```bash
npm install
npm run dev
```
