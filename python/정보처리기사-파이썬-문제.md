# 정보처리기사 파이썬 프로그래밍 언어 문제

**총 50문제**

---

## 문제 1
다음 코드의 실행 결과는?

```python
x = 10
y = 3
print(x // y, x % y)
```

① 3 1  
② 3 3  
③ 1 3  
④ 3.33 1

**정답:** ①

---

## 문제 2
다음 코드의 실행 결과는?

```python
s = "Python"
print(s[1:4])
```

① Pyt  
② yth  
③ ytho  
④ Python

**정답:** ②

---

## 문제 3
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list2 = list1
list2.append(4)
print(list1)
```

① [1, 2, 3]  
② [1, 2, 3, 4]  
③ [4]  
④ 에러 발생

**정답:** ②

---

## 문제 4
다음 코드의 실행 결과는?

```python
dict1 = {'a': 1, 'b': 2, 'c': 3}
print(dict1.get('d', 0))
```

① None  
② 0  
③ 에러 발생  
④ 'd'

**정답:** ②

---

## 문제 5
다음 코드의 실행 결과는?

```python
result = [x**2 for x in range(5) if x % 2 == 0]
print(result)
```

① [0, 1, 4, 9, 16]  
② [0, 4, 16]  
③ [1, 9]  
④ [0, 2, 4]

**정답:** ②

---

## 문제 6
다음 코드의 실행 결과는?

```python
def func(x, y=10):
    return x + y

print(func(5))
```

① 5  
② 10  
③ 15  
④ 에러 발생

**정답:** ③

---

## 문제 7
다음 코드의 실행 결과는?

```python
s = "Hello World"
print(s.upper().lower())
```

① HELLO WORLD  
② hello world  
③ Hello World  
④ 에러 발생

**정답:** ②

---

## 문제 8
다음 코드의 실행 결과는?

```python
list1 = [3, 1, 4, 1, 5]
list1.sort()
print(list1)
```

① [1, 1, 3, 4, 5]  
② [3, 1, 4, 1, 5]  
③ [5, 4, 3, 1, 1]  
④ 에러 발생

**정답:** ①

---

## 문제 9
다음 코드의 실행 결과는?

```python
x = [1, 2, 3]
y = x[:]
y[0] = 100
print(x)
```

① [100, 2, 3]  
② [1, 2, 3]  
③ [100]  
④ 에러 발생

**정답:** ②

---

## 문제 10
다음 코드의 실행 결과는?

```python
result = sum(range(1, 6))
print(result)
```

① 10  
② 15  
③ 20  
④ 25

**정답:** ②

---

## 문제 11
다음 코드의 실행 결과는?

```python
s = "Python Programming"
print(s.split())
```

① ['Python', 'Programming']  
② ['P', 'y', 't', 'h', 'o', 'n']  
③ 'Python Programming'  
④ 에러 발생

**정답:** ①

---

## 문제 12
다음 코드의 실행 결과는?

```python
dict1 = {'a': 1, 'b': 2}
dict2 = {'c': 3, 'd': 4}
dict1.update(dict2)
print(len(dict1))
```

① 2  
② 4  
③ 6  
④ 에러 발생

**정답:** ②

---

## 문제 13
다음 코드의 실행 결과는?

```python
for i in range(3):
    if i == 1:
        continue
    print(i, end=' ')
```

① 0 1 2  
② 0 2  
③ 1 2  
④ 0 1

**정답:** ②

---

## 문제 14
다음 코드의 실행 결과는?

```python
x = 10
y = 20
result = "x는 y보다 크다" if x > y else "x는 y보다 작거나 같다"
print(result)
```

① x는 y보다 크다  
② x는 y보다 작거나 같다  
③ True  
④ False

**정답:** ②

---

## 문제 15
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list2 = [4, 5, 6]
result = list1 + list2
print(result)
```

① [1, 2, 3, 4, 5, 6]  
② [5, 7, 9]  
③ [1, 2, 3][4, 5, 6]  
④ 에러 발생

**정답:** ①

---

## 문제 16
다음 코드의 실행 결과는?

```python
s = "hello"
print(s[::-1])
```

① hello  
② olleh  
③ h  
④ 에러 발생

**정답:** ②

---

## 문제 17
다음 코드의 실행 결과는?

```python
def func(*args):
    return sum(args)

print(func(1, 2, 3, 4))
```

① 10  
② (1, 2, 3, 4)  
③ 에러 발생  
④ 4

**정답:** ①

---

## 문제 18
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3, 4, 5]
result = list(filter(lambda x: x % 2 == 0, list1))
print(result)
```

① [1, 3, 5]  
② [2, 4]  
③ [1, 2, 3, 4, 5]  
④ []

**정답:** ②

---

## 문제 19
다음 코드의 실행 결과는?

```python
try:
    result = 10 / 0
except ZeroDivisionError:
    print("에러 발생")
else:
    print("정상 실행")
finally:
    print("마지막")
```

① 에러 발생  
② 정상 실행  
③ 에러 발생\n마지막  
④ 마지막

**정답:** ③

---

## 문제 20
다음 코드의 실행 결과는?

```python
list1 = ['a', 'b', 'c']
for idx, value in enumerate(list1):
    print(idx, value, end=' ')
```

① 0 a 1 b 2 c  
② a b c  
③ 0 1 2  
④ 에러 발생

**정답:** ①

---

## 문제 21
다음 코드의 실행 결과는?

```python
dict1 = {1: 'a', 2: 'b', 3: 'c'}
print(list(dict1.keys()))
```

① ['a', 'b', 'c']  
② [1, 2, 3]  
③ [(1, 'a'), (2, 'b'), (3, 'c')]  
④ 에러 발생

**정답:** ②

---

## 문제 22
다음 코드의 실행 결과는?

```python
s = "  Hello World  "
print(s.strip())
```

① "  Hello World  "  
② "Hello World"  
③ "Hello World  "  
④ "  Hello World"

**정답:** ②

---

## 문제 23
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list1.insert(1, 10)
print(list1)
```

① [1, 10, 2, 3]  
② [1, 2, 10, 3]  
③ [10, 1, 2, 3]  
④ [1, 2, 3, 10]

**정답:** ①

---

## 문제 24
다음 코드의 실행 결과는?

```python
x = [1, 2, 3]
y = x
y = [4, 5, 6]
print(x)
```

① [1, 2, 3]  
② [4, 5, 6]  
③ [1, 2, 3, 4, 5, 6]  
④ 에러 발생

**정답:** ①

---

## 문제 25
다음 코드의 실행 결과는?

```python
result = max([1, 5, 3, 9, 2])
print(result)
```

① 1  
② 9  
③ 5  
④ 20

**정답:** ②

---

## 문제 26
다음 코드의 실행 결과는?

```python
s = "Python"
print(s.replace('P', 'J'))
```

① Python  
② Jython  
③ P  
④ 에러 발생

**정답:** ②

---

## 문제 27
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list1.pop()
print(list1)
```

① [1, 2]  
② [2, 3]  
③ [1, 2, 3]  
④ []

**정답:** ①

---

## 문제 28
다음 코드의 실행 결과는?

```python
def func(x):
    return x * 2

result = list(map(func, [1, 2, 3]))
print(result)
```

① [2, 4, 6]  
② [1, 2, 3]  
③ [1, 4, 9]  
④ 에러 발생

**정답:** ①

---

## 문제 29
다음 코드의 실행 결과는?

```python
set1 = {1, 2, 3}
set2 = {3, 4, 5}
print(set1 & set2)
```

① {1, 2, 3, 4, 5}  
② {3}  
③ {1, 2, 4, 5}  
④ 에러 발생

**정답:** ②

---

## 문제 30
다음 코드의 실행 결과는?

```python
x = "123"
y = int(x)
print(y + 10)
```

① 12310  
② 133  
③ 에러 발생  
④ "12310"

**정답:** ②

---

## 문제 31
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3, 4, 5]
result = [x for x in list1 if x > 3]
print(result)
```

① [1, 2, 3]  
② [4, 5]  
③ [True, True, True, True, True]  
④ []

**정답:** ②

---

## 문제 32
다음 코드의 실행 결과는?

```python
dict1 = {'a': 1, 'b': 2}
print('a' in dict1)
```

① True  
② False  
③ 1  
④ 'a'

**정답:** ①

---

## 문제 33
다음 코드의 실행 결과는?

```python
s = "Hello"
print(len(s))
```

① 4  
② 5  
③ 6  
④ 에러 발생

**정답:** ②

---

## 문제 34
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list1.extend([4, 5])
print(list1)
```

① [1, 2, 3, [4, 5]]  
② [1, 2, 3, 4, 5]  
③ [4, 5]  
④ 에러 발생

**정답:** ②

---

## 문제 35
다음 코드의 실행 결과는?

```python
x = 10
y = "20"
print(x + int(y))
```

① 1020  
② 30  
③ 에러 발생  
④ "1020"

**정답:** ②

---

## 문제 36
다음 코드의 실행 결과는?

```python
for i in range(2, 6, 2):
    print(i, end=' ')
```

① 2 4  
② 2 4 6  
③ 0 2 4  
④ 에러 발생

**정답:** ①

---

## 문제 37
다음 코드의 실행 결과는?

```python
list1 = ['a', 'b', 'c']
result = ''.join(list1)
print(result)
```

① 'a b c'  
② 'abc'  
③ ['a', 'b', 'c']  
④ 에러 발생

**정답:** ②

---

## 문제 38
다음 코드의 실행 결과는?

```python
dict1 = {1: 'one', 2: 'two'}
print(dict1.pop(1))
```

① 1  
② 'one'  
③ {2: 'two'}  
④ 에러 발생

**정답:** ②

---

## 문제 39
다음 코드의 실행 결과는?

```python
x = [1, 2, 3]
y = x.copy()
y[0] = 100
print(x)
```

① [100, 2, 3]  
② [1, 2, 3]  
③ [100]  
④ 에러 발생

**정답:** ②

---

## 문제 40
다음 코드의 실행 결과는?

```python
s = "Python"
print(s.find('t'))
```

① 2  
② 3  
③ True  
④ -1

**정답:** ①

---

## 문제 41
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list2 = list1
list2 = list2 + [4]
print(list1)
```

① [1, 2, 3]  
② [1, 2, 3, 4]  
③ [4]  
④ 에러 발생

**정답:** ①

---

## 문제 42
다음 코드의 실행 결과는?

```python
def func(a, b=5, c=10):
    return a + b + c

print(func(1, 2))
```

① 13  
② 16  
③ 8  
④ 에러 발생

**정답:** ①

---

## 문제 43
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3, 4, 5]
result = list1[1:4:2]
print(result)
```

① [2, 3]  
② [2, 4]  
③ [1, 3]  
④ [2, 3, 4]

**정답:** ②

---

## 문제 44
다음 코드의 실행 결과는?

```python
x = "Hello"
y = x
x = "World"
print(y)
```

① Hello  
② World  
③ Hello World  
④ 에러 발생

**정답:** ①

---

## 문제 45
다음 코드의 실행 결과는?

```python
dict1 = {'a': 1, 'b': 2}
for key in dict1:
    print(key, end=' ')
```

① a b  
② 1 2  
③ ('a', 1) ('b', 2)  
④ 에러 발생

**정답:** ①

---

## 문제 46
다음 코드의 실행 결과는?

```python
list1 = [1, 2, 3]
list1.reverse()
print(list1)
```

① [3, 2, 1]  
② [1, 2, 3]  
③ [3]  
④ 에러 발생

**정답:** ①

---

## 문제 47
다음 코드의 실행 결과는?

```python
s = "Python"
print(s.count('P'))
```

① 0  
② 1  
③ True  
④ False

**정답:** ②

---

## 문제 48
다음 코드의 실행 결과는?

```python
result = sorted([3, 1, 4, 1, 5])
print(result)
```

① [1, 1, 3, 4, 5]  
② [3, 1, 4, 1, 5]  
③ [5, 4, 3, 1, 1]  
④ 에러 발생

**정답:** ①

---

## 문제 49
다음 코드의 실행 결과는?

```python
x = [1, 2, 3]
y = [4, 5, 6]
result = list(zip(x, y))
print(result)
```

① [(1, 4), (2, 5), (3, 6)]  
② [1, 2, 3, 4, 5, 6]  
③ [(1, 2, 3), (4, 5, 6)]  
④ 에러 발생

**정답:** ①

---

## 문제 50
다음 코드의 실행 결과는?

```python
class MyClass:
    def __init__(self, value):
        self.value = value
    
    def get_value(self):
        return self.value

obj = MyClass(10)
print(obj.get_value())
```

① 10  
② self.value  
③ value  
④ 에러 발생

**정답:** ①
